interface:
  display_name: "Agent-Facing Design"
  short_description: "Avoid unnecessary agent-process machinery"
  default_prompt: "Use $agent-facing-design to keep this agent-facing change judgment-supporting without unnecessary process machinery; inspect the live target when one exists, hand off to $writing-principles for instruction-doc prose edits when applicable, otherwise ask one concrete question or hand off to the owning workflow."
