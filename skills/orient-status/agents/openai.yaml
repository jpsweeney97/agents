interface:
  display_name: "Orient Status"
  short_description: "Read-only live status orientation"
  default_prompt: "Use $orient-status for a read-only status brief; planning, cleanup, and ticket actions are only done when explicitly requested."
