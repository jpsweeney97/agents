interface:
  display_name: "Simplify Code"
  short_description: "Safely execute scoped behavior-preserving cleanup"
  default_prompt: "Use $simplify-code to execute the targeted behavior-preserving cleanup, select the correct safety lane, use the backup helper when needed, and report concise verification evidence."
