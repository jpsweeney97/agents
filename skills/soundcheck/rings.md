# Soundcheck ring ledger

One line per ring: `date | project | rule(cause), rule(cause), … | note`.
Causes: `attention` (rule clear, salience lost — the default) or `text` (rule ambiguous, colliding, or missing a case — requires a quote).
Clean rings get logged too. Silent-but-clean closeout rings do not. See SKILL.md.

2026-08-06 | playpen | one-idea-per-sentence(attention), define-jargon(attention), half-length(attention, at-risk only) | ring 1 — v1 build test; cause reads applied retroactively during v1.1 design
2026-08-06 | playpen | half-length(attention), long-analysis-to-file(text), define-jargon(attention) | ring 2 — rung mid-ideate arc; capsule+17-option field stacked unoffered; patch note → charter gate
2026-08-06 | playpen | say-it-in-place(attention) | ring 3 — user-caught in passing, post-ring-2: "the parked question" backref forced a scroll-back; logged at session close as promised
2026-09-04 | cross-model | define-jargon(attention), literal-language(attention), one-decision-questions(attention), half-length(attention, at-risk) | ring 1 this session, rung by "still too loud, too much jargon" after two too-loud remixes; define-jargon now 3 attention rings across ledger → chronic; patch note: reposition the jargon bullet into Reply Shape → charter gate
2026-09-04 | agents | long-analysis-to-file(text), literal-language(attention) | ring 1 this session, rung "too loud" after the deliberate close was pasted in full (~1,800 words) into the chat; text cause: deliberate SKILL.md Close "the Recommend stage's close, as written" vs Reply Shape "A long analysis goes to a file"; patch note → deliberate Close (skill side, build-and-prune) with the Reply Shape Exception clarification as the charter-gate alternative; ledger append held: rings.md on main already carries an uncommitted line from a concurrent cross-model session
