interface:
  display_name: "Markdown Synthesis"
  short_description: "Synthesize Markdown into a standalone document"
  default_prompt: "Use $markdown-synthesis to synthesize these Markdown sources into one polished standalone Markdown document."
