---
type: review
date: 2026-09-01
scope: "Mining pass 2 over the pass-1 verdicts: fresh-skeptic stress-test of the 5 admits, cross-batch collision consolidation, and a two-agent coverage critique of the pass itself"
supersedes-in-part: "docs/reviews/2026-09-01-skill-repo-mining-pass-1.md — its five-admit list, its 27-fold inventory, and its per-verdict ledger posture are superseded by the sections below; its verdict index, digests, parks, rejects, and donor pins stand"
reviewed_commit: b19a915a (pass-1 report landed); library skill surfaces unchanged since 1257617d
method: "Six agents recovered from the stopped consolidation workflow wf_6abd8c89-cd9 (5 admit skeptics + 1 collision resolver, complete in its journal; cross-session cache resume is impossible, so results were extracted by hand), plus 2 fresh coverage agents this session (file census; methodology critique), plus 8 live spot-checks of the skeptics' most load-bearing claims by the reporting session. The old workflow's parks-and-limits agent never ran; its park-trigger audit remains undone."
posture: "Recommendations, not decisions. Nothing was built, folded, or ledgered by this pass. Verdict flips below are proposals for JP's ratification; ledger entries land with the work, per the granularity decision this report puts to JP."
---

# Skill-Repo Mining Pass 2: admit stress-test, consolidation, coverage critique

## Executive summary

**Only 2 of the 5 pass-1 admits survive a fresh skeptic: `perf-optimize` (sustained and strengthened, HIGH) and `to-questionnaire` (sustained and re-scoped, MEDIUM). The other three flip: the web-performance audit to PARK (HIGH), docs-grounded framework code to PARK with a vendor-lane counter-proposal (MEDIUM), and the weakened-checks scan to REJECT (HIGH).** The collision resolver — whose completed output the pass-1 report never absorbed — collapses the digests' 40 fold clauses to 31 across 25 local files plus one plugin file, resolves the two-performance-admissions collision into one build, nets the karpathy folds to zero, and prices the plugin cost at one review-family minor cut plus a git-cycle hold. The coverage critique finds pass 1 trustworthy at row level (six of six thin rejects re-derived and standing; every re-checked load-bearing quote verbatim) with two systematic weaknesses: material mattpocock surfaces outside the skills tree were never assessed (`CLAUDE.md` drift, `.agents/invocation.md`, the new Claude-plugin packaging cluster), and verifier-surfaced items died silently between digest and report by attrition rather than policy.

Provenance disclosure: the six recovered agents ran in the stopped workflow before the Supersession amendment landed (their charter line citations are pre-`17dc693`; no verdict depends on the amended text), against the same library skill text that is live now (the branch commits since `1257617` touch only `docs/`). The 2026-09-01 handoff's claim that the stress-test "was never run" was wrong — the journal shows all five skeptics and the collision resolver completed before the stop; only the two consolidation-phase critics never finished, and this pass re-ran the coverage half fresh.

## The five admits after stress

Full skeptic returns: `mining-artifacts/pass2/stress-*.json` (see Pointers). Each ran at high effort with instructions to kill the admission, read donor and local surfaces firsthand, and search for observed friction. Eight of their most load-bearing claims were re-verified live by the reporting session (list under Evidence boundary); all eight held.

1. **`perf-optimize` — ADMIT sustained, strengthened (HIGH).** Four kill attempts failed. The standalone-vs-`diagnose`-branch question the 2026-07-01 capability review left open is settled standalone, on the charter's own worked example (qualitative pass/fail versus measured benchmark — the axis the `skill-benchmark` admission already ruled on) plus the routing cost of widening `diagnose` past its regression gate. The decisive new evidence is observed friction pass 1 never found: markdown-reader's own perf arc — the 2026-07-17 audit handoff records a project's whole measurement history invalidated by un-optimized `-Onone` baselines, 15 refuted claims living only in session temp storage (the attempt-ledger gap written out as a risk), and three landed perf implementations whose Evidence Checked lists contain zero before/after numbers, with roughly seven optimizations under one never-measured claim in issue #56. Scope correction: build the method spine (~90-120 lines — loop, keep-or-revert table with within-noise-= -revert, variance floor via `skill-benchmark`'s paired two-standard-error idiom, attempt ledger, regression guard, fences, proof boundary), not the domain encyclopedia — JP's stacks have no web frontend and no ORM, so the donor's SQL/React catalogs are nearly all dead here. Eight hazards recorded in the return, led by: the donor's commit instruction violates the protected-branch floor; the `PERF.md` attempt ledger needs the house durable-write floor; no numeric readiness scores.
2. **`to-questionnaire` — ADMIT sustained, re-scoped (MEDIUM).** Three kill attempts failed: `decision-owner-map`'s recipient decides while this recipient supplies knowledge (a difference in kind, with a one-line seam at its move 4); `courier`'s inbound contract treats replies as allegations to refute, which inverts against a domain expert's answers that are the authority the user lacks — the boundary the build must state because `stage-prompt:16` will otherwise misroute landers; and "JP works solo" is false — athena-kb-local holds four real due-diligence questionnaires and named counterparties. Re-scope: from the donor's "a decision you can't fully answer" to "a knowledge gap only one named person can close," which keeps One Owner Per Job clean. Keep `disable-model-invocation: true`; adopt the `runbook-authoring` artifact floor; add the `outcome-shaping` Exits row. Honest hazard: the compose side of the questionnaire genre is unobserved in JP's work (the athena questionnaires are received, not authored) — if it has not fired in three months, trash it without ceremony.
3. **Web-performance audit — FLIP ADMIT → PARK (HIGH).** The ownership half of the pass-1 verdict is true (re-verified: zero Core-Web-Vitals or WCAG text anywhere in the local trees) but the demand half is false and was never tested: no web frontend exists anywhere under `~/Projects` (zero `.tsx`/`.jsx`/`.vue`/`.svelte`, no app `index.html`; the only web artifact is a single userscript), the full transcript corpus shows zero user turns about page-speed vocabulary against working positive controls, and with no chrome-devtools MCP and no deployed URL the skill's distinguishing Metric-Honesty Rule can never leave Quick mode — the guard would have nothing to guard. Disposition: one merged frontend park (this, `frontend-ui-engineering`, and the frontend half of the performance checklist — one entry, one trigger) with a unified reopen trigger whose clause (c) counts published-artifact weight/slowness friction, not only app repos; `perf-optimize` takes the donor's proof-boundary idiom and the browser material as a load-only-when-browser reference, which also implements the collision resolver's demote-to-donor resolution. The accessibility-checklist fold stays out of the park — the review lens it lands in has a live surface today (rendered artifacts).
4. **Docs-grounded framework code — FLIP ADMIT → PARK (MEDIUM).** No single owner kills it (the skeptic says so plainly), but the admission fails on merit at generic scope: the value of all four existing instances is the vendor-specific retrieval surface, which a generic lane lacks; three of its five steps are already house vocabulary and its version step is weaker than `dependency-upgrade:14`'s lockfile rule; the trigger is anti-correlated with the need (fires when the agent is already suspicious); and installed language servers already answer the existence-and-signature half more authoritatively than any fetch. The skill-versus-AGENTS.md-line fork pass 1 posed is a false fork — the third-party gate is owed either way, and the ambient line is the more expensive shape. Counter-proposal recorded with the park: if anything is built, build an Apple/SwiftUI vendor lane at the scope where JP's framework work actually concentrates (788 recorded fires), where the current answer is `build-macos-apps@0.1.4` — an installed third-party plugin routed by an always-loaded line in markdown-reader's AGENTS.md that ships version-gated API assertions with zero citation discipline (re-verified live). Two side moves survive regardless: fold the surface-the-conflict step into `dependency-upgrade:20`, and queue `build-macos-apps@0.1.4` as un-mined third-party contract material — a larger live exposure than the gap this candidate named. Reopen trigger: a recorded correction where an agent shipped a deprecated or nonexistent framework API into JP's code, or sustained work in an ecosystem with no vendor lane and no language server.
5. **Weakened-checks scan — FLIP ADMIT → REJECT (HIGH).** Three owners, one of which pass 1 inverted: the ledgered 2026-06-13 decision (`contract-decisions.md:17`) deliberately admitted "repo-instruction violations explicitly silenced in the code" into `implementation-review:116` as a false-positive *exclusion* — the proposed skill's central detection would silently reverse a settled charter call with no new evidence; the bundled `/code-review`'s actual prompt bodies (read from the CLI binary, and re-verified there by this session) own removed-guard, deleted-test, and threshold-drop detection outright, and bundled contracts are owners under charter line 9; the remainder scatters across `substitution-ledger`, `test-trust-audit`, and the existing suppression/mutation lenses. Residual after subtraction: a newly added unreasoned `.skip` or suppression — about one and a half of five moves, a clause, not a skill. The friction search ran four independent instruments (usage ledger, decision ledger, memory, fingerprinted git history across four repos) and found nothing; JP's own tree carries reason-annotated suppressions that the donor's regex would flag wholesale. Reopen trigger, to be named in the ledger entry: one observed instance of an agent reaching green by adding an unreasoned suppression or skip — which reopens this as a narrow `review-lenses.md:5` extension re-litigating `:116` on the record, not as a skill. Method note worth keeping: check bundled contracts' bodies (extractable from the binary), never their one-line descriptions.

## Consolidation: what actually lands

The collision resolver's full return is `mining-artifacts/pass2/collisions.md` — the fold work-order there (Stages A–F) is the landing inventory, and it is more complete than the pass-1 report's fold section, which dropped several digest folds by attrition (see Coverage). Applying the stress flips to its tally: **2 new skills** (not 4), **31 fold clauses across 25 local files and 1 plugin file**, **one merged frontend park plus the existing parks**, **1 plugin release**. Key resolutions:

- **One perf build.** The two performance admissions were one job packaged twice (each batch blind to the other); the auditor's proof-boundary idiom and browser material ride inside `perf-optimize` as a conditional reference, and the standalone audit capability sits in the merged frontend park.
- **The karpathy folds net to zero.** Both orphan-detection folds resolve away: the review-side lens survives only as addy-A's version narrowed to objects a linter cannot see (preserving `implementation-review:113-114`), and the closeout-side fold drops (the class is deliberately assigned to lint plus `closeout-check:51`, and the Claude runtime already tools it via spawn-task). The karpathy batch's FOLD verdict token is therefore misleading as landed work.
- **Must land as one change:** the `injection-safe-inputs` folds (SSRF sink, model-output source, third-party-API entry point, upload magic-bytes) and the `red-team` LLM folds share the `:59` rewrite — one edit, or the two skills contradict each other about who owns prompt injection. Only SSRF touches the description.
- **Deduplications:** feature-flag lifecycle is one merged clause on `deploy-plan:16` (owner, removal date, both states work, no nested flags) with its detection half as a `tech-debt-scan:68` debt category; observability lands as 3 clauses not 5 (the test-fire and runbook-content clauses violate the target's own proof boundary); the cache lever is one fold whose home is a JP decision (writing-principles with a scope note, or agent-facing-design — never AGENTS.md); `wizard` gets one merged park entry (matt-NEW's text plus matt-DELTA's credential hazard).
- **Plugin cost:** review-family takes one minor cut (5 review-lenses edits, one optional companion sentence); **git-cycle holds** — its single surviving clause (route credential-shaped staged content to a security review lane before the final commit) rides the next natural bump, and per the methodology critique it must be phrased availability-conditionally because `/security-review` is Claude-bundled and its claimed finding class is unverifiable on this machine (cheapest settle: one live invocation on a diff with a planted fake credential). handoff receives nothing.
- **Ledger granularity conflict, for JP.** The pass-1 report's posture says every one of the 86 verdicts takes its own ledger entry; the collision resolver read the live ledger and found the actual precedent is one entry per source repo per pass (86 entries would swamp a 61-entry ledger). Proposed reconciliation: three per-repo pass entries covering the folds, rejects, and parks collectively, plus one Parks line per new park — and each admit that is actually built takes its own entry at build time, which matches both the per-repo precedent and the `resolve-conflicts` model entry pass 1 cites.
- **Ledger repairs to ride along:** the karpathy evidence pointer is `:610` not `:719`, and the `wayfinder` park entry quotes a command that appears at neither revision — fix the quote, not just the line number.
- **Extraction correction:** the pass-1 report says the donor clones are gone; they are not. All three survive in the old session's scratchpad at the recorded pins (which is what made this pass's firsthand re-checks possible). At landing time, Extraction closes by `trash`-ing the three clones — never `rm`.

## Coverage critique

Two fresh agents; full returns at `mining-artifacts/pass2/coverage-census.md` and `coverage-methodology.md`.

**File census: skills-tree coverage is effectively complete for all three donors; the genuine gaps are outside the skills tree, all in mattpocock, all inside the delta's declared remit.** (1) `CLAUDE.md` (±25) — the largest gap: plugin-shipping conventions, a four-section docs-page contract, the repo-wide no-em-dash rule (the doctrine behind the sweep every row characterized, itself never assessed), and the Codex implicit-invocation pairing rule. (2) `.agents/invocation.md` (±18) — the convention document mandating the restyling the pass correctly called cosmetic; never compared against the house invocation-token convention. (3) The new Claude-plugin packaging cluster (manifest, marketplace, ADR-0002 recording acceptance into claude-plugins-official, install-block, and a version-lockstep script that independently converges on fold 27's discipline) — packaging-follows-payload disposes of it, but pass 1 never said so, and should. Remaining uncovered surfaces are inert or substance-covered without a row (addyosmani's docs tree and five-ecosystem packaging, karpathy's translation README); one bookkeeping error (the test-suite row counts seven files; the tree holds eight, all still covered by the row's reasoning). The matt-DELTA diff-stat claim verified exactly; all three donor pins re-verified firsthand.

**Methodology critique: trustworthy at row level, weak at aggregation.** Every re-checked load-bearing quote verified verbatim; the web-vitals absence greps hold under re-run; all six thinnest rejects stand under independent re-derivation — one (`validate-artifact-paths.js`) stands on corrected grounds, because its recorded "exactly one skill writes and reads docs/adr/" claim is false (`decision-record:41` names grill-with-docs as a second producer; the verdict survives via ADR-FORMAT single-sourcing instead). Two systematic findings:

- **Concept-level blind spot from file-sharding:** a discipline spread across donor files never got a unit of assessment. Confirmed instance: rate-limiting/backpressure design — rich across six donor files, genuinely thin locally, already named as the unbuilt `rate-limit-backpressure` opening in the 2026-07-01 capability review's Opening C, noted once by the addy-D verifier, and absent from the pass-1 report's named-gaps section. Second, more modest instance: error-contract design (what errors a boundary exposes) — genuinely unowned locally, but the donor fragments are individually thin, so this is a method finding with a modest capability rider, not a missed admit. A third probe (launch/rollout/rollback) confirmed dense local ownership — the pass's coverage there is sound.
- **Silent attrition between digest and report:** the rate-limit gap note, the third-party-response entry-point fold, the stale-flag debt category, idea-refine's demand-evidence question, and wait-what's comprehension-repair candidate all appear in digests and nowhere in the pass-1 report, while other LOW items were kept — attrition, not policy. The first three are recovered by this pass (the collision work-order carries the two folds; the named-gaps addendum below carries the rate-limit opening). Recommended process fix for any future pass: an explicit carried-or-dropped-with-reason disposition line for every verifier-surfaced item, plus consulting the capability review's standing openings as a fixed per-batch cross-check — pass 1 both re-derived one of its named gaps from Opening C without citation and rejected pagination in silent contradiction of it.

**Named-gaps addendum to pass 1:** add `rate-limit-backpressure` (Opening C, corroborated by six donor files; no local owner — the system-design-review dimension is a name-only lens) and, lower value, error-contract design. Note the recorded tension: addy-A's pagination reject versus Opening C's `pagination-design` build-now listing — an open contradiction for whoever next works that queue, not resolved here.

## Decisions for JP

1. **Ratify the three flips** (web-performance → merged frontend park; docs-grounding → park with the vendor-lane alternative recorded; weakened-checks → reject with the named reopen trigger). Each is a case-(d) disposition change from pass 1's recommendation.
2. **The Apple/SwiftUI vendor-lane question** — new in this pass: build the docs-grounding lane at vendor scope where the observed work is, or leave the park bare.
3. **Queue `build-macos-apps@0.1.4` for mining** — installed, routed by an always-loaded project line, ships uncited version-gated API assertions; the skeptic rates it a larger live exposure than the gap its row addressed.
4. **Build order for the two surviving admits** (`perf-optimize` first on observed-friction grounds, per its skeptic).
5. **Ledger granularity** — per-repo pass entries with per-admit entries at build (recommended), or pass 1's per-verdict posture.
6. **C1, the `prototype` capture reversal** (reserved to JP since pass 1) and **C2, the cache-lever home**.
7. **git-cycle hold** (recommended) versus spending a publish train on one availability-conditional routing clause.
8. Standing from pass 1, unchanged by this pass: the `grill-me` serialize-versus-batch design question.

## Evidence boundary

What this report rests on: the six recovered agent returns (journal-confirmed complete; copied to the durable store), the two fresh coverage returns, and the reporting session's own eight live spot-checks — `contract-decisions.md:17`; `implementation-review:113-116`; `dependency-upgrade:14`; markdown-reader `AGENTS.md:23-31`; the 2026-07-17 perf-audit handoff; the athena-kb-local questionnaire tree; the zero-web-frontend find; and the `/code-review` Angle B text verbatim in CLI binary 2.1.252. What it does not rest on: re-running the five stress skeptics (each verdict is one high-effort skeptic, not a panel — the eight spot-checks are the corroboration layer); the old parks-and-limits agent's jobs (the park-trigger audit of this pass's parks and the six pre-existing mattpocock parks remains undone); re-verification of the ~200 pass-1 citations beyond the samples above (the build-time re-verify rule stands); and any executed fold, build, or forward test. The stress returns' charter line citations predate the `17dc693` amendment and may be off by a few lines against the live charter; no verdict turns on the amended text.

## Pointers

- Durable pass-2 artifacts: `/Users/jp/.claude/projects/-Users-jp--agents/93b9e21c-f650-48e2-a069-82eb18ce67e4/mining-artifacts/pass2/` — `stress-<candidate>.json` (5), `collisions.md` (the fold work-order, Stages A–F), `coverage-census.md`, `coverage-methodology.md`.
- Recovered-from: workflow journal `/Users/jp/.claude/projects/-Users-jp--agents/93b9e21c-f650-48e2-a069-82eb18ce67e4/subagents/workflows/wf_6abd8c89-cd9/journal.jsonl`; script `workflows/scripts/mine-consolidate-wf_6abd8c89-cd9.js` under the same project dir. The workflow is closed by this report — do not resume it.
- Pass 1: `docs/reviews/2026-09-01-skill-repo-mining-pass-1.md` (verdict index, digests, donor pins, charter posture — superseded only where this report says so).
- Donor clones, still live at the recorded pins until landing-time Extraction trashes them: `/private/tmp/claude-501/-Users-jp--agents/93b9e21c-f650-48e2-a069-82eb18ce67e4/scratchpad/mined/`.
- Capability review cross-referenced throughout: `docs/reviews/2026-07-01-skill-library-capability-growth-review.md` (Openings C and E).
