---
type: disposition
date: 2026-07-02
adjudicates: >-
  docs/reviews/2026-07-02-agent-facing-design-methodology-critique.md
  (reviewed_commit 5dc9082; single-voice, ten-section philosophy critique;
  renders no change-set by design)
status: >-
  Closed. Adjudicated by the session agent at JP's direction — delegated
  judgment, not JP line-item approval, one rung below the JP-approved
  philosophy-audit disposition; any item here reopens on JP's word, no trigger
  needed. Nothing unaccepted carries a standing mandate.
---

# Agent-Facing-Design Methodology Critique — Disposition (2026-07-02)

The critique attacks the philosophy of `skills/agent-facing-design/` — the context/machinery dichotomy, the damage-only justification theory, the untested lighter-context counterfactual, the judgment/trust taxonomy, cognitive-offload's unfalsifiability, the skill's self-exemption and pointer-fed ambient authority — and closes by reframing the skill as a constitution wearing a risk calculus. This disposition accepts its text-verifiable core (the gate's stated justification vocabulary lags the skill's own evolved doctrine; context is priced at zero; offload lacked falsification conditions; there was no reader model; one genre drift), lands five repairs in the skill's own surfaces, parks the two claims that need instruments or JP, rejects four framings with reasons, and corrects four factual details for the record.

## Basis

- Every checkable figure was re-derived from git, the ledger records, and the audit text rather than trusted. Confirmed: 468 words at founding (`4e52679`) → 1,981 at `321cabe`; growth monotonic since 2026-06-12; the judgment/trust commit (`3eb0e74`) was the largest single jump; the Library Conventions edit landed the same day the audit disposition adjudicated them; audit quotes are accurate (M40-corrected "equal behavioral force," M23/M29 output-consequence and per-part bars, 4 agents dead on schema retry caps, no CUT verdict in the legend); `agent-facing-design` sits fourth-hottest at 46 fires in the framework-challenge sensor read the critique quotes.
- The ledger corroborates §3's central asymmetry claim in practice: the gate's recorded rejections are all machinery-shaped (M47/M49 fields and tiers declined, risk-register killed, metrics refused), while every ambient admission in `docs/agents/contract-decisions.md` since 2026-06-23 records "pure context, no machinery — passes trivially." The gate has fired repeatedly against machinery and never once against a prose addition. Corpus greps also confirm the premise: no confidence/scoring/validator machinery exists in `skills/` to police.
- Evidentiary class honored both ways: the critique is text-level with no behavioral incident (its own header says so) — the same class as the audit it post-dates. So text-consistency defects are adjudicated on the text they are visible in, and behavioral predictions are parked to instruments, never accepted or rejected on argument alone.

## The frame, honestly

§8 predicts that an in-frame adjudicator will accept remedies phrased in the philosophy's preferred medium and reject the rest — and this disposition's accepts are indeed all prose. Named, not escaped, on three grounds. First, the defects accepted are doctrinal (stale vocabulary, missing falsification conditions, missing reader model), and doctrine lives in prose; phrasing a vocabulary fix as a validator would be a category error. Second, the structure-shaped remedies are parked with named designs and triggers, not argued away. Third, the strict "survives iff prose" reading is factually broken by the arc's own record: M4 landed as a hard gating rule, the usage sensor as real machinery (zero agent obligations), the throughline budget as a hard cap after a measured claim failure — the live acceptance pattern is "no new agent obligations without evidence," which is the philosophy behaving consistently. Consistency and capture are observationally equivalent from inside the text, which is exactly why the exits in this disposition are behavioral (see Parked and the falsification register).

## Accepted — landed with this disposition

All in `skills/agent-facing-design/`; each passed the skill's own gate as context or lens, no new required fields, statuses, scores, or stop sequences; the Two Kinds self-rule ("add a lens, not a score or required section") was applied to the edits inside that section.

- **Core-move vocabulary reconciliation** (§2, §7a, and the workable half of §1). The damage list no longer claims to be machinery's only license: `SKILL.md` Core Move now says damage is the sharpest justification, not the only one, and names the other two the skill's own annexes already grant — reliable, repeatable execution (Two Kinds of Skill) and downstream-parseable outputs (Library Conventions) — with the instruction to name the value actually claimed instead of stretching the damage list. This is bookkeeping of live doctrine, not new doctrine: the critique's sharpest verified finding was that every major annex admits a value type the founding gate's text excludes, and the text was never reconciled.
- **Elastic member sharpened** (§2). "Loss of user trust" — the list's infinitely stretchable member — is now "misleading the user about what actually happened," which is the concrete failure it was doing legitimate work for (false success claims, silently substituted designs, misreported state) and nothing else. Vague load-bearing contract text is fixable on inspection; this is the same standard M31 applied to the charter.
- **Semantic-cut promotion** (§1's demotion complaint). The core move now states, in title position, that the sharpest form of the cut is who makes the semantic decision, not the format — prose that pre-makes the call is machinery however gently it reads; a deterministic mechanic that pre-makes nothing is a lighter design, not a threat; format only sets how hard the constraint is to override. The critique is right that this was the skill's best idea living in a subordinate clause while the rhetorical binary held the title.
- **Context priced at the moment of addition** (§3's true core). The proceed line now carries the price: context spends the same future-reader attention machinery does, accretes with no gate of its own, and on a grown surface replacing beats adding. The ledger's own "passes trivially" pattern showed the gate structurally waving through the one addition class this repo actually commits.
- **Offload falsification conditions** (§6). The offload paragraph now states that refusing a metric is not refusing falsification, and names the symptoms under which the claim is lost: the user still re-supervises or corrects the runs, or quietly stops summoning the skill — with the usage ledger as the instrument for the last. This completes what the framework challenge started (offload testable via variance and revealed preference) at the per-skill grain.
- **Tempered-not-timid rebound** (§5's genre-drift finding). The passage now states the output bar a judgment skill should demand rather than addressing the reading agent's own conduct — the design gate describes surfaces again, not general good agenthood. The drift the critique named ("a checkpoint becoming a constitution") stops here without losing the content.
- **Reader Capability calibration case** (§9, and §8's composability charge resolved in doctrine). `references/calibration.md` gains a borderline case naming the reader model the presumption always silently bet on: surfaces read by small fan-out subagents, degraded end-of-context sessions, or cross-model readers warrant firmer shapes than a frontier main loop; schema-forced returns at machine seams are this case working as designed (deterministic transport of judgments still made in prose), not a violation; and the bet runs both directions as readers strengthen. This makes the capability bet per-surface and explicit instead of global and silent.

Honest growth accounting, since §3 is about exactly this: these repairs grow `SKILL.md` 1,981 → 2,167 words (+186) and `calibration.md` 895 → 1,036 (+141). The growth is spent on falsification conditions and reconciliation — the specific prose whose absence the critique indicts — and the new pricing clause governs this skill's own future edits. The next substantive touch to this skill should prefer replacement to addition, per its own new line.

## Rejected

- **§1 strong form — the dichotomy dissolves.** Rejected. The functional cut (does this text narrow future agents' judgment or support it) remains usable and the critique's own §10 keeps the underlying instinct as "right, early, and worth building a philosophy around." What was defective was the demotion of the sharpest form and the stale justification vocabulary — both now fixed. Re-founding or retitling the skill is rebuild-class work and stays rejected on the audit disposition's standing grounds: text-only evidence does not justify rebuilding a core gate skill; reopen only if accumulated accepted edits leave it reading quilted.
- **§4's standing second-judge on gate calls.** Rejected as disproportionate standing machinery: the median gate call is obvious, and the hard end already has anchors the critique undercounts — no-silent-substitution routes every requested-machinery conflict to the user (the external judge with authority), and contested design calls already route to `scrutinize-skill` and the review lanes. Reopen on a transcript showing a self-approved structure addition a second reader would have caught.
- **§8 strong form — the acceptance pattern proves an antibody; repo practice refutes context-primacy.** Rejected as stated. The pattern is observationally equivalent to a correct gate behaving consistently (the critique concedes this), the strict prose-only reading is factually false (see The frame, honestly), and the repo's schema-forced subagent returns sit at machine seams the philosophy's own Semantic Scripts and Tool Returns doctrine already blesses — now stated explicitly in Reader Capability. The testable residue — the never-staged contest between judgment-preservation and composability — is parked to an instrument below, not dismissed.
- **§10's constitution rewrite of the skill's self-description.** Rejected as a downgrade. Reframing the gate's authority as "house taste" would license relitigating every verdict as aesthetics and weaken the no-silent-substitution contract, which the audit's negation adversary already tested and kept on strength. The taste-plus-governor reading is acknowledged as partially true *in this record* — the skill does encode a minimalist, judgment-respecting house style fused with a governor on structure-compulsion — but after the accepted repairs the "cover story" charge loses its target: the gate now claims the justification structure it actually runs on (three named value types, priced context, falsifiable offload) instead of catastrophe-only. A cover story corrected in the text is no longer a cover story.

## Corrections to the critique's record

None changes a verdict; recorded so future readers do not re-quote them (the audit's M43 discipline, applied to the critique).

- "The skill has quadrupled" ✓ (4.2×), but "nothing here has ever gotten smaller" is false in the letter: three micro-shrinks exist (−11 and −2 words on `SKILL.md`, 2026-06-11/12; −53 on `calibration.md`) — immaterial to the trend, which is monotonic growth since 2026-06-12.
- "859 → 1,528" for the judgment/trust commit: the immediate predecessor state was 886 words (`7df2e32`); 859 is two commits earlier. "+72%" not "nearly doubled," strictly.
- "The eighteen-file proof-boundary boilerplate": 16 files match, and audit M43 had already corrected "boilerplate" to a shared skeleton wrapping domain-specific bodies — the critique repeats the pre-correction characterization while citing the audit that corrected it.
- The critique's honesty header promised figures "not independently re-mined"; all three discrepancies above are of exactly that class. Its verified spine (growth trend, annex chronology, ledger figures, audit quotes) held.

## Parked — reopen triggers

- **§4 lighter-context counterfactual test** (the critique's best empirical demand: the gate's load-bearing premise has never been run). Parked with a design, not a schedule. Design sketch: take a gate-rejected machinery case — the canonical `confidence_score` → evidence-line substitution from `references/calibration.md`, or a live transcript case when one exists; run machinery-arm vs lighter-context-arm on realistic downstream-reader tasks, N ≥ 5 reps per arm; score downstream-reader accuracy and discipline-consistency, arm-blind, under the same pre-registration, prompt-authorship, and blinding rules as the banked skill-value test (`docs/plans/2026-07-02-skill-value-test-plan.md`). Execution trigger: runs with or immediately after that banked test (JP's re-prioritization or ask) so the prune deliberation and the design-gate premise get instrumented together — or earlier on any transcript-cited incident where gate-rejected machinery would have prevented real damage.
- **§7b delegated-authority gating** — the critique's strongest governance finding, and JP-tier, so parked rather than landed: charter amendments are curation-machinery edits this delegated adjudication should not make unilaterally. The facts as verified: the `AGENTS.md` pointer is gated and ledgered; the content that flows through it afterward is not; the loophole is real in structure and has the same shape M4 closed (gate by capability — here, delegated ambient authority — not packaging). Also verified: practice to date has voluntarily behaved as if gated — every material change to the pointer-designated doctrine (judgment/trust, offload, Library Conventions, and this disposition's own edits) landed via an adjudicated, recorded event. Reopen triggers: (a) the first material change to a pointer-designated section (Two Kinds of Skill, the offload doctrine, Library Conventions) that lands *without* an adjudicated record or JP approval; (b) the next time an always-loaded surface delegates canonical authority to a build-and-prune artifact. Recommended shape on reopen, for JP's call: sections of a build-and-prune artifact that an always-loaded surface names as canonical are charter-gated for material change — M4's sibling, authority-capability instead of fire-capability.
- **§3 removal rhythm / scheduled shrink.** Stays inside the existing park (the audit's ten-item machinery list already parks scheduled bloat audits); reopens only as its own charter admission on observed friction. The lighter standing form is now in place: the Whole-Surface Check plus the new pricing clause at the moment of addition. The audit disposition's recorded intent — ambient and meta surfaces net-zero-or-shrinking absent a transcript-cited incident — is reaffirmed as applying to this skill's future edits.
- **§9 capability-revision schedule.** No scheduled re-examination (kin of the parked machinery); the Reader Capability case makes the bet per-surface and explicit instead. Watch signal: any model-tier migration that visibly changes fan-out or cross-model reader capability is the moment to revisit shapes built for weaker readers.

## Posture — falsification register (recorded intent, not contract text)

The critique's fairest demand was "name what evidence would count against each of its beliefs." For the record:

- **Context-primacy**: falsified for a case class if the parked §4 A/B lands machinery-arm-wins on a gate-rejected case with sound instrumentation.
- **Cognitive-offload**: per-skill, the symptoms now named in the skill (re-supervision, correction, summons cessation); library-level, the 2026-08-01 ledger re-read commitment.
- **Damage-gate sufficiency**: a transcript-cited incident where machinery the gate rejected would have prevented real damage.
- **Two Kinds of Skill**: repeated sessions where per-part classification work exceeds the design question it was meant to sharpen and changes no decision — if seen, cut the lens rather than annotate it.
- **This disposition**: its accepts are prose because its accepted defects were doctrinal; if the behavioral instruments above start returning results the prose cannot survive, the prose loses.

No `contract-decisions.md` entry: no always-loaded surface was touched and no charter event occurred; skill-side accepts are recorded here, per the audit-disposition precedent for build-and-prune surfaces.
