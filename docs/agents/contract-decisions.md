# Contract Decisions

Ledger required by the charter's Decision Record section (`charter.md`): one entry per **gated** decision — an admission, fold, rejection, park, or retirement of an ambient contract or third-party material — with date, surface, outcome, evidence pointer, and the reopen trigger for parks. Build-and-prune skill/command churn is not ledgered. Append new entries; do not rewrite settled ones.

## Decisions

- 2026-06-12 — superpowers (pass 1): folded into `tdd` (+`mocking.md`), `diagnose`, `behavior-smoke-test`, and a global evidence-before-claims rule; admitted `design-exploration`, `implementation-planning`, `execute-plan`; requesting-review meta-skill rejected (collides with review lanes; checkpoint habit folded into `closeout-check`). Plugin removed. Evidence: commits `360fc77`, `966f903`, `d7351ec`.
- 2026-06-12 — commit-commands (pass 2): folded `[gone]` deletion-candidate discipline and a squash-merge proof path into `git-hygiene`; `commit`/`commit-push-pr` rejected (one-message/no-review mandates weaker than house discipline). Plugin removed. Evidence: commit `3b4cbc1`.
- 2026-06-12 — pr-review-toolkit + code-simplifier rider (pass 3): folded error-suppression and test-inadequacy lenses into `implementation-review` (review-family 0.3.2) and a clarity-over-brevity guardrail into `simplify-code`; six agent lanes rejected (numeric confidence/criticality scoring is rejected machinery; foreign CLAUDE.md leakage). Both plugins removed. Evidence: commit `5f64a54`.
- 2026-06-12 — feature-dev (pass 4): folded exploration grounding into `design-exploration` (read subagent-named key files first-hand before designing); command and three agents rejected (lane decomposition owns the workflow; settled no-numeric-confidence precedent). Plugin removed. Evidence: commit `2dc1b95`.
- 2026-06-12 — hookify (pass 5): zero folds — engine adjudicated capability tooling (charter-exempt as a category) but removed as unused; all six contract surfaces rejected (runtime-bundled `update-config` owns hook-backed automation). Plugin removed. Evidence: commits `ad9c0cc` (ledger entry) + `6f7833d` (route-absence amendment from the same session). Fuller detail is in the untracked local handoff `2026-06-12_18-35-34_feature-dev-hookify-mined-charter-amended-audit-reopens.md` — not a durable pointer, not part of the durable record.
- 2026-06-12 — discard audit (after the route-absence amendment, `6f7833d`): reopened two prior rejections — comment-accuracy lens folded into `implementation-review` (review-family 0.3.3, commit `1d0091a`); `friction-to-guards` admitted Claude-only in `skills-claude/` (commit `a5e6642`). All other prior discards held.
- 2026-06-12 — gh-address-comments: admitted (owns PR-comment addressing without publish authority); authoring deferred. Evidence: commit `ad9c0cc` (admission recorded); built in commit `993cd93`. Tracking: issue jpsweeney97/agents#2.
- 2026-06-12 — frontend-design (pass 6): zero folds — the sole contract surface (a 41-line web-frontend aesthetics SKILL.md) parked rather than admitted. No local lane owns web-frontend aesthetic guidance, but the job has no observed work: installed since 2025-12-17 with zero invocations across all session transcripts (search pattern control-verified against known-invoked skills), no web-frontend stack in any project directory (lyrics-software is native SwiftUI), and zero inbound routes from curated contracts. Plugin removed; source recoverable in the claude-plugins-official marketplace catalog. Evidence: commit `45c8a5a`.
- 2026-06-12 — claude-code-setup (pass 7): zero folds — the sole skill (`claude-automation-recommender` + five reference files) rejected. The job decomposes entirely into owned lanes: feature/setup questions → runtime-bundled `claude-code-guide` + local `claude-code-docs`; hook and settings implementation → runtime-bundled `update-config` (settled pass 5); permission allowlists → runtime-bundled `fewer-permission-prompts`; automation-worthiness → the charter's admission test + `friction-to-guards`; skill authoring → `skill-creator`. Its speculative codebase-signal recommendation method contradicts the observed-friction admission standard, and its tables recommend reinstalling plugins this ledger already removed (frontend-design, hookify, commit-commands, feature-dev, pr-review-toolkit, code-simplifier) — running it as-is recreates resolved collisions. Friction looked for and not found: zero invocations since 2026-01-21 install (control-verified transcript search), no setup-recommendation requests in memory or handoffs; static Claude Code facts it carries are owned live by `claude-code-docs`. Plugin removed; source recoverable in the claude-plugins-official marketplace catalog. Evidence: commit `ae12dc3`.
- 2026-06-12 — skill-creator (pass 8): keep-with-cleanup. Bundle construction stays owned on Codex by the bundled copy (`~/.codex/skills/.system/`, fixed terrain); its do-work scripts are charter-exempt and the `quick_validate.py` the Validation Ladder cites resolves there. The removable third-party Claude plugin install (`skill-creator@claude-plugins-official`) — an eval/benchmark build (`run_loop`/`run_eval`/`aggregate_benchmark`/`improve_description`/ `generate_review`; ships its own `quick_validate.py` but no `init_skill.py`/ `generate_openai_yaml.py`) — removed under Extraction. Its authoring doctrine overlaps and folds into `agent-facing-design`/`writing-principles`/ `skill-ux-design`/`behavior-smoke-test`/`scrutinize-skill` and persists in the Codex bundle. Its eval/benchmark capability re-authored to house standards as `skill-benchmark` (admitted, Claude-only in `skills-claude/`): owns quantitative skill benchmarking and trigger/description optimization, a job no local lane held (`behavior-smoke-test` is single-shot qualitative; never co-loads with the Codex-bundled `skill-creator`). Four inbound routes (`AGENTS.md` routing lane; `writing-principles` ×2; `agent-facing-design`) re-pointed availability-conditionally: Codex construction → bundled `skill-creator`; Claude construction → hand-author against the folded doctrine plus the Codex-bundled `quick_validate.py` via Bash (no invocable Claude-side constructor remains, by design). Reconciles pass-7 above: "skill authoring" still decomposes into an owned/fixed surface. Friction looked for and not found: zero failed or misrouted constructions, and the install's eval machinery never exercised, across two 2026-06-12 main-thread invocations that produced four now-live skills by hand-authoring + manual construction. Plugin uninstalled (`claude plugin uninstall`); source recoverable in the claude-plugins-official marketplace catalog. Evidence: commits `a5e6642`, `966f903`.
- 2026-06-13 — code-review (pass 9): fold-then-remove. The removable third-party Claude plugin `code-review@claude-plugins-official` — one command (`commands/code-review.md`): a fixed GitHub-PR pipeline (5 Sonnet lenses + a Haiku 0-100 confidence scorer, `<80` filter, `gh pr comment` post-back) — removed under Extraction. Three genuinely-novel false-positive exclusions folded into `implementation-review` (review-family 0.3.4), framed as the symmetric evidence burden on findings: correct code that resembles a bug; linter/typechecker/compiler/CI-catchable issues; and repo-instruction violations explicitly silenced in code (verified absent from the lane — zero matches across its skill dir). The 0-100 rubric + `<80` gate **not** folded. Scoring ruling (JP): the no-numeric-confidence precedent **bites** this internal, never-exposed gate, **scoped** to confidence/criticality scoring used as a judgment substitute, not all internal numbers; reached on inspectability grounds (a hidden gate is worse than an exposed one — "Found 0 issues" cannot be told from suppressed), reinforced by a verified miscalibration (the rubric's own 75 "important real issue" anchor sits below the `<80` cut; the same defect rode in the pass-3/pass-4 rejected artifacts). Everything else owned or banned: gh-PR-comment channel by fixed-terrain bundled `/code-review`+`/review`; 5-lens substance already folded passes 3 and 0.3.3; CLAUDE.md-adherence lens rejected pass 3 (foreign-CLAUDE.md leakage). Friction basis corrected: the queue's "in active use" is **refuted** — zero genuine invocations of the plugin or bare `/code-review` in this repo (triangulated; the apparent hits are this pass's own grep echoes). Per Admission this cannot decide removal but forecloses a relied-upon retention defense; removal stands on the same-runtime collision with the two Claude builtins. Correction for future passes: `codex-review` is itself an installed third-party plugin (codex-collaboration cache), unmined source material subject to Extraction — not a curated owner to lean on. Plugin uninstalled (`claude plugin uninstall`); source recoverable in the claude-plugins-official marketplace catalog. Evidence: commit `3691ca0` (pass-9 route-inventory + merit-adjudication).
- 2026-06-13 — security-guidance (pass 10): fold-then-remove. The installed + enabled third-party plugin `security-guidance@claude-plugins-official` (v2.0.6) — a hooks bundle with no invocation token that auto-fires on UserPromptSubmit/PostToolUse/Stop, injecting 25 regex pattern warnings on edits, a single-shot LLM diff review on Stop, and an agentic cross-file investigate→self-refute reviewer on commit/push — removed under Extraction. Scope ruling (JP-ratified): the hooks engine is charter-exempt capability tooling (ll.22-24), but the injected instruction text is in-scope contract (l.24; l.14 names "instruction text delivered by hooks"), and the fused engine departs with the contract as packaging. Removal is value-independent (Extraction ll.87-90) — the engine was demonstrably used (80 review fires over 16 days; one fully-closed fire-and-mattered loop where a commit-review finding changed committed `settings.json`), so pass 5's "removed as unused" is a distinguishable, not controlling, prior. One-Owner: the Stop/commit/push LLM-review jobs claim the same work as the fixed-terrain bundled `/security-review` (ll.16-20, 37); the collision resolves on the local side — delivery-mode differences (auto-fire vs invoke; recall vs precision; turn/commit vs branch) do not save job identity (pass-9 precedent). Two genuinely-novel disciplines folded into `implementation-review` (review-family 0.3.5), both verified zero-match in the lane: (1) resource-cap-defeat DoS — report exhaustion only when a change defeats an existing cap, not volumetric load (sits in the gap `/security-review` explicitly excludes); (2) attacker/victim + privilege-boundary + off-diff adversarial-refute discipline, including the agent-capability-gate carve-out (the model is the attacker, the user is the victim). Confidence-precedent (pass 9) does NOT trip — scoped to confidence-as-judgment-substitute: the plugin gates on an ordinal severity enum + qualitative cited-evidence self-refutation; its numeric `confidence` field is schema-optional and no code path filters on it (the exposed, inspectable kind the precedent prefers). Jobs rejected: inline 25-pattern edit warnings (lighter-alternative beats a paid slot, ll.53-55); Stop/commit/push LLM review (collision). Two lower-confidence folds (additive-only embedded-guidance guardrail; over-broad-grant sharpening) declined by JP. No inbound curated routes to repair (`tech-debt-scan`'s hand-off targets a hypothetical Codex `codex-security:security-scan`, availability-conditioned with a clean fallback). Plugin uninstalled (`claude plugin uninstall`); source recoverable in the claude-plugins-official marketplace catalog. Evidence: commit `63daf9f` (pass-10 route-inventory + merit-adjudication).
- 2026-06-13 — explanatory-output-style (pass 11): reject (zero-fold removal). The installed-but-disabled third-party plugin `explanatory-output-style@claude-plugins-official` (v1.0.0) — a single SessionStart hook (`session-start.sh`, a heredoc `cat` of a static `additionalContext` blob: an always-on "explanatory output style" teaching register emitting `★ Insight` boxes before/after writing code, codebase-specific, "may exceed typical length constraints") — removed under Extraction; the contract IS the deliverable and the engine does no work, so the pass-10 packaging-departure + value-independent removal (ll.87-93) apply a fortiori (pass 10 had a real engine to exempt; here a trivial `cat`). One-Owner: doubly owned. (1) The fixed-terrain built-in "Explanatory" output style is live/un-deprecated (changelog "Un-deprecate output styles"; `outputStyle: "Explanatory"` documented; only the `/output-style` command was removed) and does the exact job; the plugin's "deprecated" self-label is factually stale; same-runtime (Claude-only — no Codex output-style/SessionStart-`additionalContext` analogue), co-loadable, resolves local-side (ll.16-20, 36-44). (2) Global `~/.claude/CLAUDE.md` Communication defaults own the chat register; the contract inverts them on three axes (always-on insights vs task-focus; "may exceed length constraints" vs brevity/outcome-first; `★`-box scaffold in chat vs "save formal structure for artifacts"). Zero folds: the lone substantive discipline (codebase-specific-not-generic insights) fails Extraction step-1 — inseparable from the always-on packaging and already owned by `scrutinize-skill` (SKILL.md:137,155), `writing-principles` (SKILL.md:172), the built-in's description, and CLAUDE.md. Rejection evidence (ll.60-66): usage was demonstrably PRESENT (715 fires, ~1380 obeyed codebase-specific boxes, 2026-05-14 → eve of mining, concentrated in codex-collaboration), so usage-absence is barred as a rationale; the reject rests instead on a positive-control friction search (memory + handoffs + AGENTS.md + CLAUDE.md + ripgrep for teach/educational/insight/pedagog/verbose/output-style) that returned ZERO teaching-register friction while the same instrument fired on the opposite-vector brevity/compression family (caveman; pass-3 clarity-over-brevity guardrail) — that asymmetry is the finding. Lighter-alternative (ll.53-55): README concedes CLAUDE.md-equivalence and `outputStyle` is a one-line setting, both beating a paid slot. No park (the need is owned now by the live built-in, one `/config` flip away); keep-with-cleanup foreclosed (no Codex co-load, unlike pass 8). First-of-its-kind communication/output-style precedent — no prior chat-verbosity contract; this reject sets it. JP steer honored as reinforcing, not load-bearing: Ruling A (disable ruled "incidental") treated as neutral — the reject never rests on the toggle; Ruling B ("built-in is enough; no standing register") converges with the independently-reached verdict and forecloses any register-reinstating fold/park. Plugin uninstalled (`claude plugin uninstall`); source recoverable in the claude-plugins-official marketplace catalog. Evidence: commit `094ad22` (pass-11 route-inventory + merit-adjudication).
- 2026-06-13 — github plugin (assessment, not a mining pass): exempt — pure capability tooling, no contract text; stays installed and enabled. Full file inventory of the installed + enabled `github@claude-plugins-official` cache (`find -type f` over the plugin dir) shows only `.mcp.json` (one HTTP MCP server → `api.githubcopilot.com/mcp/`, `GITHUB_PERSONAL_ACCESS_TOKEN`-authed), a name/description/author `plugin.json` with no component declarations, and `.in_use/` PID lockfiles; `commands/`, `skills/`, `agents/`, `hooks/`, and `output-styles/` all absent. No slash command, SKILL.md, agent definition, hook-injected `additionalContext`, or output-style register — the plugin.json `description` is marketplace catalog metadata, not injected instruction the agent must follow (all `mcp__plugin_github_github__*` tools come from the hosted MCP server, not bundled prose). Per the capability-tooling exemption (charter ll.22-24, "MCP servers ... out of scope. Tools are tools"), a standalone exempt tool with no accompanying contract is out of scope: no mining, and — unlike the fused-engine cases (pass 5 hookify, pass 10 security-guidance, where an exempt engine departs as packaging for in-scope injected contract) — there is no in-scope contract to extract, so nothing is removed and the plugin stays installed. First pure-exemption outcome. Closes the mining program: the queue's last residual surface is resolved. Evidence: commit `71ce1ca` (plugin-cache file inventory).
- 2026-06-13 — gh-address-comments (build of the 2026-06-12 admission): authored as a dual-runtime skill in `skills/` (`SKILL.md` + `agents/openai.yaml`); owns PR review-comment addressing without publish authority — verify each thread against evidence, classify (5 dispositions), fix locally, draft replies, stop at one local commit; never push/resolve/ re-review. Folds the superpowers `receiving-code-review` reception discipline (pushback with technical reasoning; no performative agreement; Pasted-Content reconciliation) per #2's addendum. `gh-pr-review-loop` refactored to one-owner-per-job: it now runs the gh-address-comments inner loop and contributes only the publish layer, with all publish-side obligations preserved (human-reviewer caution, resolve eligibility, `@codex` trigger, no-code-change path, empty-threads guard, publish checkpoints, final-response fields). Validated (frontmatter parse, `quick_validate.py`, `openai.yaml` parse, whitespace) and three context-isolated behavior-proxy smoke tests pass: stop-line holds under deadline + authority pressure, routing-in selects the skill, publish authorization escalates to `gh-pr-review-loop`. Closes the authoring-deferred state. Evidence: commit `993cd93`; issue jpsweeney97/agents#2.
- 2026-06-15 — jp-writing-style: admitted (first-party authored, not a mining pass). Owns generation and rewriting of prose in JP's personal voice — reflective essays, personal statements, application responses, analytical/ professional narratives, memos, recommendations, explanations — grounded in a real two-piece corpus (`references/corpus.md`, lightly redacted: school names, grades, and clinical labels replaced with neutral stand-ins) plus a deduplicated move/diction/genre pattern bank (`references/patterns.md`). No local or bundled lane held voice-grounded prose generation. One Owner Per Job (checked in both runtimes, since dual-runtime): distinct in kind from `writing-principles` (obligation-only edits to agent-instruction docs), `markdown-synthesis`/`markdown-reformat` (structural Markdown consolidation/ formatting, not voice), and from every Codex- and Claude-bundled skill (none does prose-voice work) — all co-loadable without collision. Lighter context rejected: an `AGENTS.md`/`CLAUDE.md` line cannot carry the corpus, the integrity gate, the elicitation step, or the per-genre pattern bank, and the skill's first draft proved voice-by-assertion (no corpus) collapses into generic AI-formal. Houses standards: Use-when/Do-not-use boundary; one justified hard rule (the integrity gate, surviving the agent-facing-design machinery test — fabrication in personal statements/applications is fraud) that is also the first blocking quality-check item; runtime-neutral phrasing (joint `AGENTS.md`/`CLAUDE.md` naming, no Claude-only tokens). Placement `skills/` (dual-runtime), not `skills-claude/`: observed-work call — JP confirms he regularly drafts this writing from Codex too, so the skill earns its Codex skill-list slot rather than being demoted Claude-only. Validated: frontmatter parse, `quick_validate.py`, whitespace, references resolve; two context-isolated adversarial forward tests pass (thin personal-statement prompt → elicits real facts and refuses to invent a backstory; bare-notes rewrite → withholds unsupported causes/lessons and asks). Evidence: commit `6239f9c` (skill build, rewritten from review) + this ledger entry.
- 2026-06-16 — blind-evaluation blinding guard (test-5 red-team breach, plan T3): admitted a new rule to repo `AGENTS.md` (`## Blind Evaluations`) — in any blind evaluation, never reveal apparatus state (reviewer/model outputs, intermediate scores, predictions, arm identities) to a current or potential ground-truth judge until their independent judgment is recorded; lost blinding is unrecoverable. Owns an evaluation-blinding discipline no lane held (`skill-benchmark` is quantitative skill benchmarking — different job; the global evidence-before-claims rule governs claims, not blinding). Prevents the observed breach where the orchestrator narrated reviewer/disposition results before certification and contaminated the human + self as judges, forcing the human arm to a fresh judge. Repo-scoped home chosen (recurrence concentrated here) over global, by JP. Evidence: breach disclosed in results §8 and scored in §12 (`51ab24e`); rule at `AGENTS.md` `## Blind Evaluations` (this commit).
- 2026-06-17 — jp-writing-style: retired (removed from source). The skill was admitted, built, scope-narrowed, and hard-deleted all on 2026-06-15 — a targeted removal of exactly its three files (`7d76940` "chore: cleanup", 381 deletions) ~8.5h after the build (`6239f9c`, 01:51). This entry completes the retirement record left half-done at the time: the orphaned `~/.claude/skills` symlink (dangling since the delete, its source target missing) is reaped, and the 2026-06-15 admission above is superseded. Source is NOT moved to `skills-archive/` (the charter's default skill-retirement home), by JP's call: the skill's `references/corpus.md` was real personal writing, and re-committing it to a repo that pushes to GitHub is the exposure the same-day deletion avoided — a deliberate, recorded deviation from the archive default. The source remains recoverable from git history at `6239f9c` if the decision is ever reversed. Evidence: deletion `7d76940`; source/recovery point `6239f9c`; symlink reap + this entry (this commit).
- 2026-06-23 — Markdown prose-wrap default (global + this repo): admitted a style default that generated Markdown prose artifacts (handoffs, specs, reviews, plans, PRDs) are written one logical line per paragraph/bullet, never hard-wrapped at a fixed column. Recorded two ways: as a user-level global instruction (`~/.claude/CLAUDE.md`, "Markdown Artifacts"; Claude-only, untracked) and — at JP's direction that this repo not carry a different convention — adopted as this repo's own rule for both runtimes via `AGENTS.md` ("Working Defaults"), with no per-repo override. Existing hard-wrapped repo Markdown reflowed to conform on this branch (`AGENTS.md`, `CLAUDE.md` shim, `docs/`, `skills/`, `skills-claude/`, `plugins/`); `skills-archive/` left as retired history; the skill-squad spec/plan reflowed on `feature/skill-squad-spec` (`241dc91`, `d981fde`); plugin Codex-cache republish deferred to JP (cosmetic source-vs-cache drift until then). No existing lane owned generated-prose wrapping (closest: the global `Code Style` and `Communication` sections — code and chat register, not artifact line-wrapping). Pure context — a formatting default, no machinery (no field, classifier, or score) — passes the agent-facing-design gate trivially. Friction: recurring hard-wrap-at-~100-col, corrected this session and made durable at JP's explicit request. Evidence: reflow commit `241dc91` (skill-squad spec; content verified word-for-word identical) plus the convention + repo-wide reflow commits on `chore/md-prose-wrap-default` (this entry and the following reflow commits).
- 2026-06-24 — Communication brevity-first default (global, untracked): folded a sharpened chat-communication default into the existing `~/.claude/CLAUDE.md` Communication section — thesis "make every sentence earn its place" plus six principles (answer-first / no throat-clearing; plain novice-friendly language with first-use jargon definitions and no assumed repo familiarity; short paragraphs; cut filler / no needless repetition; one-line uncertainty) and three guardrails (Complete-beats-short; Prose-only — brevity governs explanatory prose, never code/file/command/requested output; No-fixed-format — prose/bullets/tables by readability, no prose-default). Not a new lane: it sharpens the existing Communication owner, which pass-11 (`094ad22`) already ruled owns the chat register with a "brevity/outcome-first" precedent — this strengthens that owner in the endorsed direction. One reversal: dropped "save formal structure for artifacts," superseded by the No-fixed-format guardrail (structure now allowed in chat when it reads better); the "concrete analogies" mandate dropped; "Code and documents stay precise" absorbed into Prose-only; "lead with the user-visible outcome" absorbed into Answer-first; "match the user's register" preserved. One-Owner checked, no collision: Default Behavior's "completeness over token cost" governs the work (Complete-beats-short bridges; brevity governs prose only), and the Markdown Artifacts line governs artifact line-wrapping (a different surface). agent-facing-design gate: pure context, no machinery (no field, classifier, or score) — passes trivially, mirroring the 2026-06-23 Markdown precedent. Friction: JP reported in-session that responses had grown too verbose, costing the point; made durable at his explicit request (outcome-interviewer → Direct lane). Evidence: durable record is this ledger entry committed on `chore/comms-brevity-default` (the global file is Claude-only and untracked, so the ledger commit is the durable pointer, per the 2026-06-23 precedent).

- 2026-06-27 — skill-purpose / judge-by-global-merit framing (AGENTS.md `## What The Skills Are For`): admitted an always-loaded framing stating the skills are global artifacts judged by merit as a global capability, where merit has two independent sources — leverage where the skill fires (any repo that needs it, including this one) and cognitive-offload value (a reusable high-quality prompt summoned by a token; a capable model improvising the work anyway does not make the skill valueless, per the Era-32/34 cognitive-offload-as-lens finding) — and that whether a skill fires in `.agents` is at most a build-order/prune-fidelity hint, never a build/keep/cut gate. Owns the orient-the-library's-judgment job at the always-loaded altitude, held by no existing surface: "Source Authority" and "Skill Layout And Delivery" state delivery mechanics but not the judgment consequence; the build-and-prune model lives in `charter.md` (consulted only at gated events) and the non-authoritative throughline — neither loads during library analysis, which is a build-and-prune activity, not a gated event. Lighter context rejected: the failure forms during free analysis that invokes no skill and triggers no charter consult, so only the always-loaded AGENTS.md surface covers the moment. agent-facing-design gate: pure context, no machinery (no field, classifier, score, or decision-making rule) — passes as the 2026-06-23 Markdown-wrap and 2026-06-24 brevity precedents did. Friction (observed, this session): the agent twice recommended against building globally-valuable domain skills by gating on local `.agents` fire and modeling the skills as self-repo tooling — a documented instance of the recurring "prove-it-first / observed-fire-as-gate" anti-pattern the throughline records at Eras 4/11/13/19. Evidence: AGENTS.md `## What The Skills Are For` + this ledger entry (this commit).

- 2026-07-02 — skill-usage evidence exposure (AGENTS.md `## What The Skills Are For`, amendment): admitted one sentence extending the 2026-06-27 admission — global fire is now measurable via the skill-usage ledger (`scripts/skill-usage-miner.py` → `~/.claude/logs/skill-usage-ledger.jsonl`, live-fed by a user-level PostToolUse hook), keep/prune deliberations should consult it, sustained global silence is legitimate prune input alongside mis-fire evidence, never an automatic verdict, and admission stays ungated by proof. Closes the keep-side evidence gap the 2026-07-01→02 framework challenge adjudicated: the paradigm already conceded the principle (mis-fire = prune evidence; local absence "at most a prune-fidelity hint") but no instrument existed, so prune had no sensor and the merit frame's improvisation shield lacked its empirical counterpart — build-and-prune was build-and-keep in practice (zero use-evidence prunes in the library's life). Not a reversal of the anti-prove-it-first guard: gate-on-build stays dead (Eras 4/11/13/19/46); this exposes only KEEP to evidence. agent-facing-design gate: context, not machinery — it informs the keep/prune judgment ("never an automatic verdict" fences the crude-rule overreach) and mandates no field, score, threshold, or auto-prune rule. The sensor itself (miner + hook) is charter-exempt capability tooling (pure telemetry, no injected instruction; the Era-63 `_EXTRA` hook precedent) and takes no entry of its own; hook registration is user-level (`~/.claude/settings.json`, untracked — this ledger commit is the durable pointer, per the 2026-06-24 precedent). First ledger read (backfill of ~6 weeks of Claude-side transcripts, most of the library's life): 36 of 76 roster skills ever fired, 40 never, hot core ≈13 — bounded by Codex-side invisibility and the ≤2-week age of the newest ~20 builds; re-read committed for 2026-08-01 before any prune tranche. Evidence: `docs/reviews/2026-07-02-framework-challenge.md` (frozen challenge record incl. branch commitments) + the amendment, sensor scripts, and this entry (this commit). Update (2026-07-09): the mined span actually reaches back to 2026-01 (Codex sessions back-mine to January), ~6 months of coverage, not ~6 weeks; the ledger file is append-ordered, not time-sorted, so consumers must not assume chronology; summary-time alias normalization (typed handoff command tokens merged into canonical skill names) and roster/non-roster classification were added to the miner so the 2026-08-01 re-read runs on merged, uncontaminated counts — raw records and the pre-registered decision branches are untouched.

- 2026-07-02 — philosophy-audit adjudication (disposition of `docs/reviews/2026-07-02-agent-facing-design-philosophy-audit.md`, per JP-approved recommendation from the same-day critical read): three amendments to always-loaded surfaces landed. (1) Capability carve-out (audit M4, the one finding grounded in verified environment capability rather than text-on-text): the build-and-prune exemption reads on capability, not packaging — a skill that can fire unattended (cron/remote-trigger/subagent dispatch) or wields irreversible-effect tools (send/merge/force-push/delete) fails the Reversibility Class lens's own premises (visible fire, clean prune) and is charter-gated; gated-events enumeration synced across `charter.md`, `AGENTS.md`, and global `~/.claude/CLAUDE.md` (untracked — this ledger commit is the durable pointer, per the 2026-06-24 precedent). (2) Markdown auto-commit skill-override clause in global CLAUDE.md, resolving the verified live contradiction with `grill-with-docs` (SKILL.md:94) and `improve-codebase-architecture` (SKILL.md:85): a governing skill's explicit instruction about its own writes overrides the auto-commit default. (3) M31 fold into charter Admission ("does it say plainly what an agent who encounters it must do"). Charter edits themselves ride under the charter's direct-editing rule (charter.md, Scope: curation machinery takes no entry of its own) — ledgered because the same decision amends `AGENTS.md` and global CLAUDE.md, which are gated surfaces. Rejected: the audit's M6/M7 re-derivation of third-party gating onto the reversibility axis with an origin-waiver for scope-local third-party material — adjudicated a downgrade (provenance/authorship is the correct axis for third-party contract risk: a third-party skill is trusted instruction text wherever it fires, and trashing the directory undoes neither steered sessions nor actions it persuaded an agent into; the M4 capability test does not cover persuasion); the charter's third-party clause stands as written, and any future reopen moves toward tighter provenance gating, not looser. Declined without ledger weight (build-and-prune surfaces): the audit's two structural rebuilds of `agent-facing-design`/`writing-principles`; skill-side accepts (Library Conventions section in `agent-facing-design`; idempotent-resume lines in `execute-plan`/`migration-campaign`) are recorded in the disposition. agent-facing-design gate: every accept is context, no machinery (no field, score, classifier, or auto-rule; the M47/M49 machinery — mandatory residual-risk fields, co-sign tiers — was not adopted). Evidence: `docs/reviews/2026-07-02-philosophy-audit-disposition.md` + the amendment commits on `chore/philosophy-audit-disposition` (this entry's commit and its predecessors).

- 2026-07-03 — methodology-critique (new skill, `skills-claude/methodology-critique/`, gated admission): admitted the methodology-and-philosophy treatment as an invocable Claude-only lane — sealed self-commission (pre-registered expectations written before any fire is read, replacing the two-session commission/critique independence of the bespoke prompts), two-runtime fire-corpus evidence, single-judge max-effort critique, JP adjudication as a hard stop, warranted repairs through the `agent-facing-design` gate, and the landing train (branch → repairs commit → `docs/reviews` brief → ff-merge → push → handoff → throughline). Gated not by packaging but by the 2026-07-02 capability carve-out: JP granted the lane standing push authority for its own landing train (push = irreversible-effect), so it fails the clean-prune lens and takes the Admission test despite skills being build-and-prune; the grant is scoped to this lane's landing train and nothing else inherits it. Admission: the work — premise-altitude critique of a skill's method against its history and fires, adjudicated, then repairs — is owned by no existing lane; the closest contract, `scrutinize-skill`, names this work as not-its-job in its own body (the 0.6.0 premise lens detects and routes "to a dedicated methodology-and-philosophy critique", SKILL.md:35/:73, which until now resolved to nothing invocable); `skill-benchmark` is quantitative measurement, `behavior-smoke-test` is uptake proof, `agent-facing-design` is the design gate the repairs pass through — One-Owner clean. Lighter context cannot carry it: the job recurred seven times in two days (Eras 66–73) as ~2,000-word bespoke hand-authored commission prompts (three preserved in `scratch-workspace/staging/`), whose baked facts each run re-verified anyway — the skill replaces the facts with their derivation procedure and makes the genre's honesty floors (record-as-calibration-never-prior, end-where-evidence-ends, circularity traps, privacy boundaries, single-judge acknowledgment) standing contract instead of per-prompt vigilance. Houses standards: Use-when/Do-not-use carve against `scrutinize-skill`, proof-class discipline (proxy pass = uptake evidence never value evidence, untested graded honestly), availability-conditioned home (`skills-claude/`; the evidence rig is Claude workflow machinery). Evidence: the seven precedent briefs `docs/reviews/2026-07-02-{outcome-interviewer,making-recommendations,scrutinize-skill,writing-principles}-methodology-critique.md` and `docs/reviews/2026-07-03-{grill-with-docs,ideate,design-exploration}-methodology-critique.md`, plus the skill and this entry (this commit).

- 2026-07-05 — mattpocock/skills reinstall (disposition of an unrequested bulk re-run): a newer mattpocock installer re-run (a fresh `skills-lock.json` manifest, since removed) reinstalled the third-party set into `skills/`, clobbering 8 first-party forks and their companions and adding 11 new third-party skills. Disposition — **restore, one admission, one fold, nine rejections**. **Restored** the 8 clobbered forks (`tdd`, `grill-me`, `grill-with-docs`, `improve-codebase-architecture`, `prototype`, `to-issues`, `to-prd`, `triage`) and their deleted companions by discarding the uncommitted install: the first-party versions carry the house layer the reinstall stripped (Use-when/Do-not-use descriptions, dual-runtime tokens, proof boundaries, `trash`-not-`rm` safety), and the reinstall had also broken a first-party single-source (`decision-record` → `grill-with-docs/ADR-FORMAT.md`), repaired by the restore. **Admitted `teach`** (`skills/`, dual-runtime; source `mattpocock/skills:skills/productivity/teach/SKILL.md`, computedHash `9cd31ea…`): owns multi-session stateful teaching (a mission/lessons/reference/learning-records workspace, fluency-vs-storage-strength, zone of proximal development) — a job no local or bundled lane holds (`explain-codebase` explains a repo, not pedagogy over sessions), and lighter context cannot carry the workspace structure or method; re-authored to house standards at the routing surface (Use-when/Do-not-use, non-use boundary, dual-runtime token, kept user-invoked). Proof boundary: routing re-authored, body adopted largely as-authored (no dual-runtime or proof-discipline defect observed), not a full body rewrite. **Folded** the one genuinely-additive discipline of `diagnosing-bugs` — the Phase-2 repro-minimise step — into first-party `diagnose`, which already referenced a "minimised repro" it never defined; donor removed under Extraction (`mattpocock/skills:skills/engineering/diagnosing-bugs/SKILL.md`, computedHash `1a993ce…`; `diagnose` owns the hard-bug loop and is richer — non-determinism cause-class table, perf work/resource-bound lens, three-failed-fixes escalation). **Rejected (zero-fold removal)** the other nine, each a One-Owner collision or infra for an un-adopted flow: `code-review` (same-runtime collision with bundled `/code-review` + first-party `review-family`; also an AGENTS.md-forbidden `skills/` name), `codebase-design` and `domain-modeling` (content already owned first-party and richer — the deep-module vocab across `improve-codebase-architecture`'s `LANGUAGE.md`/`DEEPENING.md`/`INTERFACE-DESIGN.md` and `tdd`'s companions, the domain discipline plus `ADR-FORMAT.md` with its extra `Revisit when` field in `grill-with-docs`; and `decision-record` documents keeping that format in place, do-not-relocate-in-v1), `grilling` (collides with `grill-me`), `research` (collides with `deep-research`/`research-capture`), `implement` (collides with `execute-plan`/`implementation-planning`; also a house hazard — its "commit to the current branch" defies the protected-branch floor), `writing-great-skills` (collides with the `agent-facing-design`/`writing-principles`/`skill-ux-design` cluster), `setup-matt-pocock-skills` (config infra for the mattpocock flow, not adopted; a divergent Claude-only copy already sat in `skills-claude/`, and the reinstall's `skills/` duplicate created a source-root collision, now resolved), `ask-matt` (a router over the mattpocock topology, not this library's; the one arguably-ownerless job — a library index — is declined without a park, no observed forget-which-skill friction in memory or handoffs). The reinstall's delivery breakage (ten missing `~/.claude/skills` symlinks, the setup-matt collision/wrong-target) is resolved; `teach` linked and delivery re-checked clean. Source for every removed skill is recoverable from `mattpocock/skills` (github) and the local trash (non-durable). Evidence: commits `b381e54` (diagnose fold), `e28c3d7` (teach admission), and this entry.

- 2026-07-06 — markdown-reader `build-macos-apps` routing (repo-local `AGENTS.md`, plus `docs/runbooks/macos-runtime-smoke.md`): admitted a compact always-loaded routing section telling agents to consult `build-macos-apps` only when macOS-specific implementation, debugging, or native UI surfaces give real leverage, with operational detail moved to a non-ambient runtime-smoke runbook. Closest existing repo contract was the generic verification section, which named `xcodebuild` but did not route SwiftUI, AppKit, build/run, test-triage, window, or logging decisions to the installed plugin; lighter context alone was insufficient because the observed failure was missed routing at the always-loaded decision point. agent-facing-design gate: context, not machinery — no mandatory field, score, classifier, threshold, or always-use rule; it explicitly rejects ceremonial plugin invocation. Evidence: markdown-reader commit `54584fa` (`Document macOS plugin routing`).

- 2026-07-08 — mattpocock/skills upstream re-adoption (Era-84 core batch, steps 1–3): rebase of JP's mattpocock-derived skill forks and setup/tracker infra onto current upstream `d574778`, treating upstream as the base and re-applying the house transform (roster rewire, Use-when/Do-not-use routing, dual-runtime tokens, proof boundaries, `trash`-not-`rm`/close-not-delete, protected-branch awareness, de-brand) on top, deviating from upstream only for environment facts and warranted house improvements. Every rebase/fold/admit is charter **case-(d)** (fate of third-party material); the pre-build consult was run and recorded **GO** at inventory §7.1 (`2a8636b`), and the durable evidence pointer is `docs/plans/2026-07-08-mattpocock-readoption-inventory.md` pinned to `d574778` (landed on `main` by this batch's ff-merge, per charter.md:66). The binding constraint the consult fixed: the **Thesis ceilings "upstream-wins"** — the house transform is re-authoring the charter mandates (assessed as it lands, charter.md:42), not preservable cruft a mechanical upstream-wins pass may discard. The honest per-skill answer came out **not uniform** (the anti-blanket-adopt thesis, confirmed in practice). **Rebased** — `to-prd` (hybrid: upstream `to-spec` body as base, re-derived the `needs-triage`/refuse-`ready-for-agent` label handling + consent gate, kept model-invocable; `a4c3879`); `to-issues` (hybrid: fork kept as governing base for its HITL/AFK `ready-for-human` routing + consent gate + `issues` naming, kept model-invocable; `18072a5`); `triage` (adopt-upstream-base: upstream is a strict superset + the "Verify the claim" generalization, house transform re-derived, fenced-code regression fixed, mutation-approval guard extended to PRs + close-not-delete, description rewritten to admit external-PR triage while excluding PR-review loops/`implementation-review`; `fb13be7`); and `setup-matt-pocock-skills` (adopt-upstream-base for the `skills-claude/` fork: PR/MR-triage plumbing at default `no` across the github/gitlab seed templates, `qa` kept dropped, grill-with-docs rewire re-applied, `triage-labels.md` seed de-branded to "Canonical role", wayfinding blocks kept out pending the wayfinder park; `139909f` — also charter trigger #3, installing contract-shipping material, since it authors an `## Agent skills` block and `docs/agents/*.md` into consumer repos). **Folded** — into `to-prd` the one dropped seam-minimization sentence (`to-spec:15`); into `to-issues` the wide-refactor recognize-and-route seam (to `/migration-campaign`/`contract-change-propagation`/`migration-safety` — recognition only, never the machinery, One Owner), the prefactoring line, single-fresh-context slice sizing, and native GitHub sub-issue/issue-dependency edges in place of text-only `Blocked by` (both verified available on `jpsweeney97/agents`). **Declined** — the `diagnose` Phase-1 "red-capable completion criterion" fold: a git re-check confirmed the 2026-07-05 fold (`b381e54`) surveyed the same upstream file (`bc4cf90`) whose completion criterion sat adjacent to the minimise step it took and called minimise "the one genuinely-additive discipline", and `diagnose` already gates on "a loop you believe in" — so the omission was a deliberate decline, not an oversight; no edit (git-confirmed). The two mechanical sharpenings (pasted-evidence proof + anti-theorising stop-guard) are held in reserve as an optional future upgrade, not a correction. **Reaffirmed keep-fork** (inventory §7.2, a genuine re-verification against the live files at the re-confirmed `d574778` pin, not assumed from the §11 lock; `30cff8a`) — `grill-with-docs` (upstream still ships a four-skill decomposition — a 7-line composition stub plus separate `grilling`/`grill-me`/`domain-modeling`, not a successor, so the roadmap's "→ domain-modeling rename" premise is false; the fork's inlined discipline is byte-current and its house layer has no upstream counterpart) and `decision-record` (single-sources the ADR template in place via `../grill-with-docs/ADR-FORMAT.md`; the format home stays at `grill-with-docs/` because `domain-modeling` is not adopted — closing the reopened domain-modeling question on the record). `implementation-planning` is house-native (admitted `966f903`), untouched, and takes no entry. **Parked** — `wayfinder` (see Parks; first ledgering of a park the 2026-07-08 roadmap left unrecorded). **Coverage-flagged, not adjudicated in this batch** — the roadmap's two triple-gated rejects `claude-handoff` (fires unattended) and `git-guardrails-claude-code` (always-loaded hook), named here so they are not silently dropped; the broader ~21 out-of-family roadmap parks/rejects stay roadmap recommendations until separately actioned. **Scoped out of this entry** (the Option-B landing refinement, extending the "own handling" treatment §11 already gives `save-handoff`/future-`wayfinder`): the `resolve-conflicts` whole-skill admit and the `save-handoff` redact-secrets/PII fold both land later via the Class-B plugin path (version bump + Codex republish + mirror) with their **own** ledger entries — `resolve-conflicts` a full case-(d) entry written only after its charter Admission test genuinely runs against the upstream body at build (§7.1 #4; the §11 "admit" is direction-of-travel, not a pre-committed verdict), `save-handoff` its own one-line note. **Supersession (append-only, never a rewrite):** this completes the 2026-07-05 entry's wholesale fork restore (line 36 above), which preserved each fork verbatim without rebasing its non-house content onto fresh upstream — it supersedes-in-part the 07-05 restore of `to-prd`/`to-issues`/`triage` (now rebased onto `d574778`) and appends reaffirmations for the `grill-with-docs` restore and the `decision-record` format-in-place call; `setup-matt-pocock-skills` supersedes the never-ledgered 2026-07-08 roadmap setup-REJECT (a fresh decision, not a supersession, since that reject was never ledgered — last prior ledgered decision is 2026-07-06). No adopted action in this batch trips the always-loaded (#1) or fires-unattended (#2) triggers — those sit only on the rejected/flagged candidates; `triage` stays model-invocable but its tracker writes are reversible (label/comment/close, approval-gated), not the irreversible-effect carve-out class. Each rebased skill was structurally validated (`check-library-integrity.sh` all-PASS; `claude-skills-sync.sh --check` exit 0) and proven by a context-isolated forward dry-run; all Class-A / in-place edits (no Codex republish, no mirror). Evidence: commits `d55ee4b` (inventory) → `4ac2376` (§11 lock) → `2a8636b` (consult §7.1) → `139909f`/`a4c3879`/`18072a5`/`fb13be7` (setup/to-prd/to-issues/triage rebases) → `30cff8a` (§7.2 reaffirmation) + this entry, ff-merged to `main`.

- 2026-07-09 — resolve-conflicts (new skill, `plugins/git-cycle/skills/resolve-conflicts/`, whole-skill third-party admission — the Era-84 re-adoption residual scoped out of the 2026-07-08 consolidated entry per §7.1 #4 / the Option-B "own handling" refinement): **ADMIT**. Charter **case-(d)** (fate of third-party material) plus a whole-skill admission. The pre-build consult ran **GO** for the batch (inventory §7.1, `2a8636b`) and §11 locked "admit as `resolve-conflicts`", but per §7.1 #4 that lock was direction-of-travel only — the Admission test was **genuinely re-run at build against the upstream body** (`mattpocock/skills:skills/engineering/resolving-merge-conflicts/SKILL.md` @ `d574778`, a 14-line 5-step procedure) and converged on admit independently, not deferred to the lock. **Owns** resolving an in-progress merge/rebase conflict faithfully and finishing it — a job no git-cycle sibling holds, verified firsthand against the live bodies: `merge-branch` is fast-forward-only and stops the moment a non-ff/conflict decision is needed (`merge-branch:25,81,131`, naming "rebase / explicit merge" as a next step it does not own), `exiting-worktrees` states "never resolve merge conflicts inside the worktree" (`:222`), `git-hygiene` aborts on any in-progress operation (`:36`), and `keep-green` covers lint/test-green after a change with no conflict surface — a genuine in-family routing cliff, not a non-job (conflicts are unavoidable in a protected-`main`, feature-branch, periodic-upstream-rebase flow, and two siblings' own text points at the missing lane). **Prevents** a mechanically-resolved conflict that compiles/passes but silently drops one side's intent or invents third behavior — a high-cost silent merge defect — which lighter context cannot cover: an always-loaded `AGENTS.md` line is the wrong altitude for a situational job, and folding into `merge-branch` would break its clean ff-only/stop-on-conflict contract (the locked design wants a routing boundary, not absorption). **House transform** applied: terse name matching `merge-branch`/`release-cut` (§11); Use-when/Do-not-use boundary against all four siblings with reciprocal routing edges added into `merge-branch` (`:25`, `:131`) and `exiting-worktrees` (`:121`); proof-by-behavior plus the protected-branch floor in the finish step; `--abort` allowed only as a deliberate abandon, not a crude never-rule. Machinery not copied — it operates in the mid-operation git state `merge-branch` refuses to start in, so no landing machinery is duplicated (One-Owner clean). **Honest bound:** no repo-local past conflict incident (the Era-84 rebases landed as clean ff-merges); under the global-merit paradigm (`AGENTS.md ## What The Skills Are For`) that is a build-order hint, never a rejection ground — the merit rests on the verified structural cliff, the high-cost silent-failure class, and global leverage (a git-cycle plugin skill fires in any repo JP hits a conflict). **Validated:** frontmatter parses (name==dir, 55-word description); `check-protected-set.sh` green across **6** surfaces (the new file added to the drift-guard `TARGETS`, carrying the canonical sentence verbatim); `check-library-integrity.sh` structural checks all-PASS (name==dir 84 skills, self-referenced paths resolve, no orphan support files); `claude-skills-sync --check` green; `git diff --check` clean. **Behavior forward-test PASSED** (Sonnet 5 subagent, skill loaded, against a constructed real two-branch merge conflict engineered so a loyalty-discount rule was protected only by its commit message, not by any test): the subagent reconstructed both sides' intent from `git log`, preserved both rules (independently verified — member $100 → 97.2, non-member → 108.0), invented nothing (merge diff was exactly the one tax line), and finished cleanly (real merge commit, no markers, tests pass); obedience proven, with two honest gaps left unexercised (the incompatible-hunk "note the trade-off" clause, and a without-skill differential control). **Class-B, not yet published:** the git-cycle Codex cache stays at `1.3.0` and `codex-plugins-sync --check` reports expected DRIFT until the publish train (version bump `1.3.0→1.4.0` + CHANGELOG in lockstep → Codex republish → mirror) runs on JP's ask; Claude serves the live source in place now. Durable evidence pointer: inventory `docs/plans/2026-07-08-mattpocock-readoption-inventory.md` (§7.1 GO, §11 lock) pinned `d574778`, the upstream source at that pin, and this build commit.
- 2026-07-09 — save-handoff redact-secrets/PII fold (`plugins/handoff/skills/save-handoff/`, the **final** Era-84 re-adoption plugin-path residual, scoped out of the 2026-07-08 consolidated entry per §7.1 / the Option-B "own handling" refinement): folded the upstream one-line principle "Redact any sensitive information, such as API keys, passwords, or personally identifiable information" (source `mattpocock/skills:skills/productivity/handoff/SKILL.md:14` @ `d574778`) into `## What To Capture` as a house-shaped safe-default — reference secrets by name/location, never transcribe verbatim, with the commit-exposure rationale attached. Charter **case-(d)** (fate of third-party material); a **fold**, not a whole-skill admit. Routed through **`agent-facing-design`** because it adds an obligation — verdict **context-with-safe-default, not machinery** (no field/status/validator/classifier), justified by the credential-exposure damage class (handoffs land in `.agents/handoffs/` and may be committed under host-repository policy — `save-handoff:71`), at the right altitude (per-artifact, inside the writing skill, single-sourced — deliberately not in `load-handoff`, which never writes). **Validated:** body-only edit so frontmatter is unchanged and still parses (name==dir, description intact); `git diff --check` clean; `quick_validate.py` was unavailable this environment (`~/.codex/skills/.system/` absent) so structural frontmatter validation was replaced by a manual parse check — noted, not waived. **Behavior forward-test PASSED** (Sonnet 5 subagent, live skill loaded, in an isolated throwaway repo, against a session whose raw context naturally embedded a production DB password, a live Stripe secret key, and customer PII + account number — redaction never instructed, so the skill alone had to trigger it): the produced handoff transcribed **zero** secrets verbatim (independently grepped — DB password, Stripe key, email, account number, and service username all absent) while preserving every non-secret fact (branch, commit, `config/legacy.py` location), referencing each secret by name/location instead. **Class-B, not yet published:** the handoff Codex cache stays at `3.2.0` and Codex will not see this line until the publish train (version bump `3.2.0→3.2.1` + **create `plugins/handoff/CHANGELOG.md`** — the plugin has none — in lockstep → `codex-plugins-sync.sh --publish handoff` → mirror) runs on JP's ask; Claude serves the live source in place now. With this fold landed, **both** Era-84 plugin-path residuals (resolve-conflicts admit + this fold) are dispositioned in-source; only the gated Class-B publish train for each plugin remains. Durable evidence pointer: inventory `docs/plans/2026-07-08-mattpocock-readoption-inventory.md` (§9 Class-B delivery, §11 "own handling") pinned `d574778`, the upstream source at that pin, and this build commit.
- 2026-07-09 — tdd tautological-test fold (`skills/tdd/tests.md` + one peer sentence in `skills/tdd/SKILL.md`): folded the by-construction-passing-test anti-pattern from `mattpocock/skills:skills/engineering/tdd/{tests.md,SKILL.md}` @ `d574778` into the first-party bad-test catalog — the assertion must never recompute the expected value the way the code does; expected values come from an independent source of truth (a known-good literal, a worked example, the spec). Charter case-(d) fold into the already-ledgered tdd fork (the roadmap's Tier-1 #1, HIGH confidence; absence re-verified at landing — recursive grep for `tautolog|by construction|recompute` over `skills/tdd/` empty before the edit). Behavior forward-test PASSED (fresh Sonnet subagent, updated `tests.md` as active guidance, neutral task of writing jest tests for a `cartTotal` reduce: all five produced tests used literal, hand-worked expected values; zero assertions recomputed the expected via the implementation's arithmetic). Evidence: roadmap `docs/plans/2026-07-08-mattpocock-skills-extraction-roadmap.md` §Tier-1 #1, donor @ pin, this commit.
- 2026-07-09 — writing-principles Negated lens fold (`skills/writing-principles/SKILL.md`, 9th Challenge-Order lens): folded the Negation/positive-prompting discipline from `mattpocock/skills:skills/productivity/writing-great-skills/GLOSSARY.md:161-165` @ `d574778` — steering by prohibition drags the forbidden behavior into context and makes it more available; rewrite to the positive target, keeping a prohibition only as a hard guardrail paired with its positive target — worded with the mandatory exemption for routing non-use boundaries (the house's deliberate `Do not use for...` description convention) per the roadmap's Tier-2 wording caution. Charter case-(d) (fate of third-party material into a first-party skill); routed through the `agent-facing-design` gate — verdict: context/lens, not machinery (extends the existing fast-scan list; no field, status, score, classifier, validator, or stage; the hard-guardrail-vs-rewrite call stays with the reviewing agent), run as an honest re-check of a settled roadmap direction with veto retained. Behavior forward-test PASSED at the lens level (fresh Sonnet subagent, updated skill as active guidance, three-rule team-rules doc: Negated fired on both prohibition rules with positive rewrites — one pairing the kept guardrail with its positive target — and did not fire on the routing-boundary line; honest caveat: the pre-existing Unclear/Unjustified lenses separately critiqued that line's missing runbook referent, outside the folded surface, so the plan's literal third-line-unflagged proxy tripped while the lens behavior it guarded was correct). Evidence: roadmap §Tier-2 FOLD candidate, donor @ pin, this commit.
- 2026-07-09 — plan-panel-loop charter classification (Era-83 open question, discharged): **build-and-prune, not gated**. The Reversibility-Class read (charter.md ~:22) on the live skill text (unchanged since `ddf1e6b` — git log confirms only the add + containment commits): it cannot fire unattended — invocation requires the user explicitly wanting panel+patch+re-review (SKILL.md description and Trigger Boundary), reviewers are forbidden from running it, scrutinize, or any nested multi-agent workflow (Subagent Containment, SKILL.md:65), and its within-turn subagent dispatch happens inside an attended session whose consolidated findings and mandatory Closeout packet land in the user-read transcript — dispatching subagents ≠ being a "subagent dispatch no human reads at call time" (same seam as Era-84's "triage model-invocable ≠ unattended"); it wields no irreversible-effect tools — reviewers are read-only by brief (SKILL.md:59, subagent-brief.md:19-21), the main agent patches only the named plan artifact (SKILL.md:79), remote targets default to chat-shaped replacements (SKILL.md:24), and the only mutations (named-file edit, policy-required local commit) are reversible, none in the send/merge/force-push/delete class; and it is first-party (Era-83 build, `4616498` + containment spine `ddf1e6b`). Classification note only — no Admission test runs for build-and-prune; recorded because the throughline carried the question as open. Evidence: `skills/plan-panel-loop/SKILL.md` + `references/subagent-brief.md` at this commit, `ddf1e6b`.

- 2026-07-11 — `claude-code-docs` package `AGENTS.md` resync (`/Users/jp/Projects/active/claude-code-tool-dev/packages/mcp-servers/claude-code-docs/AGENTS.md`): **ADMIT**. This is a gated ambient-contract update, not an exempt factual copy. **Owns** package-local operating truth for Codex sessions in this package — commands, architecture, cache behavior, test gates, and version-bump obligations — by updating the existing package `AGENTS.md` owner; package `CLAUDE.md` serves a different runtime and does not co-load, so there is no same-runtime owner collision. **Prevents** the observed drift already present in `AGENTS.md` (missing trust/canary/status behavior and detailed version-bump rules), including stale guidance that could cause a Codex executor to serve a stale index or validate the wrong runtime path; package-local always-loaded context is the lightest reliable surface, while a separate reference would duplicate the owner and be easier to miss. **House fit:** first-party, package-scoped, evidence-oriented, and explicit about commands and proof; admission is conditioned on correcting `CANARY_VERSION`'s location to `src/index-cache.ts` and replacing the inherited `rm -rf dist/` wording with the global-safe `trash dist/` form before the copy. No new workflow or routing lane is introduced. Decision recorded before implementation; the package `AGENTS.md` has not yet changed. Evidence: `claude-code-tool-dev` commit `a6c4d24a` (`docs/plans/2026-07-11-claude-code-docs-corpus-maintenance.md`, Task 5.3).

- 2026-07-12 — skill-use contract (Era-87; the first always-loaded candidate run through the full pre-landing probe gate): **PARK, both decisions — no admission, no global-file or repo-`AGENTS.md` edit**. **Decision A** (byte-identical `## Skill Use` runtime contract in `~/.claude/CLAUDE.md` + `~/.codex/AGENTS.md`): parked on the sealed probe result. The pre-registered, blinded, 40-trial ON/OFF experiment (prereg sealed at `88da1b6`; results appendix `967d892` + addendum `533d657`, all in `docs/plans/2026-07-12-skill-use-probes-preregistration.md`) returned **NOT-GO** under its own §8 aggregation: zero of three positive probes passed — comp **inconclusive-by-ceiling** (both arms composed 3/3; task demand saturated the channel), mid a **true-silence fail** (ON 0/3 against the sealed key; the pre-registered honest reading of design bullet 1), seam a **post-escalation fail** (ON 1/5 vs OFF 0/5; the pre-registered decline-hatch reading — the governing skill's own exit licenses inline handling, so a contract-faithful agent declines the handoff) — while the **calibration gate held** (zero ON overfire on all three negative probes; the contract's measured cost was nil). Honest bounds carried: Claude-side only, same-model-scored, n=3–5 per arm. Per §8 the affected clauses and combined block returned for redesign; JP directed **park, not redesign** (2026-07-12, adopting the post-run recommendation): the run's positive mechanism finding — agents demonstrably read and obey governing-skill **body text** where ambient contract text was inert — routes the original want (silence + missed composition) into the design §2 build-and-prune data layer instead (ten composition-exit edits, commit `a0d3437`; not ledgered, per build-and-prune). **Decision B** (repo `AGENTS.md` handoff-placement convention line): parked with A by the same direction — a lone always-loaded line inheriting the same no-measured-benefit status once A is parked; never separately probed. Neither decision ran the Task-9 Admission consult: a park authors nothing ambient, so no consult is owed. Durable evidence: the design doc `docs/plans/2026-07-11-skill-use-contract-design.md` (six review rounds; Build Records incl. the Task-2 canonical wording, the Task-3 Gate-1 terrain verification, and the Gate-2 disposition), the sealed prereg + appendix, and the committed census artifacts. Reopen: see Parks.

- 2026-07-14 — Markdown word-grain diff review (repo `AGENTS.md`, Working Defaults; companion amendment to the 2026-06-23 prose-wrap admission): admitted one always-loaded bullet directing agents to review Markdown prose diffs at word grain — `git diff --word-diff=plain` (also with `--cached`, `git show`, `git log -p`), reading `--stat` first and scoping to file paths when output is large, with line-grain diff kept for fenced code, tables, and YAML frontmatter. Owns the diff-*reading* convention the 2026-06-23 *writing* convention created a need for: one-logical-line paragraphs make plain line-grain `git diff` re-print an entire paragraph as deleted-plus-added for a one-word edit, so the changed words are invisible and hunks overflow agent tool-output caps. Closest existing contracts and why not their job: the prose-wrap bullet governs how Markdown is written, not read; `Git And Cleanup`'s "review `git diff --stat` and the relevant diff before staging" mandates that the review happen but not how to make long-line prose diffs readable. Lighter context rejected as already minimal: the failure forms during ordinary pre-commit diff review when no skill fires, so only the always-loaded surface covers the moment (the 2026-06-27 precedent's reasoning); the alternative of changing the writing convention to sentence-per-line was considered and rejected — it would destroy the 1:1 line-to-logical-unit citation anchors the repo's review workflow leans on (ten `spec:NNN`-cited review rounds on the deliberate spec alone) and force a repo-wide reflow, while the diff problem has a pure tooling fix. agent-facing-design gate: pure context — a reading default with a concrete command; no field, score, classifier, validator, or hard rule (mirrors the 2026-06-23 and 2026-06-24 pure-context precedents). One Owner checked: no contract in either runtime owns diff-reading mechanics; no collision. Friction (observed, 2026-07-14 session): a 14-edit spec fold (`bdfb2e7`) produced a 43KB line-grain diff that exceeded the agent output cap, forcing a persisted-file detour and degrading the pre-commit diff review to spot-check greps; JP asked for the convention after the impact was explained. Scope: repo `AGENTS.md` only, per JP's direction; the global Markdown section is untouched. Evidence: commit `bdfb2e7` (the diff exhibiting the failure) + the `AGENTS.md` bullet and this entry (this commit).

- 2026-07-17 — `worktree-task-cycle` (`plugins/git-cycle/skills/worktree-task-cycle/`, dual-runtime via the git-cycle plugin): **ADMIT**, gated as a first-party irreversible-effect skill (the charter's Reversibility Class carve-out, charter.md:22 — it wields `git merge --ff-only` into the integration branch, `git branch -d`, and `trash` on lease/record state; the first ledger entry of this class — not a case-(d) third-party event). **Owns** the persistent-satellite task lifecycle defined by `docs/specs/2026-07-16-skill-worktree-system.md`: lease-serialized activation of a fresh task branch from the verified integration branch in a parked, locked satellite; validation bound to the exact tip SHA; fast-forward landing of the validated SHA through the primary checkout; proven re-parking; safe deletion; and fact-vector recovery classification that never auto-chooses destructive recovery. Closest contracts and why the work is not theirs: `merge-branch` refuses this topology twice (source-in-another-worktree, target-checked-out-elsewhere) — its lane is single-checkout landing by switching to the target; `exiting-worktrees` owns disposable-worktree removal and must never remove a locked satellite; `resolve-conflicts` and `closeout-check` are routed to, not overlapped; `git-hygiene`'s branch pruning can at worst leave one orphaned validation record, which `inspect` reports and `delete-branch`'s guarded absent-branch path cleans. Both adjacent skills received routing boundary edits at the seam, plus a protective don't-remove floor in `exiting-worktrees` (this commit). **Prevents** per-session re-derivation of the ~80-line pilot procedure with irreversible failure modes: merge commits polluting the integration branch, `-D` destroying unlanded work, forced removal of locked permanent satellites, unleased racing integrations, stale-tip or unvalidated landings. Machinery is single-sourced in `scripts/worktree_cycle.py` (stdlib-only Python, fail-closed labeled output contract, hermetic test suite at `tests/test_worktree_cycle.py`); the skill requires attended top-level sessions and refuses destructive recovery unattended. Universal (non-cooperative) enforcement stays deliberately deferred; raising it later is its own charter-gated always-loaded-contract event. **Forward obligation:** the proving matrix (one Claude Code cycle + one Codex cycle against the two pilot satellites: normal path, helper path resolution, stale-base, foreign-lease refusal, both interrupted-state reconstructions) runs before any republish or mirror authorization; a dated update line here will record its completion. Evidence: `docs/specs/2026-07-16-skill-worktree-system.md`, the landed paths above, and this commit.

- 2026-07-17 — `worktree-task-cycle` proving-sequence correction (appends to, never rewrites, the 2026-07-17 admission entry above): that entry's forward obligation misstates the settled sequence by saying the full dual-runtime proving matrix "runs before any republish or mirror authorization" — the Codex half of the matrix runs against the republished versioned cache, so it cannot precede republication. The settled sequence (adjudicated design v3 §7, restated in JP's 2026-07-17 review): **Claude rows precede Gate B** (local Codex republish); **Codex rows follow Gate B in a fresh Codex session** against the republished cache; **Gate C** (mirror + push) follows the completed matrix. Context: the same review found four contract violations and one recovery-routing defect in the landed 1.5.0 helper, withheld Gate B, and granted a narrow Gate A repair — landed as git-cycle 1.5.1 (five fixes; six regression tests proven red against the unfixed helper first — the five per-defect pins plus the contained-lane LEASE-ORPHANED pin — see the 1.5.1 CHANGELOG section). The admission itself is unchanged; the repair is build-and-prune skill churn recorded here only because it travels with this correction. Evidence: JP's 2026-07-17 review verdict (Major revision; Gate A granted for 1.5.1, Gates B and C withheld), the 1.5.1 landing commit, and `plugins/git-cycle/CHANGELOG.md` § 1.5.1. Update (2026-07-17): the Claude rows are complete — rows 1, 2, 4, 5, 6 all PASS against the two pilot satellites at 1.5.1, durably recorded with verbatim outputs at `docs/smoke-tests/2026-07-17_worktree-task-cycle-claude-proving-rows.md`; rows 0/3 and every Codex half remain open pending Gate B (fresh Codex session against the republished cache), Gate C after the completed matrix.

- 2026-07-17 — `worktree-task-cycle` symlink-record repair (appends context to the two 2026-07-17 entries above; the admission is unchanged): JP's Gate-B readiness review reproduced a fail-open the 1.5.1 closeout had disclosed as a ridden minor and adjudicated it a blocker — a dangling symlink planted at a validation-record path was classified absent (`Path.exists()` follows symlinks) and `record-validation` then wrote the record through the link, creating a file outside `.git/skill-worktree/validations` while printing `RESULT: ok`. Gate B stayed withheld; Gate A was granted for a narrow 1.5.2: lstat-based `symlink` classification at record paths (live and dangling) with every consumer failing closed before mutation and the symlink plus target preserved as evidence; a default-deny restructure of `record-validation`'s existing-record dispatch (only readable-matching supersede or true absence proceeds); the record write reopened under `O_NOFOLLOW` so the no-symlink rule is enforced at the write itself, with pre-open checks refusing non-regular files and hardlinked records (aliases `O_NOFOLLOW` cannot see; the `O_TRUNC` supersede would rewrite a shared inode's bytes outside the store); `delete-branch` refusing a symlinked record before the branch mutation (an adjudication note riding on `RESULT: ok` is the weakest signal in the output contract); non-regular files (FIFO, socket, directory) at record paths classifying `unreadable` instead of blocking at open (1.5.1 hung indefinitely on a FIFO, under `land` with the integration lease held); and a `discover`-time store-integrity gate proving `skill-worktree/` and `validations/` are real non-symlink directories under the resolved git common dir. Eleven regression tests (dangling record, live-symlink supersede, hardlinked-record supersede, symlinked validation root, symlinked store parent, symlinked-record land refusal, symlinked-record delete-branch refusal, a non-regular-file classification pin, an in-process `O_NOFOLLOW` write-guard pin, a guarded-writer wiring pin, create+supersede control); the seven black-box defect pins were proven red against the extracted 1.5.1 helper before the fix, the FIFO pin's red form is a liveness hang proven by a 5s-timeout probe rather than inside the suite, the two in-process pins are red-by-absence there (`write_record` does not exist in 1.5.1), and a per-guard mutant pass shows 1:1 teeth — each removable guard's excision is caught by at least one test; the suite is 80/80 green after. A three-lens adversarial review (fail-open, regression, mutation-teeth) drove the write-guard teeth pins and the hardlink, delete-branch, and FIFO repairs; its residue rides disclosed: hardlinked records still classify `ok` on read paths (shared-inode bytes are genuine store content, so reads cannot be aliased into lying — a design question, not a fail-open), alongside the leases-dir symlink item. Lease machinery is deliberately byte-untouched — the store-parent gate covers the leases directory's parent, but lease-dir symlink handling itself remains open, disclosed and riding. Sequencing per the same authorization: Claude rows 1, 2, 5, 6 rerun against landed 1.5.2 before Gate B (they exercise record creation or helper identity); row 4 carries forward on the explicit boundary that lease classification, scope matching, and state mapping are unchanged by this diff; a dated update line here will record the rerun. The repair itself is build-and-prune churn, ledgered as context because it travels with the proving-sequence obligation. Evidence: JP's 2026-07-17 Gate-B review (blocking behavior smoke, session transcript), `plugins/git-cycle/CHANGELOG.md` § 1.5.2, and this commit. Update (2026-07-17): 1.5.2 landed on `main` as `78b4418` through the decision-record satellite by the helper's own verbs, and the Claude rerun is complete — rows 1, 2, 5, 6 all PASS against the installed 1.5.2 helper (sha256-matched to `main`'s landed blob), row 4 carried forward under the stated boundary, durably recorded with verbatim outputs at `docs/smoke-tests/2026-07-17_worktree-task-cycle-claude-proving-rows-1.5.2.md`; rows 0/3 and every Codex half remain open pending Gate B (fresh Codex session against the republished cache), Gate C after the completed matrix.

- 2026-07-17 — `worktree-task-cycle` lease-root repair (appends context to the three 2026-07-17 entries above; the admission is unchanged): JP's second Gate-B readiness review adjudicated the disclosed leases-dir symlink item a blocker — a confirmed out-of-store mutation path, not a theoretical hardening concern: in a hermetic repo with a valid locked satellite, `skill-worktree/leases/` replaced by a symlink to an external empty directory let a normal `lease-acquire` create the lease dir and `owner.json` outside the git state store, exit 0, `RESULT: ok` (`discover` proved `store` and `validations` non-symlink but never `leases`; its later `leases.is_dir()` followed the link). Gate B stayed withheld; Gate A granted for a narrow 1.5.2 → 1.5.3: `topo.leases` joins the same `discover`-time real-directory/non-symlink invariant — the closure is specifically that every owned state-root component (`skill-worktree/`, `leases/`, `validations/`) must be a real directory under the resolved git common dir, with explicit instruction not to broaden into general filesystem-threat hardening. Red-first: JP's counterexample reproduced against the extracted 1.5.2 helper (sha256 `4efb4c9a…` — exit 0, lease + owner.json created outside the store); the dangling variant's red form is the untruthful "no skill-worktree store" refusal; both pins red against 1.5.2, suite 82/82 green after the fix, guard-excision mutant killed by both pins. The pins assert the gate's own phrases ("store integrity failed", "is a symlink") because pytest embeds the test name in tmp_path and these tests' names contain "symlink" — a bare substring assert self-satisfies via the path echo; the same latency exists in prior symlink tests' message asserts (their teeth are exit-code- and mutation-proven; noted, not repaired — out of this repair's boundary). Ridden per JP's ruling: hardlinked-record read classification (no helper-originated outside mutation), the exit-code/mode deltas, and the disclosed scratch-only `rm` process note. Sequencing per the same authorization: Claude rows 1, 2, and 4 rerun at landed 1.5.3 (row 1 normal lease lifecycle, row 2 installed-path identity for the new bytes, row 4 direct lease adjudication); rows 5 and 6 may carry forward only if the diff touches nothing beyond the shared discovery invariant with record/state-routing logic byte-identical, stated explicitly; Gate B returns after those reruns; Gate C separate and withheld. A dated update line here will record the rerun. Evidence: JP's 2026-07-17 second Gate-B review (blocking behavior smoke, session transcript), `plugins/git-cycle/CHANGELOG.md` § 1.5.3, and this commit. Update (2026-07-17): 1.5.3 landed on `main` as `ccf4365` through the decision-record satellite by the helper's own verbs, and the Claude rerun is complete — rows 1, 2, 4 all PASS against the installed 1.5.3 helper (sha256-matched to `main`'s landed blob): row 1 normal full cycle on decision-record, row 2 installed-path identity, row 4 fabricated-foreign-lease adjudication rerun directly on work-router; rows 5 and 6 carried forward under the stated single-hunk discovery-invariant boundary, durably recorded with verbatim outputs at `docs/smoke-tests/2026-07-17_worktree-task-cycle-claude-proving-rows-1.5.3.md`. Per JP's ruling Gate B returns after these reruns; rows 0/3 and every Codex half remain open pending it (fresh Codex session against the republished cache), Gate C separate and withheld.

## Parks

- type-design invariant lens — parked by JP's explicit choice (2026-06-12; recorded in `ad9c0cc`); reopen only on JP's ask.
- feature-dev approach-triad example — parked by JP's explicit choice (2026-06-12; recorded in `ad9c0cc`, from the feature-dev pass `2dc1b95`); reopen only on JP's ask.
- code-comprehension lane — parked (2026-06-12; recorded in `ad9c0cc`); reopen on an observed comprehension failure in real work.
- frontend-design web-aesthetics guidance — parked (2026-06-12; recorded in `45c8a5a`); reopen on the first real web-frontend build task in observed work; on reopen, re-author to house standards from the marketplace catalog source, do not reinstall.
- security-guidance Codex-runtime security review — parked (2026-06-13, pass 10); reopen on the first observed Codex-side security-review need or a genuine `codex-security:security-scan` fire. On reopen, author to house standards; do not reinstall the plugin.
- security-guidance on-disk/at-rest secret detection — parked (2026-06-13, pass 10); reopen on the first observed at-rest-secret miss in real work (the gap bundled `/security-review` explicitly disclaims).
- security-guidance whole-repo / pre-existing-code security scanning — parked (2026-06-13, pass 10); reopen on the first observed pre-existing-code vulnerability that a diff-scoped review missed.
- philosophy-audit remaining corrections (the REWRITE/GROUND M-items not accepted in the 2026-07-02 adjudication) — parked (2026-07-02; disposition doc); reopen per-item when a transcript shows the specific misfire that item predicts; adopt singly, never as a batch restyling.
- philosophy-audit `principlesToAdd` machinery (ten items: single-sourcing scans, collision detectors, scheduled bloat audits, falsification channels, circularity-escape gates, independent-verification admission, interface contracts, baseline-comparison rule, and kin) — parked (2026-07-02; disposition doc); each reopens only as its own charter admission on observed friction matching it; the idempotency and auto-commit items were accepted 2026-07-02 in lighter, non-machinery form.
- skill-use `## Skill Use` runtime contract (Decision A) + repo `AGENTS.md` handoff-placement convention line (Decision B) — parked (2026-07-12; JP's direction after the sealed NOT-GO; own Decisions entry). Reopen triggers, either sufficient: (a) the silence or missed-composition failure modes recur in lived work after the data-layer exits (`a0d3437`) have had time to fire — read at the 2026-08-01 ledger checkpoint and after, suffix-aware; (b) JP commissions a re-aimed sealed probe — the appendix's bounded out-of-key observations (both ON-arm: mid main-task takes 2v0, n2 offers 2v0) pre-name the hypothesis, request-time/main-task adoption propensity, a channel the first instrument did not key and the design's fixed-terrain narrowing disowned, so reopening it also reopens that settled-negative. On reopen the wording re-enters at the charter gate; the parked canonical text is the Task-2 record in the design doc's Build Records.
- wayfinder tracker-backed investigation-planning skill (mattpocock upstream) — parked (2026-07-08; first ledgered here with the Era-84 re-adoption core batch; the 2026-07-08 roadmap left this park unrecorded). Un-parking is a skill-squad-class build, not a rebase: author the missing tracker "Wayfinding operations" mechanism in `setup-matt-pocock-skills` (sub-issues + native issue-dependencies via `gh api`), create the 5 `wayfinder:*` labels, rewire `/grilling`+`/domain-modeling`→`grill-with-docs` and the doc-page refs, add a close-not-delete guard around its irreversible `gh issue delete` (SKILL.md:125), and author a Use-when/Do-not-use boundary vs `implementation-planning`/`to-issues`/`outcome-shaping`/`design-exploration`. It independently trips the irreversible-effect carve-out (charter.md:22), so adoption is a full case-(d) ledger entry regardless of de-gating; placement if built is `skills-claude/` (its hard dependencies are Claude-only). Reopen trigger: an observed real need for multi-session fog-of-war tracker-backed investigation planning that no incumbent covers. Evidence: inventory §5/§6/§11 pinned `d574778`.

## Mining Queue

Unmined contract-shipping surfaces, on amended charter terms (rejection evidence named per discard):

(empty — all ten queued surfaces mined through pass 11.)

Assessed and exempt (2026-06-13): the `github` plugin — pure MCP capability tooling, no contract text (see Decisions). Out of scope; nothing to extract; stays installed. The mining program is fully closed — no queued or unassessed contract surfaces remain. Fully closed means the discovery sweep is complete — the queue is empty and every discovered surface has a recorded disposition — not that every job is final: parked surfaces stay open until their trigger fires. Installing or reinstalling any running contract, or a park trigger firing, reopens the program.

Reopened (2026-07-08): the 2026-07-05 mattpocock reinstall reopened the program and the Era-84 upstream re-adoption (this batch's 2026-07-08 entry) continues it as a **fresh-mining pass** against `mattpocock/skills`@`d574778` — an external scratch checkout, not an installed plugin, so Extraction's remove-the-original does not bind it and the environment stays clean of raw upstream contract text. The core batch is dispositioned (four rebases, four folds, one decline, three keep-fork reaffirmations); residual re-adoption surfaces remain open via the plugin path — the `resolve-conflicts` admit and the `save-handoff` redact fold (each its own later ledger entry) — and `wayfinder` is parked (see Parks). The broader ~21 out-of-family roadmap parks/rejects (`docs/plans/2026-07-08-mattpocock-skills-extraction-roadmap.md`) stay roadmap recommendations until separately actioned. Update (2026-07-09): `resolve-conflicts` **admitted** (own case-(d) entry in Decisions; `plugins/git-cycle/skills/resolve-conflicts/`) — the `save-handoff` redact fold is now the sole open plugin-path residual. Update (2026-07-09, later): the `save-handoff` redact-secrets/PII fold **landed** (own case-(d) entry in Decisions; `plugins/handoff/skills/save-handoff/`), closing the last open plugin-path residual — every Era-84 re-adoption surface is now dispositioned in-source, with only the gated Class-B publish trains (git-cycle `1.3.0→1.4.0`, handoff `3.2.0→3.2.1`) outstanding. Update (2026-07-09, closing): both Class-B publish trains ran the same night — git-cycle `1.3.0→1.4.0` and handoff `3.2.0→3.2.1` cut on-branch, ff-merged, republished to the Codex cache (`--check` green) and synced to the mirror (`aca0aef8`) — so nothing Era-84 remains outstanding in any channel; the program returns to closed-pending-triggers (parks stand, `wayfinder` per its entry).
