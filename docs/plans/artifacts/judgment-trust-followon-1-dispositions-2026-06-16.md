# Follow-on (1) — judgment-skill re-review dispositions (2026-06-16)

**What this is.** Follow-on (1) of the judgment-trust apparatus = "re-review-and-cut each judgment skill under the new reviewer, drawing on the re-triaged backlog" (`skill-library-backlog-retriaged-2026-06-15.md`). Scoped honestly to where the new bar actually *flips* a disposition: the four P2 judgment rows (19, 20, 21, 22). The re-triage (Task 10) already applied the bar to the whole library's findings; the flips concentrate in these four rows, so this is not a 22-skill sweep.

**Outcome: ZERO skill edits.** All four rows resolve to no-action. This is the *expected* result, not a null one: the flip-set confirmed by exhaustive search that the live library has no clear softened-provoke defect — it was over-flagged on conformance, not under-provoked. Applying the new bar correctly therefore *prevents* conformance edits rather than mandating cuts.

**Method.** Apparatus confirmed live on `main` (`## Two Kinds of Skill` at `agent-facing-design/SKILL.md:65`; `scrutinize-skill` carries the lens; review-family `0.3.10`), so the re-review runs under the proven reviewer. The one genuinely-open judgment (row 19) went to three independent blind reviewers, fenced from this artifact set and the flip-set key — the same contamination that hit flip-set Run 1. The three no-action rows were confirmed against live state.

## Row 19 — system-design-review finding-cap → KEEP (no edit)

Target: `references/system-design-dimensions.md:39` — "Finding targets: low 3-5, medium 5-8, high 8-12. Hard caps: low 6, medium 9, high 12 main findings or 15 with appendix." Backlog disposition was **reverse** ("question whether the cap belongs").

Blind verdicts (fenced from the backlog/flip-set): **2 keep-both, 1 split** (keep soft targets / cut hard caps). The soft "Finding targets" are a **unanimous keep** — a stakes-keyed proportionality + triage forcing function. The disagreement is confined to the **hard caps**: 2 keep, 1 cut; 2 of 3 reviewers called the overall question balanced.

Adjudication (independently re-derived): **keep, no edit.** The "or 15 with appendix" valve means real findings up to 15 are *demoted to an appendix, not dropped* — the ceiling predominantly shapes foregrounding (main vs appendix), not existence (reported vs not), and it is generous relative to the 8-12 target, so it bites only when the reviewer is over-producing, which is exactly when triage discipline is wanted. That is structure that elicits the hard judgment ("what matters most here?"), not structure that answers it. The "reverse" was honored — the cap was questioned, hard and blind — and it largely earns its place; keep here is discrimination, not conformance inertia.

**Recorded minority position (do not silently re-litigate):** cutting the *hard-cap ceiling only* (keeping the soft targets) is a defensible split — its case is that the per-finding quality gates (Lens / Decision state / Anchor / Problem / Impact / Recommendation) already enforce rigor on merit, making a global count ceiling redundant in the padding case and substitutive in the rare >15-real-findings case. This is a genuinely balanced provoke-case, which the apparatus treats as a judgment call, not a gate; the anti-over-cut default settled it toward keep. If a cut is ever made, the consequential edit is to narrow the SKILL.md "Deep Dive And Output" pointer from "decision states, finding caps, tension rules" to "finding targets" so the body does not dangle, and to leave the Stop Rules "cap findings at 4" alone (that cap is tied to sparse evidence — a genuine restraint, out of scope).

## Row 20 — review-reviewer section order → no action

Drop the "match sibling section order (move Review-Family Routing up)" nit. Live state: `## Review-Family Routing` at line 42, below `## Boundaries`:12, vs siblings that lead with it. Section ordering does not make the judgment for the agent — cosmetic uniformity.

## Row 22a — scrutinize verdict-token casing → no action

Drop the cosmetic casing nit on the verdict tokens. No effect on critique quality.

## Row 22b — tdd output-shape → no action (reverse)

The absence of a mandated output shape / done-condition section is **correct** for a judgment skill; adding one would be substitutive structure. Reverse the old conformance instinct to "add a closure/output shape."

## Row 21 — over-cap description trims (11 skills) → no action here

Preserved as budget recovery while over the Codex cap; only the conformance-only residual drops once each is back under cap. This is delivery hygiene (budget), not a judgment cut, and stays uniform across both bars.

## Honest success framing

This pass applied the new bar to the flipped rows and concluded near-zero cuts. The claim proven is: **the right bar was applied, the conformance nits were correctly *not* acted on, and the one cut-candidate (the finding-cap) is a forcing function** — *not* "the skills are now better." Uplift ("better") is unmeasurable for pure-judgment skills (`skill-benchmark` explicitly refuses to put a number on judgment quality) and is not claimed. The leading proxy (flip-set, PASS) certifies the reviewer discriminates on clear cases; it does not certify this live balanced disposition, which is a recorded judgment call, self-graded by construction (same authorship of bar and reviewer) with the blind triplicate as the only independence applied. A broad fresh sweep of all ~22 judgment skills was deliberately not done: low yield (no clear softened-provoke defect exists) and high over-cut risk.

## References

- Worklist: `skill-library-backlog-retriaged-2026-06-15.md` (rows 19-22).
- Apparatus: `../2026-06-15-judgment-trust-apparatus.md`; acceptance proof: `judgment-trust-flip-set-results.md` (the leading proxy).
