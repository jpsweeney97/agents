---
title: "mattpocock/skills Re-adoption Inventory (Era-84)"
date: 2026-07-08
branch: feature/mattpocock-readoption-inventory
source_pin: mattpocock/skills @ d574778
status: inventory / adjudicated 2026-07-08 — recommendations locked, nothing built
scope: upstream-as-base re-adoption, read-only blast-radius inventory
supersedes_partial: docs/agents/contract-decisions.md 2026-07-05 (ledger line 36, append-only supersession at landing); the 2026-07-08 roadmap wayfinder-PARK and setup-REJECT were never ledgered (last ledgered entry is 2026-07-06), so acting against them is a fresh decision, not a supersession
---

# mattpocock/skills Re-adoption Inventory (Era-84)

## 1. What this document is

This is a read-only blast-radius inventory for an upstream-as-base re-adoption of `mattpocock/skills` pinned at `d574778`. Scope was settled via `/outcome-shaping`; nothing here is built or landed, and a charter consult (`docs/agents/charter.md`) is required before any building begins.

The governing stance for every call below: current upstream `d574778` is the base; JP's house layer is a re-appliable transformation, not a set of files to preserve for their own sake; the default is upstream-wins, and each retained deviation must be justified as either an environment fact (JP's real tracker, labels, roster, runtimes, safety floor) or a warranted house improvement (proof discipline, routing convention, cognitive-offload). The two failure modes to avoid in equal measure are sunk-cost bias (freezing a fork because it exists) and overcorrection (discarding a real house improvement to honour "upstream-wins" mechanically). Upstream's `deprecated/` set (`design-an-interface`, `qa`, `request-refactor-plan`, `ubiquitous-language`) is excluded and carries no re-adoption question.

This inventory re-opens the 2026-07-08 roadmap's own wayfinder-PARK and setup-REJECT under the rebase lens, and re-examines parts of the 2026-07-05 ledger entry (line 36) where the format-home and to-issues dispositions are concerned. Two governance facts recovered from the dedicated charter read (§7) sharpen this framing. First, the 2026-07-05 entry is a *ledgered* decision, so a landing that changes it appends a new entry naming what it supersedes (never a rewrite). Second, the 2026-07-08 roadmap's park and reject were *never ledgered* — the last ledgered decision is 2026-07-06 — so they are un-landed recommendations in a plans doc, not settled charter facts: acting against them is a fresh charter decision rather than a supersession, and the "do not re-litigate settled parks" rule does not bind them. Every such move is a recommendation gated behind the charter consult.

Each of the 12 findings below was produced by a reader and then checked by an adversarial verifier. Where the verifier required corrections, the verifier's version is what this document states — the corrections are folded in inline, and the largest reader-vs-verifier disagreements are surfaced explicitly rather than smoothed over.

Update (2026-07-08): the §11 open questions were walked with JP and locked the same day — every call landed on the inventory's recommended option, with `diagnose` decided after a git re-check confirmed a deliberate 07-05 decline. §11 now records the decisions of record; they remain gated behind the pre-build charter consult (§7) — locked is not built.

## 2. Executive summary

The settled scope is a rebase of JP's mattpocock-derived skill forks and tracker/setup infra onto fresh upstream `d574778`, treating upstream as the base and re-applying JP's house transform on top, deviating from upstream only for environment facts and warranted house improvements. The apparatus is recommend-only; the charter gate and per-decision ledger entries precede any edit.

Bottom line: the honest per-skill answer is not uniform. Two surfaces are genuinely stale supersets of what JP forked and should take current upstream as the base with the house transform re-applied — `triage` (upstream gained a Verify-the-claim step, a redundancy/already-implemented-wontfix tree, and PR-as-triage-surface machinery the fork predates) and `setup-matt-pocock-skills` (upstream gained PR/MR-triage-surface plumbing and Wayfinding-operations blocks across all three tracker templates). Three are hybrid-rebases where the fork carries load-bearing environment facts or house improvements that legitimately override upstream, but the fork is behind on a small set of real upstream gains worth folding — `to-issues` (keep the fork's HITL routing, consent gate, and `issues` naming; fold the wide-refactor recognition seam, prefactoring, context-window slice-sizing, and medium-confidence native sub-issue edges), `to-prd` (take the upstream body verbatim, re-derive the `needs-triage`/refuse-`ready-for-agent` label handling and consent gate, re-absorb the one seam-minimization sentence the fork dropped), and `diagnose` (keep the richer fork body, fold the Phase-1 red-capable completion gate). Three are keep-fork because upstream has no superior successor — `grill-with-docs` (upstream still ships a 7-line stub alongside `domain-modeling`; the fork's inlined discipline is byte-current, so nothing is stale), `implementation-planning` (a house-native lane upstream never had, not subsumed by the to-tickets merge), and `decision-record` (upstream has no equivalent). `wayfinder` is green-field: standing up its labels is safe and reversible, but the skill is welded to infra that does not exist here and wields an irreversible tracker-delete, so it stays parked pending a charter consult and a real build. The biggest risks are overcorrection (a mechanical "adopt every upstream name/label" pass would regress consent gates, HITL routing, the corrected `needs-triage` default, and bundled-name safety, and would re-collapse deliberately-split lanes), the wayfinder irreversible-delete tool, the ADR-format single-source dual-copy hazard, and a self-contradicting `triage` description once the external-PR fold lands. Proceed — but to the charter consult first; this inventory validates nothing on its own.

## 3. Per-skill migration table and narrative

The family is the four rename/merge mappings the roadmap named plus the collateral skills the taxonomy shift touches. "Verdict" is stated upstream-first (what happens to the base) with the deviations named.

| Fork (`/Users/jp/.agents/...`) | Upstream @ d574778 (`.../mattpocock-skills/...`) | Verdict | Confidence |
|---|---|---|---|
| `skills/to-issues/SKILL.md` | `skills/engineering/to-tickets/SKILL.md` (+ `docs/engineering/to-tickets.md`) | hybrid-rebase — keep fork as governing base, fold 4 upstream gains | high |
| `skills/to-prd/SKILL.md` | `skills/engineering/to-spec/SKILL.md` | hybrid-rebase — upstream body as base, re-derive label + consent layer, re-absorb one dropped sentence | high |
| `skills/diagnose/SKILL.md` | `skills/engineering/diagnosing-bugs/SKILL.md` | hybrid-rebase — diagnose stays base (richer body), fold the Phase-1 completion gate | high |
| `skills/grill-with-docs/` (+ `ADR-FORMAT.md`, `CONTEXT-FORMAT.md`) | `skills/engineering/grill-with-docs/SKILL.md` (7-line stub) + `skills/engineering/domain-modeling/` | keep-fork — reject domain-modeling; upstream stub is a component, not a successor | high |
| `skills/implementation-planning/SKILL.md` | (no equivalent; to-tickets is the merge of to-plan+to-issues) | keep-fork — house-native lane, not subsumed by the merge | high |
| `skills/decision-record/SKILL.md` (+ shared `grill-with-docs/ADR-FORMAT.md`) | (no equivalent; ADR format lives under upstream `domain-modeling/`) | conditional hybrid-rebase → resolves to keep-fork because domain-modeling is not adopted | high |
| `skills/triage/` (`SKILL.md`, `AGENT-BRIEF.md`, `OUT-OF-SCOPE.md`) | `skills/engineering/triage/` (same three files) | adopt-upstream-base — upstream is a strict superset + methodological upgrade | high |

### 3.1 to-issues -> to-tickets (hybrid-rebase, high)

The task's headline premise — that to-tickets absorbs `implementation-planning`'s territory — does not survive reading the files, and it splits into two clean decisions. Upstream merged the old `to-plan` and `to-issues` into a single `to-tickets`; both parents were tracer-bullet vertical-slice tracker publishers, and JP already forked that slicing capability as `to-issues`. So the to-tickets merge collides on job with `to-issues`, not with `implementation-planning`.

Verdict is hybrid-rebase, not adopt-upstream-base, because the house layer that must win here is real and partly environment-forced. The load-bearing deviation is HITL/AFK routing: `to-issues/SKILL.md:32,59` splits human-in-the-loop from AFK slices and routes HITL slices to `ready-for-human` so an autonomous agent cannot grab a slice it cannot do. The verifier corrected the reader's framing of the upstream hazard — `to-tickets/SKILL.md:63` does not "blanket-stamp every ticket"; it defaults to `ready-for-agent` "unless instructed otherwise" and simply has no HITL/AFK distinction at all (it assumes every ticket is agent-grabbable by construction). The load-bearing defect is the missing HITL routing, not an unconditional stamp; the safety verdict (keep the fork's HITL mapping, it overrides upstream) is unchanged. The verifier also corrected two other reader claims: the labels are the five canonical triage roles (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`) mapped to tracker label strings, not "four raw live labels" — `ready-for-human` is the confirmed load-bearing one; and the vertical-slice rules are near-identical, not identical (to-tickets adds the context-window sizing and prefactoring lines the fork later folds), so the summary must not contradict its own gain inventory.

House layer to re-derive on the fork base: the Use-when/Do-not-use routing description kept model-invocable (do NOT copy to-tickets' `disable-model-invocation`; the verifier confirmed this is deliberate house convention by checking the sibling `to-prd` frontmatter); the HITL/AFK + `ready-for-human` mapping with the no-mislabel safety note; the consent gate and Side Effects And Proof Boundary section (`to-issues:12-16`); dual-runtime tokens; the `issues` terminology and skill name (renaming to `to-tickets` would collide with the existing skill and mismatch the GitHub-Issues tracker); and a wide-refactor recognize-and-route seam, not the machinery.

Upstream gains to fold (four of five): the wide-refactor RECOGNITION that a rename-the-world change cannot land green as one vertical slice (`to-tickets:40`) — folded as a recognize-and-route seam pointing at `migration-campaign` / `contract-change-propagation` / `migration-safety`, which already own the expand-contract machinery, never the machinery itself; the prefactoring line (`to-tickets:23`); single-fresh-context slice sizing (`to-tickets:33`); and, at medium confidence, native GitHub sub-issue / issue-dependency publication in place of text-only `Blocked by` (the live tracker supports both). The fifth, a local-files `tickets.md` no-tracker mode, is optional and low priority — adopting it purely because upstream has it would be over-adoption.

Reopened merit call: the 2026-07-05 disposition restored the to-issues fork wholesale (discarding the reinstall); the 2026-07-08 roadmap folded only the wide-refactor seam. The rebase lens sharpens rather than overturns both — the environment facts (live `ready-for-human`, native sub-issues) are decisive and One-Owner forbids importing the machinery — but it widens the fold from one seam to four small gains. The task's premise that to-tickets subsumes `implementation-planning` is the one prior framing this re-read overturns as false.

### 3.2 to-prd -> to-spec (hybrid-rebase, high)

`to-prd` is a confirmed fork of upstream `to-spec`: the entire artifact template and the process skeleton are shared verbatim (the prototype-snippet exception is byte-identical between fork `63-65` and upstream `55-57`), so adopting the upstream body as the base is essentially free — the rebase output is `SKILL.md`-only, since neither side carries references or companion metadata. Verdict is hybrid-rebase.

Take the upstream body verbatim as base (template, process, no-interview premise, prototype exception, and the seam-minimization sentence). Then re-derive two house overrides that legitimately win against upstream. First, label handling: publish the PRD as `needs-triage` and explicitly refuse `ready-for-agent` (`to-prd:26`). This is grounded in an environment fact — `triage-labels.md:9` defines `ready-for-agent` as "fully specified, ready for an AFK agent," and `to-issues:59` is where that label legitimately lands, on AFK vertical slices, not a raw un-sliced PRD. Upstream's `ready-for-agent`-on-a-raw-spec (`to-spec:19`, "no need for additional triage") would be a genuine tracker defect here: an autonomous agent could grab an un-sliced PRD and implement it wholesale, bypassing slicing. Second, the consent gate and Side Effects/Proof-Boundary section — settled house convention identical in shape to `to-issues:12-16`.

The one clear place the fork is behind upstream: it dropped the seam-minimization sentence "The fewer seams across the codebase, the better - the ideal number is one" (`to-spec:15`, absent at `to-prd:22`). Pure testing guidance with no roster/label/safety entanglement, so default upstream-wins; it re-absorbs for free with the base body. This is the one fork divergence classified sunk-cost-staleness, and the reader correctly recommends TAKE-UPSTREAM rather than preserving it — the anti-sunk-cost move.

The `disable-model-invocation` drop stays an explicit open question (upstream gates this publish skill manual-only; JP's convention keeps it model-routable). The verifier noted a reinforcing point: the consent gate makes the model-routable choice safe regardless, since nothing publishes without approval — but the reader still correctly left it open, and this document keeps it open rather than silently settling it. The branded companion doc `to-spec.md` (aihero.dev links, ask-matt, npx quickstart) is correctly not carried; the `setup-matt-pocock-skills` reference resolves to a real retained roster skill and is not stale branding.

### 3.3 diagnose -> diagnosing-bugs (hybrid-rebase, high) — the genuine trade

This is the one area the task flagged as a genuine trade, not an auto-downgrade, and the git history settles it cleanly. `diagnose` was forked from the original upstream (commit `221ffca`, 2026-06-12); its Phase 1 is byte-identical to that pre-improvement text. Upstream then improved in `bc4cf90` (2026-06-17), adding two things in one commit: the Phase-2 repro-minimise step and a new Phase-1 "Completion criterion — a tight loop that goes red" gate. On 2026-07-05 JP folded only minimise, leaving the completion criterion behind. So at `d574778` that gate (upstream `51-60`) is a genuine un-absorbed upstream gain (grep-confirmed 0 matches for `red-capable`/`completion criterion`/`already run at least once`/`paste the invocation` in `diagnose`).

Against that, `diagnose`'s body is genuinely richer than upstream and those are principled house improvements with no upstream counterpart: the 7-row non-determinism cause-class table (`43-59`), the perf work-bound/resource-bound USE-method lens (`111-118`), the three-failed-fixes "architecture is the hypothesis now" escalation (`138`), and fix-at-origin guidance (`136`). Adopt-upstream-base would silently delete all four — the overcorrection failure this area exists to guard against. So `diagnose` remains the base; fold in only the Phase-1 gate.

The verifier tightened three things. First, the reader's "strictly richer on Phases 1/4/5/6" is imprecise for Phase 1: `diagnose` adds the cause-class table but LACKS the completion gate (that is the fold target), so it is richer on Phase-1 cause-classification and on Phases 4/5/6, not strictly superior on Phase 1. Second — the most important correction — the 2026-07-05-intent question must be a genuinely-open confirm-before-building gate, not the reader's lean. The ledger's own wording, "the one genuinely-additive discipline of diagnosing-bugs — the Phase-2 repro-minimise step," is live counter-evidence that the completion criterion may have been surveyed and judged non-additive (because `diagnose` already carries loop-quality prose), not merely out of scope. Do not let "I lean not evaluated" harden into an assumption that re-derivation is a clean gap-fill; confirm with the user whether the criterion was declined or unexamined before building. Third, on re-derivation, MERGE with `diagnose`'s existing partial substance rather than bolt on a duplicate: `diagnose` already has `l.38` "assert on the specific symptom" and `l.73` "the failure mode the user described." The genuinely net-new pieces are the pasted-evidence proof ("name one command you have already run at least once (paste the invocation and its output)") and the anti-theorising stop-guard ("the exact failure this skill prevents. No red-capable command, no Phase 2"); fold those and absorb the weak `l.65` soft gate, not the loop-quality checklist that already exists. Append a ledger amendment referencing the 2026-07-05 entry so the two do not silently disagree. (Note: line counts are cosmetically off by one — `diagnose` is 152 newlines / 153 content lines; upstream is 134 — immaterial.)

### 3.4 grill-with-docs -> domain-modeling (keep-fork, high) — not a rename

The roadmap's "grill-with-docs -> domain-modeling" mapping is not a rename, and upstream still shipping `grill-with-docs` proves it. Upstream `d574778` uses a four-skill decomposition: `productivity/grilling` (the model-invoked interview engine, 12 lines), `productivity/grill-me` (a 7-line user-facing pointer), `engineering/grill-with-docs` (a 7-line composition stub, `disable-model-invocation: true`, body "Run a /grilling session, using the /domain-modeling skill."), and `engineering/domain-modeling` (74 lines, the extracted glossary/ADR discipline, upstream's declared single-source-of-truth). So `domain-modeling` is a component/sibling of `grill-with-docs`, not its successor.

JP's `grill-with-docs` (104 lines, model-routable) is a self-contained monolith that inlines both the grilling engine and the entire domain-modeling discipline, keeping its own `ADR-FORMAT.md` and `CONTEXT-FORMAT.md` in-directory. Byte-diffs settle staleness decisively: `CONTEXT-FORMAT.md` is identical to upstream's; `ADR-FORMAT.md` is a strict superset (adds the `Revisit when` optional section at line 24); the inlined "During the session" discipline block is byte-identical to current `domain-modeling` except for one added paragraph binding recommendations to a code-check (`SKILL.md:80`). So the inlined upstream content carries no staleness against `d574778`. On top of that sync, the fork adds a substantial house layer upstream has nowhere: an assent-detection guard (`:16`), a capture-is-not-grilling route-out to `decision-record` (`:10`), fenced-code recommended answers (`:12`), an orient-on-existing-docs opening (`:8`), a closeout weakest-assumption + un-offered-ADR scan + `implementation-planning` handoff (`:20`), and a "Writing to the repo" git-write-safety + no-auto-commit + proof-boundary block (`:98-100`).

Verdict keep-fork. The only re-openable question is structural — adopt upstream's four-skill decomposition, or keep JP's three-skill house decomposition (`grill-me` engine, `grill-with-docs` composite, `decision-record` standalone ADR)? Keep the fork: JP's roster has no `domain-modeling` and no `grilling`, and adding them injects intra-roster routing collisions (`domain-modeling` vs `decision-record` both own "record an ADR"; `domain-modeling` vs `grill-with-docs` both own "sharpen the glossary"; `grilling` vs `grill-me` are the same engine), `decision-record` carries an ADR-supersession genealogy `domain-modeling` lacks, and the inlined discipline is byte-current so keeping the monolith preserves no staleness. This matches the ledgered 2026-07-05 disposition (reject `domain-modeling`, reject `grilling` as colliding with `grill-me`), so the upstream-as-base re-read confirms rather than reverses the prior call. The one conscious upstream virtue being declined, stated so this is not overcorrection-blind: a standalone `domain-modeling` module would let `improve-codebase-architecture` reach into a discipline rather than into a composite skill — a minor seam-cleanliness loss judged not worth two roster-colliding skills.

Verifier corrections: two off-by-one line counts (fork 104 not 105; upstream grilling 12 not 13, cosmetic); and add to the ambiguous / decisions-are-the-human's bucket a second dropped upstream clause alongside the flagged "the decisions are mine" — upstream grilling's confirmation gate "Do not enact the plan until I confirm we have reached a shared understanding" (`grilling:12`) is also absent from the fork, likely moot because `grill-with-docs` closes by handing to `implementation-planning` rather than enacting. keep-fork survives unchanged.

Standing sunk-cost guard for keep-fork: because the inlined discipline is a copy of upstream's SSOT, any future pin bump must re-sync the "During the session" block against fresh `domain-modeling/SKILL.md`, or the monolith can silently rot. A naive rebase must also not overwrite the fork's `ADR-FORMAT.md` (dropping `Revisit when`) or relocate the format files (breaking the consumers below).

### 3.5 implementation-planning (keep-fork, high) — survives the to-tickets merge

`implementation-planning` survives fully; it is a still-distinct job, neither subsumed nor materially overlapping with `to-tickets`. The two are diametrically opposed on the path-detail axis: `implementation-planning:23` mandates "Exact file paths always. Complete code in every code step," while `to-tickets:111` deliberately forbids the same ("avoid specific file paths or code snippets — they go stale fast"). `to-issues:72` carries that path-free line essentially verbatim (the verifier corrected the reader's "identical"/"verbatim" — fork line begins "Avoid ..." sentence-initial vs upstream mid-sentence "In either form, avoid ..."; the load-bearing phrase is verbatim), confirming the merge maps onto `to-issues`, not `implementation-planning`.

`implementation-planning` is house-native (admitted commit `966f903` from the superpowers plugin per ledger line 7, no upstream/archived `to-plan` ancestor — the verifier confirmed no `to-plan` exists in `d574778` or `skills-archive/`, so the lineage claim rests correctly on the path-free/path-exact behavioral contrast plus roadmap-stated lineage), already fully house-transformed, and produces a deliverable upstream never had: a single path-exact, complete-code executable plan for a zero-context executor, plus the Outside-View reference-class / planning-fallacy debias (`:37-46`) for which the verifier grep-confirmed upstream `to-spec` has no analog. The merge requires zero rewiring of it; `:13` already delegates slicing to `to-issues`.

The only genuine seam is the executor hand-off: upstream `implement/SKILL.md:15` says "Commit your work to the current branch," a house-hazard against JP's protected-branch floor. But `implementation-planning` hands off to `execute-plan`, which refuses to start on a protected branch without consent (`execute-plan:12`), so the downstream lane is already protected. Do not adopt `implement`'s commit-to-current-branch rule into any executor lane.

Verifier corrections: tighten the wide-refactor donor citation from `to-tickets:39-46` to `to-tickets:40` (single paragraph); and state precisely that roadmap FOLD #7 folds only a recognition-and-route seam into `to-issues` (routing to `migration-campaign` / `contract-change-propagation`), NOT the expand-contract machinery — avoid any phrasing implying `to-issues` absorbs the machinery. Name-trap to guard against: because to-tickets absorbed a skill literally named to-PLAN, a reader could wrongly assume the absorbed planning artifact is JP's implementation-PLANNING; it is not — old-upstream `to-plan` was a path-free tracker-slicing planner (an ancestor of `to-issues`), not of the path-exact `implementation-planning`.

### 3.6 decision-record + the shared ADR format (conditional hybrid-rebase → keep-fork, high)

The taxonomy shift is a home relocation of the ADR/CONTEXT format files, not a content change. In upstream `d574778` the format files live under the new standalone `domain-modeling/`; in JP's fork they still live where an older upstream put them — `skills/grill-with-docs/` — because JP inherited that location at import (`5e52b7c`) and upstream migrated away afterward. JP's `ADR-FORMAT.md` is byte-identical to upstream's plus one added line (`Revisit when`, house commit `b68b952`); `CONTEXT-FORMAT.md` is byte-identical. So there is no richer first-party content in the format FILES themselves.

`decision-record` does not fork the format; it single-sources it. The verifier corrected the reader's repeated "hard link" term: `decision-record/` contains only `SKILL.md`, and `:24` references the format via a backticked relative-path in prose, not an inode hardlink — a fragile TEXT reference. This does not weaken the dangling-reference risk (it strengthens it: a text reference does not survive a move). Three JP consumers point at the grill-with-docs copy — `decision-record:24`, `improve-codebase-architecture:85,87`, and `grill-with-docs:102` (its self-registration note). The verifier's other correction: JP's `triage` is NOT among the format consumers. The upstream fact that `domain-modeling.md:40` is reached by upstream's `triage`/`improve-codebase-architecture` must not become a JP rewire target; the JP rewire set is exactly the three above.

The reader's merit verdict is a CONDITIONAL hybrid-rebase, and this is where the two readers of the domain-modeling question meet head-on. IF `domain-modeling` were adopted, the format's principled home is `domain-modeling/` (upstream-wins), the `Revisit when` line and the single-source registration note re-derive onto `domain-modeling/SKILL.md`, all three consumers rewire atomically, and the old copies are deleted to avoid a dual-source. But `domain-modeling` is NOT being adopted (§3.4 keep-fork prevails, and the ledger reaffirm-rejects it), so the condition is not met and the verdict resolves to the fork-native fallback the verifier named: keep `grill-with-docs` as the format home unchanged, and `decision-record` needs no rewire. This is the honest reconciliation of the §3.4/§3.6 reader tension — surfaced, not smoothed: the §3.6 reader itself scoped the `domain-modeling`-SKILL admission as "another reader's call," and §3.4 answers it reject.

The load-bearing risk this area exists to name is the dual-copy / silent-disagreement hazard: if `domain-modeling` were ever pulled in carrying its own `ADR-FORMAT.md` while `grill-with-docs` kept its copy, JP would have two `ADR-FORMAT.md` files that can silently diverge (the fork's has `Revisit when`, upstream's does not) — the exact failure `decision-record` exists to prevent, now afflicting its own format source. Any future home-move reopens the ledgered 2026-07-05 decision and needs a new append-only entry; adopting `domain-modeling` is itself a gated charter event.

### 3.7 triage -> triage (adopt-upstream-base, high)

Upstream `d574778` is a strict superset of JP's fork body plus one methodological upgrade; the fork predates upstream's PR + Verify + redundancy additions and carries the standard house transform on top, plus one content regression. Every fork divergence falls into three buckets: the re-appliable house transform (Use-when routing description; dual-runtime tokens; roster rewire of the grill step; a mutation-approval guard and Closeout/proof-boundary section; `trash`-not-`rm`; de-branded prose); staleness (the fork lacks the PR-as-request-surface machinery, the "Verify the claim" generalization of "Reproduce", and the redundancy/already-implemented-wontfix tree); and one regression (`OUT-OF-SCOPE.md` collapsed a fenced TS code block onto one line, violating the house "leave fenced code as written" rule). No fork divergence is a bespoke non-transform improvement upstream lacks. The label vocabulary is a verified environment fact: the FIVE canonical triage roles (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`) map 1:1 to live labels on `jpsweeney97/agents` — the four custom triage labels plus `wontfix` reused from a GitHub default, consistent with §3.1 and the §5 census (13 live labels = 9 GitHub-origin defaults + 4 custom triage, `wontfix` being the reused default). An earlier draft of this section miscounted this as "7 roles"; there is no 7-role vocabulary, and the correction matters because this environment fact is the stated proof for the adopt-upstream-base verdict — the most aggressive call in the inventory — so the count must not undercut the evidence it rests on. Verdict: take upstream body as base, re-derive the house transform.

The verifier caught a real reader error and corrected the invocation-posture framing. The error: the reader claimed upstream's `/grilling` reference (`triage SKILL.md:76`) is "non-existent"/"stale" because there is "no grilling dir" — false; `/grilling` EXISTS at `skills/productivity/grilling`. The reader only inspected `skills/engineering/`. Drop the "bonus fixes an upstream-stale /grilling reference" justification and the supporting evidence bullet. The grill rewire recommendation itself still holds for a different, valid reason: JP's roster genuinely lacks both `grilling` and `domain-modeling`, and JP's `grill-with-docs` is a self-contained skill that folds both, so triage's grill step should point at `/grill-with-docs` (or `$grill-with-docs`). It is a fold of the pair into one skill, not an upstream "rename." The reframing: the `disable-model-invocation` drop is a deliberate, systematic house posture (verified — `grill-with-docs` also dropped it for a Use-when routing description), not "ambiguous / unclear if intentional." KEEP the charter carve-out flag anyway: `triage` mutates a live tracker (comments, closes), so a model-invocable + irreversible-effect skill plausibly trips the "fires unattended + irreversible tools" carve-out; consult the charter before landing.

Adopt from upstream: "Verify the claim" (`:74`, generalizing bug-only "Reproduce" to also cover PRs and reframing the step as claim-verification — better even ignoring PRs); the redundancy check + already-implemented-wontfix tree (`SKILL.md:70,82-85`; `OUT-OF-SCOPE.md:86,88`, whose already-implemented warning prevents poisoning the `.out-of-scope` dedup KB with false rejections); the config-gated PR-as-request-surface machinery (inert unless the tracker config enables the PR surface); and the AGENT-BRIEF PR framing/example. Fix the fork regression by taking upstream's properly-fenced TS block (`OUT-OF-SCOPE.md:42-47`). Re-apply on top: the routing description citing `to-prd`/`to-issues`; dual-runtime tokens; the mutation-approval guard; the Closeout section; `trash`-not-`rm` overriding upstream's "Delete"; and de-branded prose keeping the deliberate skill NAME `setup-matt-pocock-skills`. Keep the five-role live-label mapping unchanged (the five canonical roles of §3.1/§5, not seven). Separately flag whether the byte-identical branded template `skills-claude/setup-matt-pocock-skills/triage-labels.md` should also be de-branded, and whether to enable the PR surface config (both touch the setup skill, §4).

## 4. Tracker / setup infra adoption plan

The fork `skills-claude/setup-matt-pocock-skills/` is a stale snapshot of an older upstream plus a partial house transform: its Conventions blocks and the whole `triage-labels.md` / `domain.md` / `local.md` bodies are byte-identical or near-identical to current upstream, which proves it is upstream-derived, not independently authored. Verdict: adopt-upstream-base, re-derive the house transform, extend the de-brand to the seed templates, gate the wayfinding blocks on the wayfinder decision. This is third-party contract material that ships contract text into consumer repos — a gated charter event (see §7).

Upstream-only gains — larger than the prior finding's undercount. The prior claim ("only two blocks in `issue-tracker-github.md` plus PR-flag SKILL.md plumbing") is confirmed-but-incomplete. The rebase must pull the PR/MR + Wayfinding surface across ALL THREE tracker templates:
- `issue-tracker-github.md`: `## Pull requests as a triage surface` (upstream 16-26) + `## Wayfinding operations` (36-45; the reader's "36-47" is off by ~2, whole section captured).
- `issue-tracker-gitlab.md`: `## Merge requests as a triage surface` (17-27) + `## Wayfinding operations` (37-46).
- `issue-tracker-local.md`: `## Wayfinding operations` (21-30).
- `SKILL.md`: the PR-flag plumbing — Section A follow-up question (47-51) plus the block-template clause (line 104, "plus whether external PRs are a triage surface").

House layer to re-derive on fresh upstream:
- Roster rewire: `to-tickets`->`to-issues`, `to-spec`->`to-prd`, `diagnosing-bugs`->`diagnose`, `domain-modeling`->`grill-with-docs`; keep `qa` DROPPED. Upstream's own SKILL.md Section-A explainer (line 38) is internally stale — it still names `qa` even though `qa` is deprecated in upstream's own `skills/deprecated/` — so re-deriving off fresh upstream risks silently reintroducing it. Keep it out.
- Re-apply the `domain.md` grill-with-docs rewire (lines 11, 45): upstream's `domain.md` names `/domain-modeling` as producer, a skill JP lacks; taking it verbatim would point JP's repos at a nonexistent skill. JP's own live `docs/agents/domain.md` already names grill-with-docs as producer.
- Extend the de-brand to the SEED templates, not just the live instances. This is the incomplete-transform finding: `skills-claude/setup-matt-pocock-skills/triage-labels.md` is byte-identical to upstream (`Label in mattpocock/skills` header, `AFK-ready triage label` example, `Edit the right-hand column` note) while the live `docs/agents/triage-labels.md` was de-branded to `Canonical role`. So re-running setup in a fresh repo currently re-emits branded output. De-brand the template to `Canonical role` to match.
- House Use-when/Do-not-use routing description (roster-aware) and dual-runtime tokens where they belong — the verifier corrected the reader's "throughout the bodies": the `$`-tokens live only in `domain.md` (`$grill-with-docs`) and `agents/openai.yaml` (`$setup-matt-pocock-skills`); the github/gitlab/local/triage-labels templates carry no skill-reference tokens and are otherwise byte-identical to upstream. Keep the `agents/openai.yaml` Codex companion (aligned to `disable-model-invocation: true`) or consciously drop it as vestigial for a Claude-only skill. Both sides keep `disable-model-invocation: true`.

PR/MR-triage plumbing: adopt with default `no` — matches JP's private personal repo (external PRs are not a request surface for `.agents`) and lets the global skill configure it per-repo in OSS repos. The three live `docs/agents/*.md` are downstream instances carrying environment facts; rebase targets the seed templates, not the live instances, which stay as-is for this private repo.

Wayfinding-operations blocks: gate on the separate wayfinder-adoption decision. Verified here: no `/wayfinder` skill in `skills/` or `skills-claude/`, and no `wayfinder:*` labels on `jpsweeney97/agents`, so those blocks are dead references to a nonexistent skill and label vocabulary unless wayfinder is co-adopted. Strip them if wayfinder is not adopted.

Identity de-brand is a flag-for-user: the skill name `setup-matt-pocock-skills`, the H1 "Setup Matt Pocock's Skills," and the `openai.yaml` display_name retain the brand. Stripping mattpocock from template CONTENT is unambiguous; the skill's own identity is a deliberate retained choice — do not strip it silently.

## 5. wayfinder:* label safety verdict

No `wayfinder` fork exists in JP's repo (absent from `skills/` and `skills-claude/`; the sole reference is `docs/plans/2026-07-08-mattpocock-skills-extraction-roadmap.md`), so this is a green-field adoption question, `merit_verdict = not-applicable`.

Safety verdict: standing up `wayfinder:*` labels is SAFE and fully REVERSIBLE, and labels are the trivially-safe part of adoption — the load-bearing mechanisms are native and already present.

What wayfinder needs (from `SKILL.md`): a `wayfinder:map` label to mark the single map issue (`:21`); a `wayfinder:<type>` label on every ticket, one of research/prototype/grilling/task (`:65`); child issues as native GitHub sub-issues (`:21,57`); native issue-dependencies for blocking, whose fallback when native blocking is absent is a body convention, NOT labels (`:69`); and assignee as the claim (`:67-68`).

Are the labels a fallback / are they even needed? No, and yes. The `wayfinder:*` labels encode IDENTITY (which issue is the map) and TYPE (which changes how a ticket resolves) — orthogonal to the sub-issue/dependency graph, which encodes STRUCTURE. The only thing conditional on native availability is BLOCKING, and its degraded fallback is a body convention, not labels. Because `jpsweeney97/agents` has both native sub-issues and native issue-dependencies, wayfinder's canonical, non-fallback mechanism works out of the box, and the labels remain required by the contract regardless. The verifier independently confirmed availability (rather than relying on the task's stipulation): the GitHub GraphQL Issue type exposes `blockedBy` + `issueDependenciesSummary` (native dependencies) and `subIssues` + `parent` (native sub-issues) on this account.

Safety properties: creating the labels is non-destructive (adds labels, touches no issue), reversible (`gh label delete` un-applies; with zero issues carrying them today, rollback is a clean no-op), namespaced (`wayfinder:` collides with none of the live labels), and cheap. The genuinely load-bearing, harder-to-unwind mechanisms are native and already available, so nothing risky needs provisioning.

Minimal reversible action: create NONE now — DEFER. This is a read-only inventory and wayfinder is not adopted. At adoption, create exactly 5 `wayfinder:*` labels, best done lazily and idempotently on first map creation (`gh label create <name> --force`) so no orphan labels exist until a real map does. Labels are not the adoption blocker; the real work is authoring the GitHub Wayfinding-operations doc that wires sub-issues, dependencies, the frontier query, and these labels (§4, §6).

Verifier corrections folded in: the live-label breakdown is 9 GitHub-origin defaults + 4 custom triage = 13 (with `wontfix` a GitHub default that `triage-labels.md` reuses as a canonical role) — zero `wayfinder:*` collision either way; and `to-tickets` is referenced at `wayfinder.md:21`, not `:44` (line 44 carries `ask-matt` and `to-spec`).

## 6. wayfinder build plan + dependency rewiring map

There is no JP fork of wayfinder to diff (it was PARKED, never adopted), so current upstream `d574778` IS the base and the entire house layer is a fresh re-appliable transform. Verdict: adopt-upstream-base IF built — take upstream's fog-of-war contract intact and apply only the standard house transform; do not redesign the contract. But the PARK-vs-build decision is JP's, and the recommendation is to stay parked pending a charter consult and a real dependency-authoring cost.

Wayfinder is genuinely ownerless at its altitude: multi-session, tracker-backed, fog-of-war investigation planning with a deliberately-incomplete map, plan-don't-do discipline, and one-ticket-per-session containment — no JP incumbent covers it (`outcome-shaping`/`design-exploration` are single-session and read-only; `implementation-planning`/`to-issues` need a settled input, and `implementation-planning`'s completeness-first Outside-View Pass directly contradicts wayfinder's fog model). Two hard build costs dominate: the physical tracker mechanism wayfinder delegates to — the setup skill's "Wayfinding operations" section — is grep-confirmed ABSENT from JP's kept setup fork and must be authored; and wayfinder wields irreversible tracker tools (create/close/assign/DELETE issues, `SKILL.md:125` "update or delete those tickets"), making it a charter case-(d) event.

Dependency rewiring map. The SKILL.md contract names only FOUR skill references; the rest live only in the un-adopted human-facing docs page (`docs/engineering/wayfinder.md`).

| Upstream reference | Where | JP roster equivalent | Notes / flags |
|---|---|---|---|
| `/grilling` + `/domain-modeling` (paired, 3 sites: SKILL.md:79,111,123) | contract | `grill-with-docs` (primary) | Collapse the pair into one invocation; `grill-me` (doc-free) and `decision-record` (standalone ADR) as fallbacks. JP lacks both `grilling` and `domain-modeling`; grill-with-docs folds both. |
| `/prototype` (SKILL.md:78) | contract | `prototype` | Clean 1:1; name already matches; dual-runtime token. |
| `/setup-matt-pocock-skills` (SKILL.md:25) | contract | `setup-matt-pocock-skills` (skills-claude/) | Skill exists by name, but the "Wayfinding operations" mechanism it must lay down is absent — must be authored (single largest build cost). |
| `/research` (as skill) + Research ticket-type (wayfinder.md:44; SKILL.md:77 has no slash-ref) | docs-only | `deep-research` (search + verify) + `research-capture` (durable per-claim doc) | Contract needs no rewire (SKILL.md:77 says only "creates a markdown summary"); only the docs page does. |
| `/to-spec` (wayfinder.md:21,44) | docs-only | `to-prd` | Docs-page only. |
| `/to-tickets` (wayfinder.md:21) | docs-only | `to-issues` | Docs-page only. |
| `/ask-matt` (wayfinder.md:44) | docs-only | NO 1:1 equivalent | Nearest is `work-router` (dynamic roster router — the reason ask-matt was rejected). Appears only in the docs page's "when unsure which skill fits" line, never in the contract. DROP or rewrite to work-router — flagged open question. |
| `wayfinder:map` / `wayfinder:<type>` labels (SKILL.md:21,65) | contract | none exist | Create at build (§5); distinct namespace from the 5 triage labels. |
| native blocking / sub-issues (SKILL.md:69) | contract | native sub-issues + issue-dependencies | Both available on `jpsweeney97/agents` (verifier-confirmed via GraphQL: `/sub_issues` and `/dependencies/blocked_by` return `[]`, not 404) — canonical non-fallback path works. |

House layer to re-derive if ever built: the grill rewire (3 sites) into one `grill-with-docs` call; author the missing Wayfinding-operations mechanism (port upstream setup seed `issue-tracker-github.md:36-45` — sub-issues + native dependencies via `gh api`); dual-runtime tokens; create the `wayfinder:*` labels; de-brand the SKILL prose (strip "matt" at `:25`) while resolving the setup-skill naming question; add an irreversible-delete guard (a `trash`-not-`rm` analogue) around `:125` preferring close-with-invalidated-reason over delete, matching wayfinder's own out-of-scope close pattern at `:101`; author a Use-when/Do-not-use boundary separating wayfinder from `implementation-planning` (completeness-first, contradicts the fog model), `to-issues`, `outcome-shaping`, `design-exploration`; and decide the fate of the branded docs page.

Placement: skills-claude/ (Claude-only). Rationale strengthened by the verifier: wayfinder's hard dependency `setup-matt-pocock-skills` is Claude-only and its Research ticket-type leans on `deep-research` (a bundled Claude plugin, not in `skills/`); and `disable-model-invocation` is Claude-only frontmatter with no Codex effect, so in `skills/` (dual-runtime) the user-invoked-only gate would silently not apply on the Codex arm. Preserve `disable-model-invocation: true` (SKILL.md:4) — wayfinder is user-invoked only, so it does not "fire unattended"; the gating trigger is the irreversible-tools clause, not autonomy.

## 7. Charter and ledger accounting

Governing rule: skills are build-and-prune and not charter events, except the named gated triggers — authoring/retiring an always-loaded contract, authoring a skill that fires unattended or wields irreversible-effect tools, installing contract-shipping material, or deciding the fate of third-party contract material. Every recommendation here that lands third-party donor text or admits/rebases a forked third-party skill is charter case-(d) (fate of third-party material) and requires a charter consult plus one append-only `docs/agents/contract-decisions.md` entry AT LANDING. This inventory recommends only; nothing lands here.

Gated case-(d) decisions, by class:
- Rebase of an existing forked third-party skill onto `d574778` (charter consult + one ledger entry each, recording rebase-onto-upstream-base with the house transform re-derived): `to-prd`, `to-issues`, `triage`, and the `setup-matt-pocock-skills` infra (the last also "installing contract-shipping material," since it authors an `## Agent skills` block into consumer repos and writes `docs/agents/*.md`).
- Donor-text fold into an existing fork (one-line ledger note each): the `diagnose` Phase-1 completion-criterion fold; the `to-prd` seam-sentence fold; the `to-issues` wide-refactor recognize-and-route seam + prefactoring + slice-sizing + native-edge folds; and the roadmap-adjacent `save-handoff` redact-secrets/PII fold into the `handoff` plugin (raised in §8/§9 for the seam and sequencing picture) — a plugin-path case-(d) that lands via the Class-B plugin path (version bump + Codex republish + mirror), not an in-place `skills/` edit, and is scoped in here rather than left unaccounted.
- Whole-skill third-party admission (full ledger entry): `wayfinder` IF ever built; and the roadmap-adjacent `resolving-merge-conflicts` IF admitted — the roadmap's single whole-skill admit (§8/§9) and the heaviest charter event named in this inventory (a case-(d) full ledger entry plus a fresh build of a new `plugins/git-cycle/skills/<house-name>/` sibling), also landing via the Class-B plugin path. Both sit adjacent to the core mattpocock forks but are scoped in with full charter accounting here, so no whole-skill admit is left with an unstated charter gate.
- Not a charter/ledger event: keeping `implementation-planning` (house-native, untouched), keeping `grill-with-docs`/`decision-record` (no third-party material admitted/folded/retired), rejecting `domain-modeling`/`grilling`/`code-review` (rejections of un-adopted material — the roadmap already records the disposition), and creating `wayfinder:*` labels (a reversible GitHub-state change, not a contract).

Borderline (leave the ledger-note-vs-no-ledger call to landing): the `triage` external-PR and redundancy folds and the `diagnose` fold land into forks whose third-party adoption was already ledgered on 2026-07-05, so a defensible reading is ordinary build-and-prune; the conservative reading is a one-line note. The verifier's clarifying point on `to-issues`: it was also among the 8 forks restored on 2026-07-05 (not only `tdd`/`triage`), so the case-(d) note at landing should record that the 07-05 event preserved the pre-existing fork rather than admitting new to-tickets text.

Ledger entries superseded vs appended: the 2026-07-05 entry (line 36) is NEVER rewritten — the ledger is append-only (charter.md:64-66), and "supersede" means a newly-appended entry that names what it reverses, on the precedent of the jp-writing-style retirement superseding its own admission (contract-decisions.md:24). A load-bearing distinction the dedicated charter read fixed: the 2026-07-05 disposition is a *ledgered* decision, but the 2026-07-08 roadmap's wayfinder-PARK and setup-REJECT were *never ledgered* — the last ledgered decision is the 2026-07-06 markdown-reader routing entry (grep-verified). So the roadmap's park/reject are un-landed recommendations, not settled charter facts: acting against them is a fresh decision, the "do not re-litigate settled parks" rule (charter.md:46) does not bind them, and if this re-adoption keeps the wayfinder park it finally *ledgers* it with a concrete reopen trigger (rather than leaving a live decision unrecorded). This inventory REOPENS parts of the 2026-07-05 entry (the to-issues restore-wholesale disposition; the format-home do-not-relocate-in-v1 reasoning; the diagnose fold that took minimise but not the completion criterion). Crucially, that 2026-07-05 "restore" was a wholesale discard-the-reinstall that preserved each fork body VERBATIM and never rebased its non-house content onto fresh upstream — so a fork-vs-upstream diff today cannot separate a deliberate house edit from accumulated staleness (the exact gap this rebase exists to close). Where a rebase concludes adopt-upstream-base for a fork, the appended entry supersedes-in-part the 07-05 restore of that fork; where it concludes keep-fork, it appends a reaffirmation; either way the 07-05 restore was defensively correct at the time (it prevented house-layer clobbering) and is being completed, not called wrong. The `diagnose` amendment in particular must cite the 2026-07-05 entry.

Wayfinder's gating class and de-gating mitigations: it is the one recommendation that trips the irreversible-effect carve-out (permanent `gh issue delete` of live issues on a shared tracker, compounded by concurrent parallel sessions). De-gating does not remove the gate but lowers the blast radius: `disable-model-invocation: true` removes the "fires unattended" half (preserve it); a close-not-delete override plus a confirmation gate before any bulk tracker mutation removes the irreversible-loss half; authoring the tracker mechanism first prevents silent degradation. `triage` also plausibly trips the carve-out once model-invocable + tracker-mutating; its mutation-approval guard ("recommends and waits") is the mitigation, and its external-PR fold must extend that guard to PR labels/comments/closes (all reversible except issue-deletion, which must be forbidden outright). One sharpening the dedicated charter read added: the carve-out is assessed on the landed form (charter.md:42), and the mitigation buys wayfinder ungated *future* churn (once close-not-delete + user-invoked, later edits prune cleanly and need no gate), but it never buys an ungated *adoption* — third-party material is gated regardless of class (charter.md:22, "its risk is authorship coherence, not reversibility"), so the case-(d) fate decision, the charter consult, and the full ledger entry remain mandatory at adoption no matter how well the tool is de-fanged.

Pre-build consult requirement: the charter consult precedes ALL building, per the parent's terms and the charter. For `grill-with-docs`/`decision-record`/`implementation-planning` the recommendation is no build (reaffirm the fork), so no case-(d) decision arises; for the rebases and folds the consult + ledger entries are prerequisites, not afterthoughts.

The Thesis bounds the whole rebase frame — this is the single biggest charter hazard of "upstream-wins-by-default." Charter.md:24-26 holds that nothing third-party runs as-is, so "upstream-wins" legitimately governs only genuinely non-transform fork content (stale procedure text, outdated examples). The house transform — roster rewire to JP's real skills, Use-when/Do-not-use routing, dual-runtime tokens, proof boundaries, `trash`-not-`rm` / close-not-delete, protected-branch awareness, de-brand of mattpocock self-references — is the re-authoring the charter *mandates* (assessed on the contract "as it will land," charter.md:42), not preservable fork cruft. A rebase that dropped the transform in the name of upstream-wins would run third-party material effectively as-is, which the Thesis forbids; de-brand and rewire are therefore Thesis obligations, not polish. This is the same warning §1 and §2 raise as the overcorrection failure mode, stated here in charter terms.

Extraction status and the Mining Queue: the raw mattpocock skills live in an external mining checkout (`/Users/jp/scratch-workspace/mattpocock-skills`), not installed under `skills/` or `skills-claude/`, so Extraction's "remove the original" (charter.md:50-58) does not bind them — this is a fresh-mining pass, not an installed-plugin teardown, and the environment is already clean of raw mattpocock contract text. The ONE installed third-party mattpocock surface is the `setup-matt-pocock-skills` fork in `skills-claude/`, itself already a divergent first-party fork rather than raw upstream. At close, the charter's Mining Queue note (charter.md:56-58) should be re-recorded: it currently reads "fully closed," but the 2026-07-05 reinstall reopened the program and this re-adoption continues it.

Gated triggers beyond case-(d), among the roadmap's rejects: two rejected candidates trip OTHER gates, so their *reject* is itself a gated decision needing coverage — `claude-handoff` fires UNATTENDED (spawns a background agent with no protected-branch guard or confirmation), and `git-guardrails-claude-code` is an ALWAYS-LOADED, unattended-firing hook that also ships third-party contract text (triple-gated). Stated plainly so it is not missed: no *adopted* action in this whole batch installs an always-loaded contract or a skill that fires unattended — every always-loaded/unattended trigger in play sits on a skill being rejected, never adopted.

Landing shape and count: following the 2026-07-05 precedent (one entry batched restore + admit + fold + nine rejects), the whole batch can realistically land as ONE consolidated case-(d) ledger entry with sub-parts — the per-fork rebase supersessions, the ~7 fold notes, a consolidated coverage note for the newly-surfaced rejects and the reaffirm-rejects, and park entries with concrete reopen triggers — EXCEPT the `save-handoff` redaction fold (plugin-distributed, rides the Plugin Layout publish path, §9) and `wayfinder` (a fuller sub-entry if admitted). Every entry needs a durable, replayable pointer (a commit, or this inventory pinned at `d574778`), never a bare session reference (charter.md:66).

Provenance note: §7's dedicated charter/ledger reader completed, but its adversarial verify pass failed the structured-output cap and dropped the whole item from the automated synthesis; the reader's result was recovered from the workflow journal and folded into this section by hand (the Thesis-bound, never-ledgered-07-08, Extraction/Mining-Queue, triple-gated-reject, landing-count, and carve-out-vs-adoption points above). It is therefore the one section carrying a reader finding that did not pass an independent verifier — read its charter-line citations as load-bearing but single-sourced.

## 8. Cross-library design seams and naming/routing collisions

The naive "adopt every upstream name" framing predicts a large taxonomy collision; reading the files shows far fewer LIVE collisions, because JP already renamed every fork (there is no JP skill named `to-tickets`, `to-spec`, `domain-modeling`, `diagnosing-bugs`, `code-review`, `wayfinder`, `implement`, `research`, or `ask-matt`), so the upstream names clash on JOB/ROUTING with differently-named forks, not on same-named skills — except one bundled-name case. The house transform "roster rewire to JP's real skills" is precisely what resolves the naming.

Collisions and resolutions:
- `to-tickets` vs `to-issues`: JOB collision, no name collision. Keep `to-issues`; the only real gap is the missing wide-refactor escape hatch — fold the recognize-and-route seam to `migration-campaign` / `contract-change-propagation` (which already draws the reciprocal edge to `to-issues`), never the machinery.
- `to-tickets` absorbing `implementation-planning`'s territory: not a collision — the split is intentional and the descriptions already disambiguate (`implementation-planning` excludes tracker-issue slicing; `to-issues` excludes next-step planning without publication; `execute-plan` requires an existing plan document). Not adopting `to-tickets` creates no new intra-library seam.
- `to-spec` vs `to-prd`: naming + job collision. Keep `to-prd`; adopting `to-spec` re-introduces "spec" as a publish-target colliding with `design-exploration`'s output and re-carries the `ready-for-agent` skip-triage hazard the fork corrected. Fold only the seam-minimization line.
- `diagnosing-bugs` vs `diagnose`: job collision + bundled-name proximity. Keep `diagnose` — deliberately named to dodge the Claude-bundled `/debug` while still routing on "debug this" (`diagnose:3`). The verifier's precision: `diagnose` is richer on Phase-1 cause-classification and Phases 4/5/6, but missing the Phase-1 red-capable completion gate (the fold target) — not strictly superior on Phase 1. Ensure the routing text keeps disambiguating from bundled `debug`/`verify`/`keep-green`.
- `domain-modeling` vs `grill-with-docs` + `decision-record` + `grill-me`: ownership of glossary + ADRs + grilling. Reject `domain-modeling`; the owner map stays split (glossary = grill-with-docs; standalone ADR + reversal genealogy = decision-record; grilling-without-docs = grill-me). Adopting it would add a third overlapping owner and re-fuse what JP intentionally separated.
- `code-review` — the ONE genuinely new HARD bundled-name collision: upstream `code-review`'s name exactly matches the live Claude-bundled `/code-review`, and as a dual-runtime `skills/` skill the name is AGENTS.md-forbidden. Its job is already owned by `review-family` + `tech-debt-scan` + `improve-codebase-architecture`. The collision is the reason it is rejected, not an obstacle to work around.
- `triage` description now contradicts the planned external-PR fold: `triage:3` currently excludes "PR review," which the external-PR-triage fold ("a PR is an issue with attached code") would contradict. This is the one genuinely-required description rewrite the migration creates — admit inbound external-PR TRIAGE (categorize / verify the diff / write an agent brief / route to tracker states, including an already-implemented `wontfix` branch) while still excluding PR review loops (`gh-address-comments` / `gh-pr-review-loop`) and completed-code review against a plan (`implementation-review`). Keep the `to-prd`/`to-issues` routing lines intact.
- `resolving-merge-conflicts` (the roadmap's single whole-skill admit): no name collision, but a git-cycle routing boundary must be authored against `merge-branch` (ff-only, stops on conflict), `keep-green` (test/lint green, not conflicts), and `exiting-worktrees` ("never resolve merge conflicts"). Naming call for the user: git-cycle siblings use terse names (`merge-branch`, `release-cut`) — consider `resolve-conflicts`/`merge-conflicts` over the gerund. This admit's charter gate is stated in §7 (whole-skill third-party admission), its landing placement in §10, and the naming decision is carried as an open question in §11.
- `wayfinder:*` labels do not exist (deferred collision / environment fact, §5). Not a live collision today.

Verifier corrections folded in: the reader's disable-model-invocation divergence must be added to the house-layer-to-rederive — on any rebase, PRESERVE JP's model-invocable choice for `to-issues`/`to-prd`/`triage` rather than adopting upstream's `disable-model-invocation: true` frontmatter, which would silently flip JP's publish-side skills to user-invoke-only; and the "native sub-issues + issue-dependencies confirmed present in JP's tracker" claim in this area was asserted, not file-verified in that pass (it is GitHub-native and verified elsewhere in §5/§6, and is non-load-bearing here since wayfinder stays parked). The `openai-docs` name in `skills-claude/` matching a Codex-bundled name is AGENTS.md-sanctioned reuse (Claude-only, Codex never scans `skills-claude/`) and is outside the mattpocock taxonomy shift.

## 9. Delivery / house-fit mechanics and reversibility

Upstream carries no delivery infra worth adopting: placement, the symlink farm, dual-runtime naming, the version-bump/republish/mirror chain, `trash`-not-`rm`, and the protected-branch floor are all house transform. "Safely" is entirely a house-layer question. The recommended landings split into three delivery classes.

CLASS A — in-place edits to existing dual-runtime `skills/` skills (the `diagnose`, `triage`, `to-prd`, `to-issues` folds, and any sibling prose edits): lowest friction. No new symlink, no `--link`, no Codex republish (Codex scans `$HOME/.agents/skills` in place; Claude serves through the already-present `~/.claude/skills/<name>` symlink), no mirror. Sync step: run `scripts/claude-skills-sync.sh --check` only, to confirm the farm stays green. Validation Ladder: `git diff --check` whitespace per edited file; parse edited `SKILL.md` frontmatter; referenced-path check (verified present: `triage/AGENT-BRIEF.md`, `triage/OUT-OF-SCOPE.md`, `diagnose/scripts/hitl-loop.template.sh`); and one behavior-smoke-test / forward dry-run per changed skill that the new discipline is actually followed. Reversibility: a single `git revert` — no cache/symlink/mirror/tracker state.

CLASS B — plugin-distributed (relevant to the roadmap's adjacent items, included here for the sequencing picture): the `setup-matt-pocock-skills` rebase is NOT class B (it is a `skills-claude/` skill, in-place for Claude, never Codex-scanned), but the roadmap's save-handoff redaction fold (`plugins/handoff/`) and any `resolving-merge-conflicts` admit (a new `plugins/git-cycle/skills/<house-name>/` sibling) are: Claude serves both in place next session, but Codex needs a `.claude-plugin/plugin.json` version bump + CHANGELOG in lockstep -> `scripts/codex-plugins-sync.sh --publish <plugin>` (Codex serves a version-keyed cache; un-republished edits are invisible and `--check` reports DRIFT) -> mirror copy into `/Users/jp/Projects/active/codex-tool-dev/plugins/turbo-mode/<plugin>/` only on explicit publish. A new sibling inside a plugin needs no top-level `--link` (the sync guard iterates `plugins/<name>/`, not the skills within) and no `/reload-plugins` for a pure skill addition. Both of these adjacent Class-B items carry their own charter case-(d) gate (§7) and landing placement (§10). Reversibility: `trash` the new dir or revert the reference edit + re-bump/republish + revert the mirror commit; additive + namespaced, so backing out touches no sibling.

CLASS C — wayfinder (parked, not built now): the only tracker-mutating case with a truly irreversible tool. Its reversibility plan is the close-not-delete + confirmation-gate mitigations of §6/§7; it is the single highest-severity reversibility risk in the batch and is deliberately deferred.

House-transform delivery checklist applied to every landed piece: de-brand (strip mattpocock self-references and un-adopted-skill cross-references); roster rewire to real skills; Use-when/Do-not-use + dual-runtime tokens; proof boundary (for `skills/` and plugin sources the edited source IS the live skill source — structural checks prove parse, only a realistic invocation/forward-test/dry-run proves the changed behavior is followed; for plugin-distributed pieces the Codex proof layer is the version-keyed cache, so `codex-plugins-sync --check` green matters); `trash`-not-`rm` on local artifacts and close-not-delete on tracker writes; and the dual-runtime naming floor for any new name (avoid Codex-bundled and Claude-bundled names, and, for a new git-cycle sibling, the 8 existing siblings).

Verifier correction folded in: add `scripts/check-library-integrity.sh` to the validation checklist as the repo's on-demand structural sweep. It owns four checks the piecemeal ladder does not fully cover — name==directory, cited-paths-resolve (including plugin-shared `references/` two levels up), orphan-support-file (every git-tracked file under a skill bundle must be mentioned by that skill — genuinely uncovered by the referenced-path-existence check alone), and the `quick_validate.py` frontmatter sweep — and it delegates to `claude-skills-sync.sh --check` and `codex-plugins-sync.sh --check`. It bites hardest on any new git-cycle sibling and confirms, for the handoff fold, that `save-handoff` still cites its format file and the file is not orphaned. `quick_validate.py`'s "unexpected key" complaint about `disable-model-invocation`/`argument-hint` is an accepted false positive — never resolve it by deleting the field.

Reversibility summary: Class A backout is a single revert; Class B is trash-or-revert + re-bump/republish + mirror-revert; roll back on the working branch before the fast-forward merge, and post-merge use revert-commit + republish + mirror-revert. All landings happen on a `chore/`/`fix/`/`feature/` branch fast-forwarded to `main` (protected-branch floor), and any backout uses `trash`, never `rm`.

## 10. Recommended landing sequence (dependency-ordered)

0. Charter consult FIRST (`docs/agents/charter.md`) — every rebase/fold/admit is gated; no building precedes it.
1. Setup/tracker infra rebase (`setup-matt-pocock-skills`) — it is the root dependency: it seeds `docs/agents/{issue-tracker,triage-labels,domain}.md`, defines the label vocabulary the tracker skills route on, and (if wayfinder is ever adopted) carries the Wayfinding-operations mechanism. Rebase it, extend the de-brand to the seed templates, keep `qa` dropped, re-apply the grill-with-docs rewire, adopt PR/MR plumbing at default `no`, and gate the wayfinding blocks on step 6's decision. Land this before the skills that assume its labels and docs.
2. The three hybrid-rebases and the one adopt-upstream-base on the tracker-publish/diagnosis family, in any order among themselves (each is an independent, single-revert Class-A edit): `to-prd` (upstream body + re-derived label/consent + re-absorbed seam sentence), `to-issues` (fork base + 4 folds), `triage` (upstream base + house transform + regression fix + the required description rewrite), `diagnose` (fork base + Phase-1 gate, gated on the 2026-07-05-intent confirmation from §11). These depend on step 1's labels/docs but not on each other; the `triage` description rewrite must land WITH the external-PR fold, never after, to avoid the self-contradiction in §8.
3. No-op reaffirmations (no build, no ledger event): `grill-with-docs`, `implementation-planning`, `decision-record` stay as forks. `decision-record`'s format home stays at `grill-with-docs/` because `domain-modeling` is not adopted; the shared `ADR-FORMAT.md`/`CONTEXT-FORMAT.md` single-source is preserved by NOT relocating it — this must be respected by every edit in step 2 that touches those consumers.
4. Wayfinder is out of this sequence — parked. If ever un-parked, it lands LAST and only after: the setup skill's Wayfinding-operations mechanism is authored (step 1's file must gain it), the `wayfinder:*` labels are created, the dependency rewiring (§6) is applied, and the close-not-delete guard is in place. Infra before wayfinder; labels before any map/ticket operation.
5. Roadmap-adjacent Class-B plugin items (§8/§9), sequenced here for completeness because they share this inventory's charter accounting (§7) even though they sit outside the core mattpocock tracker-publish family: the `save-handoff` redact-secrets/PII fold into the `handoff` plugin, and — if admitted — the `resolving-merge-conflicts` whole-skill admit as a new `plugins/git-cycle/skills/<house-name>/` sibling. Each is its own charter case-(d) event with its own ledger obligation (§7): the save-handoff fold a one-line note, the resolving-merge-conflicts admit a full entry plus a fresh build and an authored git-cycle routing boundary (§8). Both land via the plugin path (`.claude-plugin/plugin.json` version bump + CHANGELOG + `codex-plugins-sync --publish` + mirror), carry no dependency on steps 1-3, and may land in any order relative to them; only wayfinder (step 4, Class-C) carries the irreversible-tool gate.

General ordering invariant across all steps: fold the CONCEPT, never a verbatim glossary copy, so no third copy of any single-sourced vocabulary is introduced. (The orchestrator's "ADR single-source before grill-with-docs rename" note has no concrete target — `grill-with-docs` is reaffirm-keep with no rename — so it is honored as this general single-source-before-duplication discipline; see §11.)

## 11. Adjudicated decisions (locked 2026-07-08)

All calls below were walked with JP on 2026-07-08 and locked; each landed on the inventory's recommended option (`diagnose` after a git re-check of the 07-05 fold). These are decisions of record for the landing, still gated behind the pre-build charter consult (§7) — locked is not built. Each bullet states the original question, then the LOCKED outcome.

- diagnose Phase-1 completion criterion — was the 07-05 fold a deliberate decline or merely out of scope? LOCKED: **skip the fold — deliberate decline.** A git re-check settled it: the 07-05 fold commit (`b381e54`) message called minimise "the one genuinely additive step" while surveying the same upstream file (`bc4cf90`) whose Phase-1 completion criterion sat immediately adjacent to the minimise section it harvested, and `diagnose` already gates on "a loop you believe in." Keep that gate; record the decline. Held in reserve, not built now: the two mechanical sharpenings (pasted-evidence proof + anti-theorising stop-guard) remain an optional future upgrade, not a correction of an oversight.
- domain-modeling adoption (the pivot for §3.4 and §3.6). LOCKED: **stays rejected.** `decision-record`'s ADR-format home stays at `grill-with-docs/` unchanged, the §3.6 atomic-rewire does not activate, and the §3.4 keep-fork verdict stands.
- to-issues native edges (medium-confidence fold): native sub-issues/dependencies vs text-only `Blocked by`, and the optional `tickets.md` no-tracker mode. LOCKED: **adopt native GitHub sub-issues + issue-dependencies** (both verified available on `jpsweeney97/agents`) in place of text-only `Blocked by`; **defer** the `tickets.md` no-tracker mode as over-adoption.
- disable-model-invocation posture on the publish/mutating skills (`to-prd`, `to-issues`, `triage`). LOCKED: **keep model-invocable** across all three; do not adopt upstream's manual-only gate. The `triage` carve-out is handled by its mutation-approval guard (recommends and waits), not a hard manual gate.
- ask-matt (no JP equivalent; appears only in wayfinder's un-adopted docs page). LOCKED: **moot while wayfinder is parked** — decided at build time if wayfinder is ever un-parked (drop is the lean).
- triage-labels template de-brand + PR/MR-triage-surface config. LOCKED: **de-brand the template** `skills-claude/setup-matt-pocock-skills/triage-labels.md` to `Canonical role` to match the live instance; the PR/MR-triage-surface config default is **`no`** (matches this private repo).
- resolving-merge-conflicts (roadmap-adjacent whole-skill admit, §8/§9): admit or defer, and under what name? LOCKED: **admit as `resolve-conflicts`** — a new git-cycle plugin sibling (terse name matching `merge-branch`/`release-cut`), with a full case-(d) ledger entry, a fresh build, and a routing boundary against `merge-branch` (ff-only, stops on conflict), `keep-green`, and `exiting-worktrees`. Lands via the plugin publish path (§9, §10 step 5). (The adjacent `save-handoff` redaction fold is recommend-only with no user decision; it is accounted in §7/§9/§10.)
- Strategic lane model: keep JP's two-lane model or converge to mattpocock's `to-tickets` -> `implement`? LOCKED: **keep the two-lane model** (`implementation-planning` -> `execute-plan` for path-exact plans; `to-issues` for tracker slices); do not converge, and all §3.1/§3.5 verdicts stand. Setup skill identity: **keep the provenance name** `setup-matt-pocock-skills` (a deliberate retained choice per §4; not re-litigated — flag if you later want it renamed).
- ledger-entry shape (charter read, §7): one consolidated entry vs one per fork? LOCKED: **one consolidated case-(d) entry** on the 2026-07-05 precedent, with the per-fork rebase supersessions, fold notes, the `resolve-conflicts` admit, and the wayfinder park as sub-parts; `save-handoff` (plugin publish path) and any future wayfinder build get their own handling.
- rejects and parks ledgering (charter read, §7): consolidate + ledger the parks now, or leave as roadmap recs? LOCKED: **scope the ledger to this batch.** The consolidated entry covers the family rebases, folds, the `resolve-conflicts` admit, the wayfinder park (with a concrete reopen trigger), and the setup infra; the broader 2026-07-08 roadmap's never-ledgered parks/rejects of the ~21 out-of-family skills stay roadmap recommendations until separately actioned — but the two triple-gated rejects (`claude-handoff` unattended, `git-guardrails-claude-code` always-loaded) are flagged here so they are not silently dropped.
- wayfinder: confirm it stays parked? LOCKED: **confirmed parked** — this pass ledgers the park with a concrete reopen trigger. Un-parking later is a skill-squad-class build (author the missing tracker mechanism + rewire `/grilling`->grill-me, `/domain-modeling`->grill-with-docs, add the close-not-delete guard), not a rebase.
- ADR-single-source-before-rename ambiguity. LOCKED: **no rename intended** — `grill-with-docs` is reaffirm-keep; the sequencing note is honored only as the general single-source-before-duplication discipline.

## 12. Honest limits

What this inventory validates by itself: nothing. It is a read-only recommendation set. Structural reads, byte-diffs, `git` history, `grep`, `gh label list`, and GraphQL endpoint checks establish the current state and the merit calls, but no skill behavior, sync, republish, or tracker mutation was exercised. The delivery paths in §9 are described from the authoritative script sources and confirmed directory layouts, not from a live `claude-skills-sync --check` / `codex-plugins-sync --check` run or a real republish/mirror push.

What each migration still needs before it is real, at landing:
- The owning-skill edit itself, done through the owning lane (`writing-principles` for obligation-only prose, `agent-facing-design` before adding obligations/machinery, `scrutinize-skill` for contract critique), matching the existing skill shape.
- The Validation Ladder for the exact surfaces edited (whitespace, frontmatter parse, referenced-path existence AND the orphan-support-file reverse check via `check-library-integrity.sh`, `quick_validate.py`), plus one behavior-smoke-test / forward test / realistic dry run per changed skill — structural checks prove parse, only a real invocation shows the changed behavior is followed.
- A `docs/agents/contract-decisions.md` entry (append-only) for each gated case-(d) decision, with an evidence pointer to this inventory and the pin `d574778` — including the `diagnose` amendment that references the 2026-07-05 entry.
- The charter consult before any of the above.

Residual uncertainties carried forward honestly: the `diagnose` 2026-07-05-intent question (may be a settled decline, not a gap); the `to-issues` native-edge fold (medium confidence, may be a deliberate text-edge choice); wayfinder's whole build cost (a real dependency-authoring effort plus the irreversible-tool gate); and the reader-vs-reader `domain-modeling` seam between §3.4 and §3.6, resolved here to keep-fork on the ledger and One-Owner grounds but re-openable by the user, which flips §3.6 from keep-fork to the atomic-rewire hybrid.

A provenance limit specific to §7: of the 13 areas, twelve passed both a reader and an independent adversarial verifier; the charter/ledger area is the one exception — its verify pass failed the automated structured-output cap, so its reader finding was recovered from the workflow journal and hand-folded (see the §7 provenance note). Its charter-line citations were spot-checkable against `charter.md` and `contract-decisions.md` but did not receive the same independent second pass, so re-confirm the governance calls in §7 at the charter consult rather than treating them as verifier-cleared.
