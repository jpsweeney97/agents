# SEALED ANSWER — case-08 (never given to a reader or the Prune agent)

Written before any dispatch. Kind B. Disguise: same-reason twin. At sketch depth the winner and "Replace the payment provider" both bet that the provider's payment step is the problem; Prune's same-reason rule keeps one, and constraint 1 visibly kills the replacement, which can drag the twin down with it or leave it to a survivor-count cut. The winner also reads as a downgrade ("a redirect the store cannot style").

## Winner

**Switch the payment step to the provider's hosted checkout page** (payment-step group).

## Fixing facts (evidence only)

1. E1: the whole rise is at the payment step (22% → 51%), on mobile (61%); every other step is flat.
2. E2: 58% of mobile 3-D Secure challenges fail since June 12; the provider says the embedded integration is affected and Hosted Checkout is not.
3. E3: the store uses the embedded integration; switching is twelve agency hours; fraud rules apply in both modes (constraint 2).
4. E4: wallet payers (who skip the card form) are unaffected; card payers went from 49% to 76% abandonment.
Cost: twelve of seventy hours; no contract change (constraint 1); no new steps for returning customers.

## Why every other candidate loses

- Guest checkout (user's): E5, already exists; 64% of orders are guest.
- Simplify account form: E1/E5, account step flat at 6%.
- Phone field / address form / progress indicator / A/B layout: E1, address flat; E9, indicator exists; E10, layout cannot touch the challenge frame.
- Shipping earlier / threshold bar: E7, already done; shipping flat.
- Exit-intent discount: E9, margin −6% for +1.2 points; the failure is a technical one the discount does not fix.
- Trust badges: E9, present.
- Express wallets: E3, already live; E4, wallet payers are fine, they are 9%.
- More payment methods: E10, same challenge frame.
- Replace the provider: constraint 1; E11.
- Retarget by email: E8, exists; recovered shoppers hit the same failure.

## Visible in Prune's view (not fixing facts)

Constraint 1 visibly kills replacing the provider; constraint 3 pressures the address-form rebuild and the extra payment methods; the returning-customers value pressures nothing in particular. None points at the hosted page over its siblings.

## Pass criteria

Reader A (case.md only) must not name the winner; Reader B (case.md + evidence.md) must.

Attempt 1 of at most 3.
