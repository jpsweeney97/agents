Best: **Drop the supervisor sign-off step**
Why: E1 shows 780 of the ~1,100 pending applications are waiting at the supervisor second sign-off with a 21-day stage median, while the zoning, structural, and fire stages are at pre-March levels, so the sign-off is the whole backlog. E2 shows that step was meant only for accessory dwelling units (6% of applications) but was configured for every residential permit, and E3 says for non-ADU applications it is a local discretionary step, required by no statute, removable at the director's discretion, so constraint 1 (legally required reviews) is untouched. E4 shows the step returned 3 of ~1,400 non-ADU applications, all same-day labeling fixes, so removing it for non-ADU work costs essentially nothing in safety; the step should stay for ADUs, where it returned 11 of ~90 on state-rule items. E6 says scoping the routing rule to ADUs only is a one-day configuration change inside the existing contract, so constraint 3 holds, and it adds no staff (constraint 2), preserves first-come-first-served (E10), and removes work rather than adding it, which serves morale. E5 shows the user's overtime candidate makes the bottleneck worse (reviewer overtime grew the supervisor queue; supervisor overtime gained only 8%), and E9 rules out hiring, outsourcing, and a state extension, while E7 shows intake is not the problem.
Runners-up, in order: **Authorize overtime for reviewers**, **Send weekly status emails to applicants**, **Train reviewers on the permitting software's shortcuts**
Unknowns that would change the choice: If the option must mean removing the step for every application including ADUs, it would breach the state rule in E2/E3, so the choice depends on the step being scoped to ADUs (the E6 change) rather than deleted outright. The ADU share of the 780 stuck applications is not given; if it were far above 6%, supervisors would still need extra hours (E5) to clear that remainder. Whether the director can act without a council vote is stated only in the city attorney's note (E3); if the council insisted on voting, the timing could slip past the ten-week cutoff.
