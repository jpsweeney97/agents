Best: **Remove the second sign-off added in March**
Why: E1 shows 780 of the roughly 1,100 pending applications sitting at the supervisor second sign-off with a 21-day stage median, while the three legally required reviews are at their pre-March levels, so the backlog is that one step. E2 shows the step was meant only for accessory dwelling units (6% of applications) but was configured for every residential type, and E3 confirms the city attorney holds it discretionary and removable by the director for all non-accessory-dwelling applications, so constraint 1 is untouched. E4 shows the step caught 3 same-day labeling fixes in about 1,400 non-accessory-dwelling sign-offs, so removing it costs no safety. E5 shows the alternative capacity lever fails: reviewer overtime only grew the supervisor queue, and supervisor overtime raised sign-off throughput by just 8%. E6 makes the fix a one-day configuration change inside the existing contract, and E9 and E10 rule out hiring, outside services, a state extension, and a fast lane; the sign-off should stay for accessory dwelling units, where E3 says it implements the state rule.
Runners-up, in order: **Authorize overtime for reviewers**, **Add pre-application checklists**, **Send weekly status emails to applicants**
Unknowns that would change the choice: Whether the director will actually issue the change and the vendor can schedule the one-day reconfiguration immediately, since a delay of weeks would push overtime back up the list. Whether the 780 applications already queued at the sign-off can be released straight to issuance or must each be individually cleared, which sets how fast the backlog drops. Whether any council direction or local ordinance beyond what E3 covers requires the step, which would take this option off the table.
