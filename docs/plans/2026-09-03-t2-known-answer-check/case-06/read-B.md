Best: **Restore the previous export date format**
Why: E1 shows the entire doubling is one new category: `export-dates` went from 0 to 482 of 830 tickets while every other category stayed flat or fell, and E6 confirms the per-account rate of all other tickets is unchanged at about 0.4 a month. E2 says the format change was an unintended side effect of a library upgrade, the documented format is ISO 8601, and the fix plus a per-account toggle is a two-day job, well inside the one-engineer-one-week constraint. E3 removes the only apparent blocker: restoring the documented format is a correction, not a change, so the ninety-day notice rule does not apply, and the toggle covers the three customers who adapted. E4 shows the macro route is already exhausted, since 470 of those tickets got the macro yet each still spawns 2.3 follow-ups because the export re-runs weekly, so only removing the cause stops the load, and E7 shows 74% of these tickets come from the two highest-paying plans, so this also serves the stated value of customers getting what they need. By contrast E5 caps the user's chatbot at about 35 tickets a month, and E8 rules out the forum (four to six months), auto-close (61% reopened), and outsourcing (budget freeze, and the vendor cannot change the product).
Runners-up, in order: **Show a known-issues banner in the app**, **Expand the help documentation**, **Add a chatbot to the help center**
Unknowns that would change the choice: If more customers than the three in E3 have silently rebuilt their imports around the accidental format, the toggle's default direction and the communication plan matter more, though the fix itself would still be right. If the two-day estimate in E2 is stale or the release process needs more than a week to ship a fix, the banner becomes the first move and the fix the second. Nothing in the evidence explains why a medium-priority backlog item with this ticket impact was not already shipped, and any hidden reason for that would need to be checked.
