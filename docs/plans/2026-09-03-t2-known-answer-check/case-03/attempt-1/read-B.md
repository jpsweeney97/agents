Best: **Update the analytics SDK to its latest release**
Why: E1 pins 96% of crashes to one stack trace inside the analytics SDK's SessionTracker on OS 19 foreground transitions, and E2 shows the vendor shipped 4.2.1 three days ago with a fix naming that exact class, exception, and trigger, while E3 shows the app is still on 4.1.0 and the 4.1 to 4.2 migration needs no integrator changes, so this is a dependency bump well inside the two-week window with no new contract. E4 kills the user's rollback candidate: release 7.3 crashes at 3.0% on OS 19, the same as 7.4, because both declare the same SDK version (E3). E6 kills the guards and the global handler, since the exception fires on the SDK's own thread during a callback the app never invokes, and the crash is not feature- or screen-specific, which also kills the feature flag and the native rewrite (E1, E8). E5 kills postponement, dropping analytics, and any option that removes the schema events; E8 puts vendor replacement and a server-side pipeline at three to six weeks; E9 shows sampling is already at 100%; E10 rules out memory as the cause; and constraint 3 rules out the contractor. Dropping OS 19 support would cut 61% of users and rising (E7), which contradicts the stability-first value and would likely be visible to the featuring team.
Runners-up, in order: **Drop support for the new operating system version**, **Add a remote feature flag to disable the crashing feature**, **Reduce the app's memory use**
Unknowns that would change the choice: Whether 4.2.1 actually eliminates the crash in this app's integration on real OS 19.0 and 19.0.1 devices is asserted only by the vendor changelog (E2), not by a test the team has run. Whether the 4.2.0 consent API (E2) changes which events the SDK emits before consent is granted, since E5 requires session start, screen view, and retention cohort to be present and reporting. Whether enough users adopt the new app release fast enough for the seven-day crash-free window in E5 to clear 99%, given that sessions on un-updated builds keep crashing.
