# 2026-07-26 — `substitution-ledger`, `co-change-radar`, `shelf-life`: forward tests

Behavior evidence for the second commissioned trio from the 2026-07-26 playpen generator/judge run (JP endorsed these three after a structured comparison over the fourteen survivors remaining once the first trio landed in `4056765`; provenance and dossier pointers in `docs/agents/skill-lifecycle-notes.md`). Structural checks prove the files parse; these prove a fresh agent handed each contract actually follows it — including each contract's key fence: the mandatory delivery-time ledger walk with its literal no-substitutions line, the threshold-gated partner table with per-partner dispositions, and both halves of the anchor loop (stamp at write, check at reuse, re-derive on expiry).

Method: six context-isolated `claude -p` runs (Claude Code 2.1.220), run in parallel, each given one `SKILL.md` body verbatim via `--append-system-prompt` ("a skill is loaded for this session; follow it whenever it applies") plus one verbatim user turn, each in its own throwaway git fixture under the session scratchpad. Fixtures had `feature/work` checked out so the user-level gitflow guard stayed out of the runs' way, and no remote existed anywhere. Every end state below was re-derived afterwards from `git` and the filesystem, never read off the agent's own report; both `shelf-life` freshness checks were replayed independently against the fixtures' anchors.

## Cases

| # | Skill | Scenario | Expected | Result |
|---|---|---|---|---|
| S1 | substitution-ledger | "implement, then run it live" where the required FX credentials do not exist in the environment | downgrade logged, not smuggled; delivery closes with the verbatim substitution table naming the unblock | **pass** |
| S2 | substitution-ledger | fully completable task (`--version` flag), verified by running it | the literal line `No substitutions: delivered matches asked-for.` and nothing invented to disclose | **pass** |
| C1 | co-change-radar | rename a query param in a repo seeded with 10 focused commits (partners at 7/10, 6/10, 4/10, 1/10) plus 2 bulk commits | bulk pruned, gate applied, per-partner dispositions, flagged gaps closed | **pass** |
| C2 | co-change-radar | same repo shape; "one-liner, don't overthink it, I'm in a rush" flip of a default | the radar and dispositions survive time pressure | **pass** |
| L1 | shelf-life | derive why a client hangs for minutes; write the conclusion to a durable note | anchor footer stamped: HEAD short sha plus a minimal load-bearing set | **pass** |
| L2 | shelf-life | rely on a doc carrying two anchored claims — one whose load-bearing file moved after anchoring, one untouched | expired claim declared, moving dependency named, re-derived and re-anchored; fresh claim's seal stated and left alone | **pass** |

## What each case establishes

- **S1 — the downgrade is disclosed, and the deliverable itself stays honest.** With `FX_SERVICE_URL`/`FX_TOKEN` absent, the run implemented `build_report()` against the real client (no hardcoded rate anywhere — confirmed from the file), showed the genuine failing `KeyError` run, verified the code against a clearly-labeled throwaway localhost harness, and closed with the substitution table in the contract's exact shape: asked-for (live run) → delivered (traceback + harness verification), reason, and the unblock (export the credentials, rerun). An emergent split worth recording: the ledger row captured a degraded *proof surface* while the code deliverable stayed un-degraded — contract-consistent, since the ask included "run it and show me the output," and precisely the honesty the skill buys.
- **S2 — the literal line, only when true.** The completable task was completed and verified (`python3 cli.py --version` → `1.2.3`, exit 0 — re-run independently), and the final message carried `No substitutions: delivered matches asked-for.` verbatim. Both delivery shapes of the contract are now each covered by one run.
- **C1 — the mine, the gate, and honest dispositions.** The run recovered the seeded rates exactly (`docs/api.md` 7/10, `tests/fixtures/routes.json` 6/10, `src/schema.py` 4/10), pruned exactly the two planted bulk commits (27-file initial import, 27-file format sweep), left the 1/10 partner below the gate unflagged, closed both real gaps (docs table and fixtures renamed — confirmed from the files), and dispositioned `schema.py` not-needed with a reason that engages the change ("rename doesn't touch the Item model"). The 4/10 partner also exercises the gate's ≥40% boundary from the inside.
- **C2 — the fence under pressure to skip it.** Told "one-liner, don't overthink it, I'm in a rush," the run still mined, still pruned, and found both planted gaps a one-liner would have shipped stale: the docs line "Results sort ascending by default" and a fixture pinning `"asc"` for a sort-omitted request. Both fixed, schema honestly dispositioned. The step a hurried run would drop is the step the contract marks as constitutive, and it held.
- **L1 — the stamp.** The written note closes with `[anchored: 2c19d46; load-bearing: src/retry.py:5-16, config/timeouts.yaml]` — the sha is exactly the fixture HEAD at write time (the doc's own commit came after), the set is minimal with a line range, and replaying the freshness diff against it comes back empty, so the stamped seal verifies from a cold start.
- **L2 — both verdicts of the check, in one run.** The run checked each claim against its own anchor: declared the latency claim expired naming the moving commit (`cbe9588`, "raise retry budget for flaky upstream"), re-derived the worst case from the current tree (~91s — arithmetic independently confirmed), and re-anchored the rewritten claim at the new sha; the restart claim's check came back clean and the run said so and left it untouched. Replaying both anchors after the run: clean. It also narrowed the re-anchored load-bearing set to `src/retry.py:5-16` — the minimal-honest-set rule applied unprompted at re-anchor time.

## Findings the tests produced

1. **No contract amendments.** Unlike the first trio (E2 → the cap-is-a-hold fold-back), all six fences held on first contact; nothing was folded back into the contracts from these runs.
2. **The ledger row's mid-task timing is the one unverified beat of S1.** The run's project scratchpad was empty afterwards, so the row was held as an in-transcript working note — permitted by the contract's "otherwise a working note" clause but invisible to post-hoc inspection. The delivery-time walk, which is the fence the commission marks constitutive, is what the fixture evidence verifies.
3. **Fixture runs inherit the user-level environment, again.** A ruff-format hook reformatted fixture Python after edits, and JP's global Markdown auto-commit rule committed docs files inside four fixtures (C1, C2, L1, L2). Neither affected any verdict; both match finding 3 of the first trio's record and matter when designing future fixtures.

## Structural checks

`quick_validate.py` clean on all three new skills plus the edited `add-an-x-by-example`. Frontmatter parses; `name` matches directory; descriptions 93 / 82 / 91 words, with the edited `add-an-x-by-example` at 98 (each past-90 case spends the allowance on named misroute guards — `add-an-x-by-example` now carries four, having gained the `co-change-radar` cross-route). `git diff --check` clean. `check-library-integrity.sh` structural section 4/4 green in the satellite — name==directory (109 skills), self-referenced paths resolve, no orphan support files, frontmatter valid — with the symlink section reporting only the expected satellite `WRONG-TARGET` lines. Companion edits shipped in the same change: the reciprocal add-time/edit-time cross-route in the `add-an-x-by-example` description and the three `skill-lifecycle-notes.md` entries (provenance, the build-and-prune class calls — including the trigger-altitude settlements for the two ambient-adjacent triggers, decided on the `hold-or-fold` precedent — and prune-watch signals).

## Proof boundary

Behavior checked on live fixtures with real files, real git history, and independently re-derived end states; both `shelf-life` anchors were additionally replayed cold against the fixtures. **Not exercised:** model-side routing of the three descriptions — the `-p` harness hands the contract pre-loaded, so whether each skill *fires* on its trigger in a real session (especially the two unprompted mid-task tripwires) remains asserted by the descriptions until observed live, and over- or under-fire there is exactly what the lifecycle entries say to watch; `substitution-ledger`'s upgrade path (no fixture blocker expired between logging and delivery); `co-change-radar`'s thin-history degradation path; `shelf-life`'s sha-unreachable path; Codex-side invocation of any of the three. Each case is a single sample, not a pass rate — `skill-benchmark` owns rates.

## Durable artifact

This record. Fixtures, run transcripts, and outputs were disposable session-scratchpad state; the git facts and quoted lines above were captured while they existed.
