## Census, graded

| Surface | Result | Grade |
|---|---|---|
| Body-marker grep, Claude + Codex | 61 sessions had the body in context. 30 are genuine fires, 19 of them in the field (working directory outside the skill repo). I am using these counts as reported and read no transcripts. | **contextualizes**. It confirms use. Every body sentence is in context in every fire, so it cannot show one step live or dead. |
| Route-name probe, assistant text in 19 field fires | `design-exploration` 16, `ideate` 0, `prototype` 0, `grill-me` 0 | **contextualizes** for the three zeros. **Inconclusive** for the 16, because assistants quote the Exits table and those hits may be quotes, not routes. |
| Local proxy sessions (repo test fixtures) | `prototype` and `ideate` named 40+ times | **Excluded.** The fixtures were built to exercise those routes, so they are not field use. |
| Marker vintage, corpus reach | Cleared | Not evidence. It bounds every claim below to 2026-07-02 onward. |

## Findings

**1. Decided-here: the only visible sign of a re-type is the route name.**
- The skill types mud silently, so on text and census the only trace of a re-type is its route being named.
- This is not a contract finding because it concerns what the method can know about its own central claim ("mud re-types to the right tool"), not wording or output shape.
- **What the route-name row can carry:** none of `ideate`, `prototype` or `grill-me` was named in any of the 19 field fires since 2026-07-02. Quoting can only add hits, so these zeros are not inflated.
- **What it cannot carry:**
  - That re-typing never happened. A route stated in plain words ("let's widen the options first") matches nothing.
  - That no field fire had mud of these kinds. Zero demand looks the same as a dead step.
  - Anything about `design-exploration`. Its 16 hits may be quotes, and it is the exit that needs no re-type.
  - Whether any route chosen was the right one.
- **Verdict on text + census:** the field shows no announced re-type to `ideate`, `prototype` or `grill-me`. It does not show the step is live or dead.

**2. Escalate-rider: does the re-typing step fire in the field, and in the right direction?**
- Finding 1 settles the structure.
- These need the fires read: how often mud of these kinds appears, whether it was re-typed silently, and whether the route fit the mud.
- To settle whether it fires the right way, commission `methodology-critique` on this axis. I have not invoked it.

Need from you: tell me whether to treat the re-typing step as a `methodology-critique` candidate (1: yes, commission it; 2: no, leave it at this verdict).
