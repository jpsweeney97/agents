### Census, graded

| Surface | What it shows | Grade |
|---|---|---|
| Body-marker grep (Claude + Codex, since 2026-07-02) | 61 sessions have the body in context. 30 are genuine fires and 19 are field fires, as supplied. I did not check the classification, so 30 is unconfirmed. The whole body loads at every fire, so every body marker appears at the fire count. Per-step deadness of body steps, re-typing included, is not cheaply probeable. | contextualizes |
| Output probe: route names in field assistant text | `design-exploration` 16 hits, `ideate` 0, `prototype` 0, `grill-me` 0. These are mention counts. The hits are not stated as distinct sessions. | inconclusive for liveness |
| Marker vintage and corpus reach | Cleared. Every claim below is bounded to fires from 2026-07-02 on. | confound cleared |
| Local proxy sessions | `prototype` and `ideate` are named 40+ times, but these are test fixtures inside the repo, not fires. | excluded (contamination); no weight on field |

No row reaches "drives". No marker is grep-proven absent, because the Exits table is in context at every fire.

### Findings

**F1: Whether mud re-types to `ideate` or `prototype` in the field. Tag: escalate-rider** (structure decided here; how often it happens, and in which direction, needs real transcripts).

Text plus census cannot show this step is live or dead. Re-typing means changing the mud's kind when questions stop helping. The body types mud silently, and the only census observable is route-name mentions, which do not see that step.

Not a contract finding because a contract pass checks that the Exits table is well-formed. It does not check whether the central claim ("mud re-types to the right tool") leaves any trace in output.

**What the route-name row can carry**
- In 19 field fires since 2026-07-02, assistant text named `ideate`, `prototype`, and `grill-me` 0 times and `design-exploration` 16 times.

**What it cannot carry**
- **That re-typing is dead.** A real route to evidence reads "run a small test", not "`prototype`". A zero is not deadness.
- **That the 16 hits are exits.** The loaded table is in every fire, and assistants quote or paraphrase it. `design-exploration` is also the "want clear" route, which involves no re-typing.
- **A denominator.** Nothing says how many of the 19 fires hit missing-information, missing-options, or conflict mud. Zero `prototype` mentions is expected if none did.
- **Anything before 2026-07-02.**
- **Field behavior from the fixtures.** The 40+ fixture mentions show the names appear in staged scenarios, not in real use.

**Recommendation:** to settle whether re-typing fires the right way, commission `methodology-critique` on this axis (re-typing in the 19 field fires). I have not invoked it.

**Need from you:** decide on the commission.
1. Commission `methodology-critique` on re-typing in the field fires.
2. Leave F1 open.
