Listing every tradeoff across three named options is what `option-shaping` does (a skill that compares options you've named, side by side). I'd hand that to it rather than build the full list here. Your three options are now the candidate set, so nothing needs re-asking.

My read so far: you want to see permanent worktrees, throwaway worktrees, and branches in the one checkout side by side before deciding what you want. Still open: "safe editing" versus "each skill has a home I can find", and whether a weekly sync chore is acceptable.

Here is only the part that bears on those two open questions:

- **Branches in the one checkout:** if that checkout is the one serving live skills, this does not protect it. Switching branches changes the files the live skills read. There is no home per skill.
- **Throwaway worktrees:** editing is safe, and there is no sync chore because each one starts from current main. There is no home either, since a skill has no fixed place between edits.
- **Permanent worktrees:** editing is safe and each skill has a home. The cost is the sync chore, repeated for every skill.

Need from you:
1. Hand the three options to `option-shaping` for the full tradeoff comparison.
2. Stay here and tell me which matters more, safe editing or a findable home. Your answer may rule out one option.
3. Something else.
