# Behavior Smoke Test — `deliberate` end to end (Codex stage agents): v30 exact-prompt re-smoke, accepted close capsule at run 1, the field-not-ready defect did not recur

- **Date:** 2026-07-18 (run commenced; wall clock rolled to 2026-07-19 local mid-run — the run ID `smoke-v30-codex-20260718-r1` was fixed at spawn and preserved verbatim, disclosed, no effect).
- **Target:** the `deliberate` skill bundle (`skills/deliberate/`) at spec v30 — the Codex half of the standing dual-runtime exact-prompt re-smoke the v30 entry owes. Companion Claude record: `2026-07-18_deliberate-v30-claude-end-to-end-re-smoke.md` (which carries the shared environment and method-identity disclosures; this run executed entirely at HEAD `b23e6f0`, clean at every boundary, no ambient advancement observed).
- **Skill source at test:** the live Codex source path `/Users/jp/.agents/skills/deliberate` executed in place, matching the v29 Codex record's shape. Eight-surface v30 method identity as in the companion record; `contract-data-version` 6; 162/162 fixtures at the run's preflight; constituent pins at the three constituent entrypoints (ideate `33deb96f…`, option-shaping `080db567…`, making-recommendations `9b8f183e…`), identical to the Claude half's pins.
- **Input identity:** the exact 3,760-byte fixture, SHA-256 `253f1bfe…`, hash-verified independently by operator and orchestrator; post-`init-setup` byte-exactness gate PASS on both sides (stored `invocation-wording-initial` byte-identical, curly quotes preserved), the operator reading the stored echo item directly.
- **Harness:** the v29 Codex relay shape. A fresh background orchestrator agent ran the contract and helper locally (the lifecycle-notes caveat stands: `codex exec` *invocation* of the skill exits `store unavailable` pre-spend, so the orchestrator holds the store and only the stages run on Codex); at each stage it rendered the brief through the pinned helper, recorded the identifier, and relayed the exact bytes by hash-verified file; the operator launched one fresh, non-forked **Codex CLI** stage agent per stage (`codex exec`, codex-cli 0.144.6, read-only sandbox, envelope captured verbatim via `--output-last-message`, with an explicitly-labeled relay note fixing the return format — raw YAML, no fences, final message only). Effective models at provenance: generate/prune/shape/recommend envelopes self-reported `unknown`; contest `self-reported: GPT-5` (ungrounded); the orchestrator's disagreeing ambient identities (environment block `claude-opus-4-8[1m]` vs session identity `claude-fable-5`) recorded as the disagreement, unresolved.
- **Headline:** run 1 **passes end to end at first attempt**: five stages accepted (`amendments: []` each), **check first** close, terminal `close rendered`, store-backed capsule accepted (`capsule-complete` `b70b364d958cfe475b60df5d989ddbcae5573491360ef224f292fdc391141420`, capsule document 66,033 bytes), independently re-validated storelessly by the operator through the repo path from the capsule extracted out of the user-facing carrier — byte-identical to the orchestrator's copy. **The v29 field-not-ready close-conflict did not recur**; the repaired brief-extra rendered live in the Recommend brief, and the branch itself was not re-taken (see proof boundary).

---

## Run ledger (one run)

- **Run 1 (byte-exact; ACCEPTED):** store `smoke-v30-codex-20260718-r1`, 16 items at close. Generate (17 options: 8 seeds byte-verbatim + 9 generated) → Prune (4 survivors + 13 active records: 11 survivor-budget, 2 constraint; partition exact; no overflow; the leaned candidate cut blind into the excluded share) → Shape (~10K-char rank-free surface, 20 constraint-consequences, four benign isolation encounters recorded — including a withheld-class encounter at the composition-provenance elision, reported rather than inferred) → Recommend (completed; **check first** close — the recommended readiness gate returned as an unperformed side-effecting check under the evidence authorization; empty survivor authority-note branch: `user-visible-lean` limited to seed authorship of the two user-seeded survivors, explicitly registering that it supplies no basis for preferring either) → Contest (exclusion check names three excluded candidates including the user's leaned candidate — the mandatory excluded-favorite challenge fired; most worth contesting: the three-team paid concierge pilot). Helper-call ledger: 17 calls, every one exit 0 (fixtures, check-renderings, init-setup, write-item pins, five render-brief, five validate-envelope --accept, record-proof-inputs, record-terminal, validate-capsule --store --accept); fail-fast never tripped. Store forensically copied (16 items), then trash-retired after the FINAL carrier existed.
- The designed end-to-end lean topology completed visibly: the lean the setup decomposed out of the invocation was cut blind by Prune, withheld from Recommend by the empty authority-note channel, and caught by Contest via `authority-notes-excluded` — the same three-stage arc the v29 records certified, re-fired under v30.

## Disclosed anomaly (tooling; no artifact or containment effect)

- **`__pycache__` import incident:** to derive store-compared capsule values and dry-run capsule validation in-process (a construction aid — authoritative validation stayed the real helper CLI), the orchestrator imported the validator module inside its own scripts. The first import wrote `skills/deliberate/scripts/__pycache__/…`, which the validator's own ADR-0001 census forbids; the orchestrator's self-test script exited 2 on exactly that refusal. Operator ruling, matching the v29 boundary line between the helper's own CLI surface and everything else: the nonzero came from a harness-side script, not a helper CLI invocation, so the categorical fail-fast rule did not trip — the helper ledger is unbroken at exit 0. The `__pycache__` was trashed immediately, the import re-run with `sys.dont_write_bytecode=True`, and every close helper call ran with `scripts/` clean; `__pycache__` is gitignored, so containment (net-Git-visible) was never touched. Operator verified no residue post-run.

## Containment

Behavioral, net-Git-visible snapshot only. HEAD `b23e6f0` and clean porcelain at every stage boundary, orchestrator and operator checks agreeing throughout; net Git-visible change by the run: none. Store outside the worktree; forensic copy taken before retirement; retirement is durable relocation to Trash, no byte-destruction claimed.

## Proof boundary and status

- Together with the Claude record, this supplies both halves of the owed v30 exact-prompt re-smoke; run 1 is the live Codex certification of the v30 method identity on the close-bearing path.
- The v30 repair's live proof is bounded honestly: the repaired brief-extra rendered live in both runtimes' Recommend briefs (the close-less instruction and the validation warning), but neither run took the `field not ready` exit, so the repaired branch end-to-end — exit taken, close-less envelope accepted, field-not-ready capsule terminal reached — remains fixture-proven only (red-first must-block pair plus the must-pass close-less fixture, 162/162). The defect's non-recurrence here is a fact about this run's branch, not a live proof of the repaired branch.
- Evidence bounded to codex-cli 0.144.6 stage agents under read-only sandbox via the relay accommodation, this environment, and these fields; stage model identities self-reported or unknown throughout. Re-run rather than trust this record after any contract edit or method-surface change.

## Durable artifacts

- Ephemeral: relay dirs, prompts, FINAL carrier, forensic store copy in the session scratchpad. The durable proof is this record.
- Prior lineage: `2026-07-18_deliberate-v29-codex-end-to-end-re-smoke.md` (superseded as certification by this record; its run 1 is the defect record the v30 repair answers).
