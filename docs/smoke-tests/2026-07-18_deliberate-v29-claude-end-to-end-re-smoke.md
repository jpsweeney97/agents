# Behavior Smoke Test — `deliberate` end-to-end (Claude/sonnet stage agents): v29 exact-prompt re-smoke, accepted close capsule at run r4, all three v29 repairs live-fired

- **Date:** 2026-07-18
- **Target:** the `deliberate` skill bundle (`skills/deliberate/`) at spec v29 — the owed half of the standing dual-runtime exact-prompt re-smoke the v29 entry names ("three method-identity surfaces changed: the standing exact-prompt dual-runtime re-smoke is owed and unrun at this entry"). Companion Codex record: `2026-07-18_deliberate-v29-codex-end-to-end-re-smoke.md`.
- **Skill source at test:** `main`, HEAD `e41cdb9f2572772d3542c71497375f127173dd44`, clean porcelain at every stage boundary. The commissioning session began at `ffa6036`; a concurrent session landed two simplify-code commits (`e2ac272`, `e41cdb9`) mid-setup — disclosed ambient repo activity, zero deliberate-surface delta (all eight method surfaces re-hashed byte-identical across the advancement). The certification target of this smoke is therefore the eight-surface v29 method identity by hash, not an absolute HEAD: SKILL.md `5cf60d44…`, references/capsule.md `2eccd2f9…`, references/contract-data.yaml `154a2d11…`, references/methods.md `7f0b25dd…`, references/schemas.md `554aca6f…`, references/stage-packets.md `eef5c8f8…`, scripts/_deliberate_shared.py `a7da9a45…`, scripts/deliberate-validate.py `a7f5ba33…`. `contract-data-version` 6. 159/159 fixtures green at operator preflight and again at each run's orchestrator preflight.
- **Input identity:** the exact 3,760-byte `$deliberate` prompt at `docs/smoke-tests/fixtures/2026-07-14-deliberate-exact-prompt.txt`, SHA-256 `253f1bfe697124f685124f03adb539f5f55005284cb4f107de598b2272493a82`, hash-verified independently by operator and orchestrator. New discipline this cycle (forced by run r2, below): after `init-setup`, the stored `invocation-wording-initial` is byte-compared against the fixture file before any pin or render — the accepted run's stored invocation is byte-identical (3,760 bytes, the U+201C/U+201D curly-quote pair on the lean bullet preserved).
- **Effective interpreter:** CPython 3.13.12 selected by uv 0.10.11 via `uv run --script`; macOS 26.5.2 (build 25F84). Same bounded environment as the 2026-07-16 records.
- **Harness:** same relay shape as `2026-07-15_deliberate-recommend-enum-fix-and-success-capsule.md` and the 2026-07-16 Claude record. A fresh background orchestrator agent ran the contract with the fixture verbatim as its invocation through the served path `~/.claude/skills/deliberate` as skill root; at each stage dispatch it wrote the rendered brief bytes to a relay dir and paused; the operator (top-level session) ran one fresh, non-forked **sonnet** stage agent per stage on the exact brief bytes (delivered by hash-verified file, with an explicitly-labeled relay note naming the envelope return path), envelope returned by file, byte-exact. Effective models recorded at provenance per v29: all stage envelopes self-reported `claude-sonnet-5` (corroborated by, not resolved to, the operator's sonnet spawn label); the orchestrator's ambient sources disagree (harness identity `claude-fable-5` vs environment block `claude-opus-4-8[1m]`) and the capsule records the disagreement unresolved — no bare banner value.
- **Headline:** the v29 tree **passes end to end on Claude Code at run r4**: all five stages accepted mechanically with zero concerns amendments, close rendered (clear call), store-backed capsule accepted, capsule independently re-validated storelessly by the operator, and all three v29 repairs (R1 lean-basis close discipline, R2 effective-models provenance, R3 fixture-grain closure + assembled-coherence disclosure) rendered and behaved correctly under a live pipeline for the first time. Three earlier attempts were terminated honestly and are part of this record.

---

## Run ledger (four runs; every termination fail-fast-honest)

- **Attempt 1 (killed at setup, fail-fast):** the orchestrator's first `write-item` call used a nonexistent `--body` flag; argparse exited 2 before any store mutation. Operator ruling: a nonzero exit from the helper process — including a usage error from the helper's own CLI surface — is a nonzero helper result; the categorical rule ("after the first nonzero result do not invoke any helper command again during that run") applies, matching the 2026-07-16 Codex duplicate-render precedent. Non-resumable emergency receipt rendered, store trash-retired, wholly fresh run required.
- **Run r2 (completed mechanically; terminated for input-identity failure):** all five stages accepted, close rendered, store-backed capsule accepted and storelessly re-validated (digest `f285a32f…`) — but post-hoc operator comparison found the capsule's stored `invocation-wording-initial` was 3,756 bytes against the fixture's 3,760: the orchestrator's hand-authored setup document had silently straightened the fixture's two curly quotes (U+201C/U+201D → U+0022) on the lean line. The run was a genuine live fire of the v6 topology and the v29 rules (R1 fired correctly on the empty authority-note branch) but is **not** an exact-prompt run and does not discharge the owed certification. A harness-fidelity failure, not a skill defect — the contract byte-compares everything downstream of the store, but the invocation's fidelity to an external source file is the harness's to prove. The r3/r4 byte-exactness gate exists because of this run. Caught by the Codex-half orchestrator's independent byte-diff, not by the Claude harness — a live demonstration of the v29 theme that certified form can outrun self-supplied content at unvalidated seams.
- **Run r3 (byte-exact; all five stages accepted; killed at the capsule by the validator's own defense):** byte-exactness gate passed; Generate (20 options) → Prune (4 survivors + 16 records, the leaned candidate cut blind) → Shape → Recommend (conditional call; R1-scoped leans) → Contest (both live challenges caught, including the excluded favorite) all accepted with zero amendments. At `validate-capsule` the orchestrator's PyYAML serialization emitted an anchor/alias pair (one Python list object reused at two capsule positions) and the validator refused with exit 2 (`YAML anchors are rejected`) — a live, unplanned fire of the fixture-proven billion-laughs defense, refusing correctly. Fail-fast honored: no corrected re-submission, no diagnostic second probe; emergency receipt (`receipt-complete f489733c…`), forensic store copy, store retired. Operator ruling reaffirmed: an operator-authorized "transport fix" override would have placed a deliberate fail-fast violation inside a certification run.
- **Run r4 (byte-exact; ACCEPTED):** fresh store, fresh sonnet stage agents, serialization hardened (dumper with `ignore_aliases=True` plus deep-copied values — harness hygiene, not a contract change). Generate (17 options: 8 seeds byte-exact + 9 generated) → Prune (4 survivors + 13 records: 6 constraint, 6 survivor-budget, 1 equivalence with seed protection honored — the generated twin was cut, the seed preserved) → Shape (rank-free surface, 7 live questions, 16 constraint-consequences, overlap recorded without merging) → Recommend (clear call for the five-team design-partner cohort seed) → Contest, all `validate-envelope --accept` exit 0, zero amendments. Close order: `record-proof-inputs` → `record-terminal` → `validate-capsule --store --accept`, each exit 0. Terminal `close rendered`; `capsule-complete` digest `ba3dad6926662060ae0aba1f51cf5e331e683013ca7da3fd4491c7fe56468048`; capsule 76,400 bytes, zero anchors/aliases; store (16 items, seq 0–15) forensically copied, then trash-retired after the carrier existed.

## v29 repairs — first live fires (the certification's point)

- **R1 (lean-basis rule):** rendered verbatim in the Recommend brief-extra and obeyed in both byte-exact runs, on exactly the branch it was written for — the empty survivor authority-note set (the user's stated lean rode out at Prune with the excluded interviews candidate). r4's `user-visible-lean` names seed-authorship as its only lean-adjacent signal and explicitly grades it "weak and circumstantial: pre-widening authorship is not a stated preference"; the close contains **no** sentence claiming agreement with the user's visible lean — every user-referencing sentence points at named values or constraints. The pre-v29 failure shape (seed authorship silently substituted as the lean basis, closed with "matches your own visible lean") did not recur.
- **R2 (effective-models provenance):** every capsule model entry is provenance-tagged; the orchestrator's disagreeing ambient identities are recorded as the disagreement, unresolved — the exact repair of the 2026-07-15 run-1 false-value specimen.
- **R3 (what-none-of-it-proves):** present in the accepted capsule with all three required clauses — closure proven at fixture grain only, assembled-capsule cross-section coherence unvalidated, sections stage-local and never reconciled — plus the standing not-proven scope.
- **Contest's exclusion check** caught the excluded favorite in both byte-exact runs (mandatory rule: a visible user preference on an excluded candidate is always a live challenge), and in r4 additionally named a genuine cross-record inconsistency as most worth contesting: the cold-outreach freebie was equivalence-cut as "same underlying reason" as the paid concierge pilot while the surface and close both assert a paid-specific suppression risk the free variant would not carry — a validator-invisible semantic catch, exactly Contest's job.

## Containment

Behavioral, net-Git-visible snapshot only. Porcelain clean and HEAD stable at `e41cdb9` at every stage boundary of every run; net Git-visible change by any run: none. The one HEAD advancement (`ffa6036` → `e41cdb9`) occurred pre-r2 from a concurrent session's unrelated landing and is disclosed above; pin verification before every stage was the mechanical drift detector throughout and never fired. Stores lived under the session scratchpad, outside the worktree. Side effects outside the worktree are unverifiable by this method (disclosed in the capsule).

## Proof boundary and status

- Together with the Codex record, this discharges the v29 entry's owed dual-runtime exact-prompt re-smoke: the 2026-07-16 records certified a superseded tree; these records are the live certification of the v29 method identity.
- Evidence is bounded to macOS 26.5.2 + CPython 3.13.12 + uv 0.10.11, sonnet stage agents, the relay dispatch shape (explicitly-labeled accommodation — the native Claude dispatch shape has still never run), and these runs' fields. Honest-exit branches, drift terminals, and live capsule re-import remain unexercised on Claude. The Codex half's run 1 surfaced an open v1 contract defect on the `field not ready` branch (see that record); the branch was not taken by any Claude run here.
- Re-run rather than trust this record after any contract edit or method-surface change.

## Durable artifacts

- The relay dirs (briefs, envelopes), FINAL outputs, receipts, forensic store copies, and extracted capsules lived in a session scratchpad and are ephemeral; the durable proof is this record.
- Prior lineage: `2026-07-16_deliberate-v6-claude-end-to-end-re-smoke.md` (v6/v28 topology, superseded as certification by this record), `2026-07-15_deliberate-recommend-enum-fix-and-success-capsule.md` (harness shape; its Correction appendix is the R2 specimen).
